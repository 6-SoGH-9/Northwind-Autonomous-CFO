# Northwind — Autonomous CFO Office — Project Handbook

**Status:** Living document. Contains only stable, approved information — not work in progress.
**Maintained by:** Architect/PM (Claude), updated after each reviewed Builder/Test Return Report.
**Handbook version:** 2.8
**Last updated:** Cycle 3 remains closed (D10/D11/D12 Verified, not reopened) and D14 remains Verified — neither is touched by this revision. **v2.8 records that Phase 4–6 v5 scope (the optional-`Commentary.xlsx` behavior and the three-way Phase 7 narrative handoff) is now implemented, regression-verified, and promoted to the canonical repository**, on top of v4's already-promoted foundation, and that D13's independent UAT is now ready to proceed.

**What v2.8 changes (this revision, on top of v2.7's still-standing v4 record below):**
- **v5 scope is implemented and promoted to canonical GitHub**, commit `5709c9e` (parent `1bc750e`, which sits on `789351b`). Independently confirmed via fresh `git clone`: correct HEAD; the three files v5 touches (`Northwind_Financial_Dashboard.py`, `commentary_workflow.py`, `rollups.py`) modified as expected; and — critically — a byte-level diff between `1bc750e` and `5709c9e` that is **identical** to the exact diff the Architect had already independently reviewed, applied to a working copy, and verified before promotion. Nothing changed between review and promotion.
- **Evidence basis:** implementation evidence (Architect direct code review of `build_narrative_commentary_section()` — all three Section G cases, correct priority order, "do not invent a cause" instructions embedded directly in the generated prompt text for both the unresolved and unexplained cases, lexically distinct terminology `UNRESOLVED`/`UNEXPLAINED` with a fixture check confirming neither string leaks into the other); Builder regression evidence (a new fixture, 23/23 checks, independently reproduced byte-identical by the Architect, explicitly testing Criterion 1's byte-identical-when-no-flags claim against the real `rollups.build_user_prompt()` call across three permutations, not just the isolated helper); no regression on v4 (49/49 on the original fixture, independently reproduced) or on any earlier component (18/18 `rollups.py` tie-outs); a baseline AppTest run (0 exceptions, uploader correctly relabeled "Commentary.xlsx (optional)," absence still presented neutrally, never as an error) — performed by the Architect, since none was submitted this round.
- **Known gap, recorded rather than smoothed over: the v5 regression fixture (`build_test/commentary_workflow_v5_demo.py`) was independently reviewed and run by the Architect from the delivered files, but was never committed to canonical** — confirmed by direct inspection of `build_test/` at `5709c9e`; it isn't there. The implementation it validates is genuinely promoted and correct (verified above), but a future session cloning the canonical repo fresh cannot itself re-run this specific regression check. This is a Return Report / canonical-completeness gap, not an implementation defect, and should be closed (fixture committed) at the next opportunity — flagged here so it isn't lost.
- **D13 remains In Progress — still explicitly not Verified.** v5's implementation adds to the same evidence categories D13 already had (implementation, Builder regression, canonical sync) but does not supply the missing fourth: independent Test evidence. **D13 is now ready for its independent UAT** — see the new Section 13 below for exactly what's needed and from whom.
- **Outstanding, unrelated to this revision's substance:** the canonical `governance/builder_briefs/phase4_6_commentary_validation_brief_v5.md` is still the pre-cleanup draft (checksum `c321eaef...`), not the final approved text (`6fbc02cd...`) delivered to and confirmed by the principal. The difference is confined to four header/metadata lines (D13's status label, a Handbook-version cross-reference, two stale "(once active)" qualifiers) — Sections A–K, the actual requirements Builder implemented against, are identical either way, so this did not affect v5's implementation. Still open; batch with the fixture gap above.
- No architectural decision changes. D10, D11, D12, D14 are not reopened. Cycle 3 remains closed.

---

## Current Release

| Field | Value |
|---|---|
| Product target version | v1.0 (Information Architecture frozen per Section 3; updated v2.6 for D14, v2.7 for Phase 4–6 — see notes below) |
| Environment | **Live** (Phase 4–6 implementation promoted to canonical `main`; not yet exercised in the End-to-End Product Demonstration's two scenarios) |
| Current cycle | **Cycle 3 — complete and closed out.** Not reopened. Phase 4–6 is tracked as part of the End-to-End Product Demonstration milestone, not as a new numbered cycle. |
| Current task / milestone | **End-to-End Product Demonstration — in progress.** Phase 4–6 implementation is complete for both v4 scope (commit `789351b`) and v5 scope (commit `5709c9e`) — regression-verified, canonical-synced. D13 is In Progress, not yet Verified — **ready for Section H/J independent UAT**, blocked only on the principal supplying the commentary case content (Section 13 below). |
| Current Builder Brief | **v5 — Active, implementation complete.** Governance file: `governance/builder_briefs/phase4_6_commentary_validation_brief_v5.md` (canonical copy is the pre-cleanup draft, not the final approved text — cosmetic, does not affect what was implemented; see note above). |
| Current status | Cycle 2 (Tasks 1–4), Cycle 3 Task 1, Cycle 3 Task 2, and **D14 are all Verified, promoted to Live.** **Phase 4–6 (D13 scope), both v4 and v5, is implemented, regression-verified, canonical-synced, and In Progress — not yet Verified.** The End-to-End Product Demonstration's two scenarios have **not** been run end-to-end and are not claimed as verified. |
| Last approved milestone | **Phase 4–6 implementation, v5 Brief scope** (superset of v4) — implementation-complete and regression-verified on Architect-independent evidence: direct code review, 23/23 new fixture checks + 49/49 original fixture (both independently reproduced byte-identical), 18/18 `rollups.py` tie-outs (no regression), AppTest baseline, and confirmed canonical synchronization (commit `5709c9e`, byte-diff-verified identical to the exact diff reviewed pre-promotion). Not yet a "Verified" Decision Log status — see D13. |
| Next milestone | **Section H/J independent UAT — ready to proceed, blocked on principal-supplied commentary content** (Section 13). Once reviewed, D13 moves to Verified; then the End-to-End Product Demonstration's two full scenario runs; then the write-up document — the final graded deliverable. |

*A new Builder session should be able to read this table alone and know exactly where the project stands before opening anything else.*

## Current Milestone: End-to-End Product Demonstration

**Objective:** demonstrate the entire product running end-to-end — not just the already-Verified components — across two required scenarios.

**Scenario 1 — Wrong / changed data:**
Data intake → detect the problem (Phase 2/3 validation, D11) → identify/match the issue (Phase 4 — Commentary Matching) → request or handle Controller commentary (Phase 5 — Finance Collaboration) → validate the explanation (Phase 6 — Explanation Validation) → human approval → executive narrative (Phase 7) → HTML story → PPTX (Phase 8)

**Scenario 2 — Correct data:**
Data intake → validation passes (Phase 2/3, no issues raised) → human approval → executive narrative (Phase 7) → HTML story → PPTX (Phase 8)

**Purpose:** prove the product behaves correctly both when something is wrong (full detect → explain → validate → approve chain) and when everything is correct (clean validation straight through to executive output) — using one coherent workflow, not two different code paths asserted to be equivalent.

**Phases 4–6 — implementation status (v4 Brief scope, complete):**
- Phase 4 — Commentary Matching: implemented, regression-verified, canonical-synced.
- Phase 5 — Finance Collaboration (drafted note for the finance/CFO user only, never sent — corrected architecture, Section 6): implemented, regression-verified, canonical-synced.
- Phase 6 — Explanation Validation: implemented, regression-verified, canonical-synced.

All three are built, integrated (`Northwind_Financial_Dashboard.py`, `rollups.py`, `close_history.py`), and promoted to canonical GitHub (commit `789351b`). This is genuinely different in kind from D10/D11/D12's prior integration-of-already-Verified-components work — it was new implementation, and is recorded as such. It is **not yet "Verified"**: D13's Implementation Status is In Progress, pending Section H/J independent Test evidence (Section 5). The governing Builder Brief has advanced to **v5**, which layers two further requirements on top of this completed v4 scope — see below.

**Explicit final outputs of this milestone:** the HTML story and the PPTX executive deck, produced for both scenarios.

**Boundaries:**
- Cycle 3 is not reopened. D10, D11, D12, and D14 remain Verified as recorded in the Decision Log; this milestone does not re-litigate them.
- No settled architectural decision changes as a result of this milestone.
- The end-to-end product is **not** verified by Phase 4–6 implementation alone, and full end-to-end completion is not claimed anywhere in this Handbook. It remains unproven until v5's own requirements are built, D13 reaches Verified on independent UAT evidence, and both scenarios are actually run and reviewed against evidence, per the same standard applied everywhere else in this project (Architect's permanent instructions, Section 4).
- The write-up document is deferred until after this milestone completes successfully, so it can be grounded in an actual demonstrated run of the complete product rather than component-level evidence alone.
- **v5 is now the Active Builder Brief**, adding: `Commentary.xlsx` becomes optional (core workflow fully functional with no file supplied); Phase 7's narrative handoff gains a required three-way distinction (no flags; flags with commentary; flags with no commentary supplied, explicitly reported as unexplained). Neither is implemented yet — that is the next concrete Builder task.
- **D13 (Machine Recommends, Human Decides)** governed the v4 Brief's scope and continues to govern v5's. Confirmed by direct code inspection: no path in the implementation sets or implies close approval/rejection status; every Phase 6 output is Supported/Contradicted/Insufficient only, never a decision.

**Distinction preserved by this milestone (do not collapse these):**
1. **Components already independently Verified and requiring only integration** — D10 (Close History), D11 (Close Validation Engine, Phase 2/3), D12 (canonical dataset), D14 (Cost Structure views), rollups/dashboard, narrative generation, HTML/PPTX generation logic (where it already exists). Unchanged, not re-tested by this milestone.
2. **Phase 4–6 (v4 Brief scope) — implemented, regression-verified, canonical-synced, not yet Verified.** The remaining gap to Verified is independent Test evidence (D13), not missing implementation.
3. **v5's own requirements — implemented, regression-verified, canonical-synced.** Optional-file behavior and the three-way narrative distinction. Not yet Verified — D13 remains In Progress pending independent UAT.
4. **Integration/workflow behavior — not yet demonstrated.** Whether the Verified components, the implemented-but-not-yet-Verified Phase 4–6, and v5's still-unbuilt requirements compose into one coherent, two-scenario run is the open question this milestone still has to answer.
5. **The write-up** — the final graded deliverable, sequenced after this milestone, out of scope for this milestone's evidence standard (per Architect's instructions, Section 15).

## Source of Truth

When any two documents disagree, precedence resolves the conflict in this order:

1. **Project Handbook** (this document)
2. **Current Builder Brief**
3. **Current project code and documentation** (`rollups.py`, dashboard, `assumptions_and_limitations.md`, `northwind_narrative_prompt.md`)
4. **Archived Builder Briefs**
5. **Archived Builder Return Reports**

Archived documents (4, 5) exist for traceability only and must never override the current Handbook. If a discrepancy is found — e.g., code that doesn't match what the Handbook records, or a brief that references a superseded decision — the correct response is to flag it and update the Handbook (or correct the code, if the Handbook is right and the code drifted), never to silently defer to the lower-precedence source. This is the same discipline that caught the narrative-prompt drift in Cycle 2, Task 1 — applied now as a standing rule instead of something rediscovered each time.

**Configuration Integrity Check (standing procedure, added after the Cycle 2 Task 3 drift discovery):** A task is not accepted as Pass on code correctness alone. Every review must also confirm the active project's files are actually synchronized with what's being claimed as current, specifically:
- A case-insensitive (not case-sensitive) terminology resweep across every file the task touches or references, every time — not just after a rename task, and not assumed clean because a prior sweep passed.
- Where a `.md` template mirrors a code-embedded string (e.g., `northwind_narrative_prompt.md` vs. `rollups.py`'s `SYSTEM_PROMPT`), the review confirms **content correspondence** between the two directly — a clean grep on each file separately is not sufficient, since both files can independently pass a banned-terms sweep while still saying different things.
- When a discrepancy is found between an "accepted" state and the file currently in the project, the default recovery path is to **regenerate the drifted file from the verified-correct source already in the project** (usually the code), not to source a fix from an external archive of unconfirmed provenance — an unverified archived copy carries the same risk it's meant to resolve.
- This check does not depend on anyone's memory of which file was uploaded when — it's evidence-based, run fresh, every review.
- **Canonical Sync Verification (added after Cycle 2, Task 4):** A Return Report may only record a fix as "Pass" once the fix is confirmed present in a freshly-pulled copy of the canonical file set that feeds promotion — not merely verified in the session's working copy. A fix that only exists in-session is not yet a fix, regardless of how it was tested. This closes the specific failure mode found in Cycle 2 Task 4: a terminology correction was reviewed and accepted as Pass in Task 3/Item 3, but never propagated to the canonical store, so Test re-encountered the original defect.

**Self-healing boundary (added after Cycle 2, Task 3, Item 3):** if a review's own verification step (e.g., "resweep must show zero matches across all four files") reveals that a file has regressed from a *previously-approved, specific* fix, the Builder may reproduce that exact previously-approved content without waiting for a new brief — provided the fix is reported prominently, not silently absorbed into a "clean" claim. This is narrow: it covers restoring already-agreed content only. Any new label, threshold, structural change, or judgment call is not covered by this exception and still stops and flags rather than proceeds.

**Validation Independence Principle (added Cycle 3, Task 2):** The Builder may not be the sole author of both a piece of validation/detection logic and the test data used to prove that logic works. Doing so creates an unavoidable conflict of interest — logic can trivially "pass" a test whose specific values its own author already knew in advance, without that being evidence the logic is actually generic. This applies wherever Build produces a rule, threshold, or check meant to operate on arbitrary future data (e.g. Phase 2/3 plausibility and deterministic-diff logic). Consequences for roles:
- Builder's responsibility is limited to the generic logic itself and documenting the business rules it implements (thresholds, rationale).
- Builder does not author, extend, or tune test data, injected anomalies, or fixtures meant to prove that logic's correctness going forward.
- Data used to validate genericity must come from outside Builder's authorship — either genuine approved closes from Close History, or a dataset independently constructed by Test/Architect.
- Pre-existing Builder-authored fixtures (e.g. `close_v1_v2_simulation.py`) may still serve a **narrower** purpose — regression continuity, confirming a refactor didn't change behavior versus an already-established baseline — but must not be cited as proof of genericity. The two claims ("didn't regress" vs. "works generically") must be kept explicitly distinct in Return Reports and Handbook records.

This mirrors, at the role level, the same discipline Canonical Sync Verification already applies at the file level: evidence must come from somewhere the claim-maker doesn't fully control.

**Dataset Provenance Discipline (added Cycle 3, Task 2):** A dataset supplied to Test for a real-data validation methodology must be checked for actual coverage range before results are treated as satisfying the methodology's stated scenarios — the file's own contents govern, not its filename or an assumption about what it should contain. Consequences, standing:
- If a supplied dataset's coverage falls short of what a Test Instructions document specifies, Test flags this explicitly and either substitutes the closest genuine equivalent (clearly labeled as a substitution, never silently) or declines to proceed — fabricating synthetic "real" data to fill the gap is never acceptable under a real-data methodology.
- Silent substitution is prohibited. Transparent substitution, disclosed in the Return Report with an explicit mapping of what was asked for versus what was used, is acceptable interim protocol and does not itself constitute a failed test.
- Evidence produced against a substituted or short dataset is **provisional**, not Verified, until re-run against the canonical dataset the instructions actually specified.
- The Architect verifies dataset coverage directly (opening the actual file, checking min/max dates across every sheet the methodology touches) before accepting a Test Return Report's coverage claims as fact — this is the same "artifact overrides narration" discipline applied at the file-content level, not just the filename level.
- **Resolver hygiene corollary (added at Cycle 3 close-out):** once a canonical dataset is confirmed, any prior-round dataset files used only to build up to that confirmation (e.g. intermediate quarter-truncated versions used across a multi-round provenance sequence) must be kept outside the production resolver's search path — archived for evidence/traceability of the provenance chain, never left alongside the canonical file where a wildcard resolver could match more than one. This is a repository-hygiene rule, not a new architectural decision — it follows directly from `rollups.py`'s existing "exactly one match" resolution contract (Section 12).
- This round's sequence (Q2 dataset → flagged gap, substituted → Q3 dataset → flagged remaining gap → Q4 dataset → exact match, no substitution) is the discipline working as designed: Test independently rediscovering and flagging a provenance gap is a protocol success, not a defect to be smoothed over.

---

## 1. Project Vision

An AI-powered quarterly close workflow for a submission to the AI in Finance Challenge (deadline August 18). The differentiator is **not** any single KPI or dashboard page — it is the automation of the analyst-judgment step of financial reporting: validating numbers, flagging anomalies without inventing causes, matching real explanations to flagged items, and only then generating executive-ready narrative and deliverables, with a human approval gate before anything publishes.

The graded deliverable is a **single written document** (problem, workflow, impact). The dashboard, workbook, deck, and HTML story are supporting evidence for that document, not the product being judged in themselves.

**The canonical object this system manages is the Approved Financial Close** (see D10). The dashboard, executive narrative, and board deliverables are outputs generated from an approved close — evidence of what the workflow produced — not independent products in their own right. This distinction is architectural, not cosmetic: it's why the workflow diffs against a prior approved close (Phase 2), uses historical context for plausibility (Phase 3), and retains rather than overwrites each close (Workflow State Model, ending in Archived).

## 2. Product Principles

1. **Every number traces to a real calculation.** Nothing invented, nothing asserted without a corresponding figure in the data.
2. **Don't manufacture a metric the dataset can't support.** If a calculation requires more caveats to use safely than it delivers in insight, the calculation is wrong for the product, not just poorly labeled. (See Decision Log, Cycle 2.)
3. **Name metrics after the business question they answer, not the mechanics of how they're computed.** A CFO should understand a page title without opening it.
4. **Structural prevention over disclaimed caveats.** If a UI layout invites a wrong reading (e.g., a side-by-side % comparison), fix the layout — don't add a warning box next to the wrong reading.
5. **Every dashboard page answers exactly one business question**, has a defined primary audience, and does not overlap another page's job.
6. **Settled architectural decisions stay settled** unless a genuine defect is found — not a preference, not a stylistic reopening.
7. **Evidence over claims.** "Done" and "passed" require the actual output shown, not a status word.
8. **The Handbook is an index and a record of decisions, not a container for evidence.** It records what was decided and why, and points to where the proof lives (a Return Report, a tie-out log) — it does not reproduce that proof. This keeps the Handbook small and durable while keeping every claim in it checkable.
9. **A review checks configuration state, not just code correctness.** A task isn't verified merely because the logic is right — the active project's files must also be checked for drift against what was previously accepted as done. "Passed before, assumed synced since" is not evidence; a review that skips this can approve a project that quietly regressed to an earlier state.

## 3. Information Architecture (v1.0, frozen — page set and business questions unchanged; D14 added evidenced KPIs to an existing page, see Cost Structure row)

| # | Page | Business Question | Audience | Primary KPIs | Flagship / Supporting | Status |
|---|---|---|---|---|---|---|
| 1 | Executive Summary (KPI strip) | How did we do this period, at a glance? | CFO, Board | Revenue, Opex, Op. Profit, Op. Margin vs prior | Flagship | Built |
| 2 | Revenue Performance | Where is revenue growing/shrinking, broad-based or concentrated? | CFO, FP&A | Revenue by region/product, QoQ/YoY %, breadth/concentration, revenue mix % | Flagship | Built (Cycle 2 Task 2 added mix %) |
| 3 | Cost Structure | Which departments drive opex change, and why? | CFO, FP&A | Opex by dept, QoQ/YoY %, S&B volume/rate bridge, breadth/concentration, Opex per Employee by dept; **Department × Cost Category breakdown; company-wide Cost Category-only breakdown** | Flagship | **Built and Verified — D14 (v2.6).** Department × Cost Category and company-wide Cost Category-only views added, Current/Prior/Variance $/%. First IA change since the v1.0 freeze — applied under the Architect's standing authority to record already-approved, evidenced criteria, not a new architectural decision. |
| 4 | Regional Revenue & Go-to-Market Investment | Is GTM spend proportionate to where revenue comes from, and is that shifting? | FP&A (primary), CFO (secondary) | Region Revenue ($), Allocated GTM cost ($), Net of GTM Cost ($); single-region % trend only | Supporting (demoted from flagship in Cycle 2) | Redesigned, built |
| 5 | Product Line Revenue & R&D Investment | Is R&D investment proportionate to revenue by product, and is that shifting? | FP&A (primary), CFO (secondary) | Product Revenue ($), Allocated R&D cost ($), Net of R&D Cost ($); single-product % trend only | Supporting (demoted from flagship in Cycle 2) | Redesigned, built |
| 6 | Headcount & Efficiency | Is headcount growth tracking ahead of, in line with, or behind revenue growth? | CFO, Board | Company-wide Revenue per Employee (trend), headcount growth % vs revenue growth %, headcount by dept (trend) | Flagship | Corrected (Cycle 2 Task 2 — removed dept-level Revenue/Employee) |
| 7 | Budget vs Actual / Variance Review | Which variances against plan need explanation, which are recurring? | CFO, Controller | Largest fav/unfav variances, Major Miss/Watch flags, consecutive-Watch streaks | Flagship | Built |
| 8 | Close Validation Status | Has this close been reconciled, are anomalies investigated, is it safe to publish? | Controller, FP&A | Close state (Workflow State Model), Explained/Partial/Unexplained counts, diff + plausibility flags | Flagship (differentiator) | **Phase 2/3 logic: Built and Verified — Cycle 3, Task 2. Phase 4–6 outcomes (commentary match, four-check validation trace, version history, accept action): Built, regression-verified, canonical-synced (v4 Brief scope) — not yet Verified, pending D13's independent UAT.** The interactive close-approval control (Human Approval Gate) remains unbuilt — see Section 10. |
| 9 | AI Executive Narrative | What's the board-ready explanation of this period? | CFO | Prose, 6 sections (Section 4 = "Investment Alignment," $-only) | Flagship | Built, regenerated Q4 FY2026 vs Q3 FY2026 |
| 10 | Executive Deliverables (HTML story, PPTX deck) | What do I hand to the Board? | Board | No new calc — presentation only | Flagship | **Pending — explicit final output of the End-to-End Product Demonstration milestone, for both scenarios; current files not yet supplied** |

**Explicitly rejected metric:** department-level Revenue-per-Employee (attributes company-wide revenue to a single department's headcount — same structural fallacy as the original Contribution Margin design; see Decision Log).

## 4. Workflow Specification

Reused unchanged from `autonomous_cfo_office_master_brief.md`. Ten phases (0-9), modular, each consuming only the prior phase's defined output:

| Phase | Name | Status |
|---|---|---|
| 0 | Close Trigger | Simulated only (folder/naming convention) |
| 1 | Data Intake & Version Control | Simulated (Close v1/v2), in progress |
| 2 | Deterministic Validation | **Verified — Cycle 3, Task 2.** `close_validation.py` Phase 2 (historical-revision diff detection), independently tested against real data through Q4 FY2026. Its role in the End-to-End Product Demonstration (both scenarios) is not yet exercised. |
| 3 | Plausibility Review (AI) | **Verified — Cycle 3, Task 2.** `close_validation.py` Phase 3 (QoQ threshold + headcount-band plausibility), independently tested against real data through Q4 FY2026. Its role in the End-to-End Product Demonstration (both scenarios) is not yet exercised. |
| 4 | Commentary Matching | **Implemented, regression-verified, canonical-synced (`commentary_workflow.py`, v4 Brief scope) — not yet Verified, pending D13's independent UAT.** Matches imported `Commentary.xlsx` entries against the observation register on identifiable references; returns explicit no-match rather than a forced guess. Required for the End-to-End Product Demonstration's Scenario 1. |
| 5 | Finance Collaboration (draft note) | **Implemented, regression-verified, canonical-synced — not yet Verified.** Reinterpreted per the corrected architecture: drafts a plain-language note for the finance/CFO user's own reference when Phase 4/6 can't proceed; never addressed to, formatted for, or sendable to the Controller; no send path exists in the implementation (confirmed by structural code check). |
| 6 | Explanation Validation | **Implemented, regression-verified, canonical-synced — not yet Verified.** `commentary_workflow.py`'s four-check validator (specific claim present; checkable against the evidence package; supported/contradicted; sufficiently specific to be useful), covering two claim types (`headcount`, `category_reallocation` — the latter genuinely wired to D14's Department × Cost Category breakdown). Outputs Supported (worded as consistency, never proof)/Contradicted/Insufficient, never a close-status decision. Subject to D13. |
| — | **Human Approval Gate** | Between Phase 6 and 7 — no executive output publishes without sign-off on the validated observation register. Required in both scenarios of the current milestone. Per D13, this is a genuine blocking state; no phase or automated agent may advance a close past it. |
| 7 | Executive Insight Engine | Reused (`northwind_narrative_prompt.md`) — existing capability; its use as the final leg of both End-to-End Product Demonstration scenarios is not yet exercised. |
| 8 | Executive Deliverables | Reused (HTML story, deck) — **explicit final output of the End-to-End Product Demonstration milestone, for both scenarios**; current source files not yet supplied |
| 9 | Knowledge Archive | Not started — single log entry only, explicitly framed as future architecture, not a learning system. Not part of the current milestone's two scenarios (neither scenario's flow above includes Phase 9). |

**Workflow State Model** (per close version): Close Received → Data Validated → Observations Generated → Awaiting Controller Input → Explanations Validated → Executive Ready → Published → Archived.

**Simulated close versions (Build/Test fixture only, regression use):** Close v1 = dataset truncated through Q3 FY2026. Close v2 = full dataset through Q4 FY2026 + two injected, exactly-documented changes (a historical revision to a closed period, a Q4 plausibility anomaly). Both clearly labeled as simulated, not real events, in the write-up. **Superseded, for genericity-proof purposes, by the Cycle 3 Task 2 real-data sequential methodology** — see Section 5's Validation Independence Principle and Section 9's Review & Acceptance History.

**Relationship to the current milestone's two scenarios:** Scenario 1 (wrong/changed data) exercises Phases 0/1 → 2/3 (catch) → 4 → 5 → 6 → Human Approval Gate → 7 → 8. Scenario 2 (correct data) exercises Phases 0/1 → 2/3 (no catch) → Human Approval Gate → 7 → 8, skipping 4–6 because there is nothing to explain. Both scenarios terminate at Phase 8; neither exercises Phase 9.

## 5. Product Decisions — Decision Log

**Convention (added Cycle 3):** Decision Log entries are approved architectural direction, effective immediately upon being agreed — not contingent on implementation. Each entry carries an **Implementation Status**: `Planned` / `In Progress` / `Verified`. A decision's own text describes the decision only, never an implementation claim — "Verified" requires a reviewed Return Report with actual evidence, same bar as everywhere else in this Handbook.

| # | Decision | Rationale | Cycle | Implementation Status |
|---|---|---|---|---|
| D1 | Fiscal year = Jul-Jun, labeled by ending calendar year | Dataset's 36 months split into exactly 3 clean fiscal years on this convention | Pre-Cycle 1 | Verified |
| D2 | "Operating Profit/Margin," never "Gross Profit/Margin" | No COGS line in dataset | Pre-Cycle 1 | Verified |
| D3 | Segment allocation: S&M+CS → Region, R&D → Product, by revenue share; G&A excluded from both | No finer driver available (no rep count, no engineer count, no time-tracking) | Cycle 1 | Verified |
| D4 | Professional Services excluded from R&D allocation base | Real allocation error, not a labeling issue — Professional Services is a services line, not something R&D builds | Cycle 1, retained through Cycle 2 | Verified |
| D5 | BvA flag thresholds: Watch >4%, Major Miss >8% | Reverse-engineered from organizer's reference workbook, 100% match | Pre-Cycle 1 | Verified |
| D6 | **Region/Product allocation views redesigned from "margin" to "investment proportionality"** | Any proportional allocation key makes CM% mathematically identical across all segments in a period — not a labeling problem, a structural one. The metric required 5 simultaneous caveats to read safely and invited the one comparison (cross-segment %) that the math forbids. Renamed, cross-segment % removed entirely, replaced with $-only comparison + single-segment % trend. Demoted from flagship to supporting. | **Cycle 2** | Verified |
| D7 | Department-level Revenue-per-Employee rejected; company-wide Revenue/Employee and department-level Opex/Employee kept instead | Attributing company-wide revenue to one department's headcount reproduces the same attribution fallacy just removed in D6 — revenue isn't produced by a single department. Opex/Employee has no such problem (department's own cost over its own headcount). | Cycle 2 | Verified |
| D8 | Revenue Mix (%) added to Revenue Performance page | Independently varying (reflects real mix shift over time), not a ratio constructed from proportional allocation — does not reduce to D6's mathematical-identity problem | Cycle 2 Task 2 | Verified |
| D9 | `rename_instruction_contribution_margin.md` fully superseded | Superseded by D6; do not apply that file's renaming table in any future session | Cycle 2 | Verified |
| D10 | **Canonical object of the Live system is the Approved Financial Close, not a single active dataset. Live maintains an immutable Close History (one snapshot per approved close). The dashboard, executive narrative, and future board deliverables are outputs generated from an approved close, not independent products.** | The workflow already assumed this: Phase 2 diffs against a previous approved close, Phase 3 uses historical context, the Workflow State Model terminates in Archived (retained, not overwritten), Phase 9 was already framed as a Knowledge Archive. Storage mechanism (GitHub, for this challenge) is an implementation choice, not part of the decision. | Cycle 3 | **Verified — Cycle 3, Task 1.** Minimal folder-based Close History (resolution + archival) confirmed: bootstrap path and normal path both independently exercised (not hand-placed), dynamic resolution with zero hardcoded filenames, Phase 2/3 catches reproduced exactly against archived snapshots, no regression (12/12 tie-outs). Full retention/query/lifecycle management remains deferred to Phase 9, per scope. Not reopened by the End-to-End Product Demonstration milestone. |
| D11 | **Close Validation Engine (`close_validation.py` Phase 2 historical-revision diff + Phase 3 plausibility/headcount-band check) is generically correct, not merely non-regressing against Build's own fixture.** | A fresh, memory-independent Test session — given only the governing methodology document, the canonical Q4 FY2026 dataset, `close_validation.py`, and `close_history.py` — ran Scenarios 1-3 and the immutability check against the real, unmodified Q2/Q3/Q4 FY2026 sequence with no substitution, using department/category/value combinations Test chose independently and never disclosed to Build. The Architect independently re-executed the same harnesses against its own copy of the canonical dataset and reproduced every result exactly. This satisfies the Validation Independence Principle's bar for a genericity claim. **Earlier v1/v2 rounds (dataset-short substitutions, and a procedural claim Test had sourced from prior-conversation memory rather than the governing document) are explicitly excluded from this evidence basis — see Section 9.** | Cycle 3, Task 2 | **Verified — Cycle 3, Task 2, on fresh-session evidence.** See Section 9 for full evidence chain. Not reopened by the End-to-End Product Demonstration milestone. (Note: D11's own Scenarios 1-3 are validation-engine test scenarios, distinct from the current milestone's Scenario 1/Scenario 2 product-demonstration scenarios defined in Section 4.) |
| D12 | **Canonical raw dataset is the Q4 FY2026 file (Jul 2023–Jun 2026), single file at repo root. The Q2 FY2026 and Q3 FY2026 intermediate files used during Task 2's provenance sequence are retained only as archived testing/provenance artifacts, outside the production resolver's search path.** | Principal confirmation, resolving the resolver-collision risk flagged at Task 2 close-out: `rollups.py`'s dataset resolver requires exactly one `*.xlsx` match; multiple dated dataset files at repo root would break it. The Q2/Q3 files have ongoing evidentiary value (they support the v1→v2→v3 evidence chain in Task 2's Return Reports) but no ongoing production role once Q4 superseded them. | Cycle 3 close-out | **Verified — principal-confirmed, resolver-hygiene action, not a code change.** Not reopened by the End-to-End Product Demonstration milestone. |
| D13 | **Machine Recommends, Human Decides. Phases 4–6 (Commentary Matching, Finance Collaboration, Explanation Validation) may produce evidence, matches, drafts, and an adequate/inadequate assessment — they must never set close status to Approved or Rejected. The Human Approval Gate is a genuine blocking state requiring explicit human action; no phase, and no automated agent including Claude itself, may advance a close past it. An "inadequate" assessment is a flag and recommendation to investigate, not a rejection — the human retains full discretion to approve despite it, reject, or request further clarification.** | Preserves the standing principle that the machine assists but does not make the final business decision, made explicit and binding ahead of building Phases 4–6, which introduce automated recommendation/assessment logic into this project for the first time. | Current milestone (End-to-End Product Demonstration prep) | **In Progress — not yet Verified; ready for independent UAT.** Phase 4–6 implementation is complete for both v4 scope (`commentary_workflow.py`'s matching/validation core, canonical commit `789351b`) and v5 scope (`build_narrative_commentary_section()`'s three-way narrative handoff, canonical commit `5709c9e`) — integrated across the Dashboard, `rollups.py`, and `close_history.py`. No path anywhere in the implementation sets or implies close approval/rejection status — confirmed by direct code inspection, not narration; every Phase 6 output is Supported/Contradicted/Insufficient only, and Section G's Case 2/3 items are explicitly instructed, in the generated prompt text itself, never to have a cause invented for them. Three of the four evidence categories required for Verified are present: implementation evidence (Architect code review of both v4 and v5 scope), Builder regression evidence (49/49 v4 fixture + 23/23 v5 fixture, both independently reproduced byte-identical by the Architect), and confirmed canonical synchronization (both commits, fresh-clone-verified). **Missing: independent Test evidence — the one remaining requirement.** Per the Validation Independence Principle, genericity cannot be claimed on Builder's own regression fixtures alone. The Section H/J UAT commentary set (principal-supplied, not yet provided) has not been run. **D13 is now ready for that UAT** — see Section 13. |
| D14 | **Cost Structure page (IA #3) gains two new sub-views: a Department × Cost Category breakdown and a company-wide Cost Category-only breakdown. Both show Current / Prior / Variance $ / Variance %.** | Both real, Verified D11 flagged items (Customer Success/Other Opex, Sales & Marketing/Other Opex) are cost-category-level movements the pre-existing Cost Structure page didn't expose — a Controller could not investigate a flagged variance using only what the product showed. Added to support the "flag → Controller investigates → explains" chain required by the End-to-End Product Demonstration milestone, ahead of Phase 4–6 implementation. | Current milestone (End-to-End Product Demonstration prep) | **Verified.** Criteria 1–19 of the v2 (FINAL) Brief evidence-supported and Architect-reviewed directly against actual delivered files: diffs additive-only, 18/18 tie-outs, AppTest/rendered output reviewed, several Variance $/% and reconciliation figures independently recomputed by hand. Canonical GitHub sync independently confirmed via fresh `git clone` (commits `02438bf`/`45c66ca`), byte-identical to reviewed copies. Two dashboard defects found during product review (period-label format; chart chronological ordering) are both confirmed fixed, with the fixes independently re-verified by the Architect, not accepted on narration — see Section 9 and Section 10. `close_history.py`/`close_validation.py` confirmed byte-identical to canonical (previously open as "Partial," now resolved via direct comparison against fresh-cloned canonical copies). Criterion 9 closed as an evidence-retention limitation, not a defect; D11 is not reopened. |

## 6. Assumptions & Limitations (current, approved)

- Headcount is a stock: rollups report **average** (mean of monthly values) and **ending** (last month's snapshot) headcount, never a sum. Starting company-wide headcount (Jul 2023) = 135; ending (Jun 2026) = 167.
- Region/Product "Net of Allocated Cost" views are **two separate, partial views, not a full segment P&L.** Region view nets S&M+CS only; Product view nets R&D only. Neither is directed at a profitability claim — both are investment-proportionality views by design (D6).
- The mathematical-identity property (any proportional allocation key forces % to be identical across segments in a period) is documented as the reason cross-segment % was removed entirely, not disclaimed alongside it.
- No true volume/rate split exists for revenue or non-headcount expense categories (no unit/customer count in the data) — breadth/concentration measure used instead, named for what it is.
- Department-level Revenue-per-Employee is deliberately not built — see D7.
- Real historical closes: Close History is populated only from this project's own Cycle 3 activity to date — not a long production history — stated explicitly as a limitation, not hidden.
- Finance replies are drafted, never sent — confirmed in the implementation by direct structural code check (no send/transmit function exists anywhere in `commentary_workflow.py`). Phase 6 validation's Verified status is intended to rest on Gregory's synthetic commentary (the Section H/J UAT set), not a live reply thread; that set has not yet been supplied and remains load-bearing for D13 moving off In Progress — see Section 10.
- **`Commentary.xlsx` is optional, not a prerequisite for the core workflow** (v5 Brief, principal decision) — the challenge's judging model requires the workflow to run against the required Northwind sample dataset alone, with no assurance a candidate-specific commentary file will ever be supplied. The core workflow (dataset → dashboard → analysis → narrative → Board output) works correctly whether or not one is present — **implemented, regression-verified, canonical-synced (commit `5709c9e`)**, independently confirmed including the flags-exist-with-no-file case specifically. See Section 8, Section 10.
- Cross-close learning: Phase 9 is a single log entry, described as designed architecture, not a mature capability. Phase 9 is out of scope for the current milestone (Section 4).
- `assumptions_and_limitations.md`'s worked-example dollar figures (e.g. Core Platform Net of R&D Cost) are rounded to the nearest dollar for readability; exact pipeline output carries cents (verified match: $1,638,011 documented vs. $1,638,011.09 computed).
- **Open item:** narrative generation's behavior when `ANTHROPIC_API_KEY` is not present (falls back to a "download prompt" button rather than generating text) has been observed in Test but is not yet formally decided as intended Live behavior vs. a gap — see Section 10. Directly relevant to both scenarios' Phase 7/8 leg.
- **Resolved:** the dataset-coverage gap that caused Task 2's Test evidence to be provisional through two prior rounds is closed — the canonical dataset now genuinely spans Jul 2023 through Jun 2026 (confirmed by direct inspection of all 5 sheets, not just the Expenses sheet the test scenarios exercised), and is the single file at repo root (D12).

## 7. Current Build Status

| Component | Status |
|---|---|
| Rollups (Revenue, Expenses, Headcount, BvA), QoQ/YoY | Done, verified, 12/12 tie-outs passing |
| Region/Product Investment views (redesigned) | Done — Cycle 2 Task 1 |
| Headcount & Efficiency correction | Done — Cycle 2 Task 2 |
| Opex per Employee (Cost Structure) | Done — Cycle 2 Task 2 |
| Revenue Mix (%) | Done — Cycle 2 Task 2 |
| Narrative prompt — `SYSTEM_PROMPT`/`build_user_prompt` in `rollups.py` and `northwind_narrative_prompt.md` template | **Done — both confirmed byte-identical in substance (rule 2c, Section 4, data block), Cycle 2 Task 3 Item 3** |
| Dashboard tab structure matching IA names | Done — Cycle 2 Task 1 |
| Close v1/v2 simulation + Close Validation Status page | **Done — Cycle 2 Task 3, verified: exact injected values, Phase 2/3 catch confirmed** (regression-only fixture as of Cycle 3 Task 2 — see Validation Independence Principle) |
| Dashboard dependency pin / Styler API | **Done — Cycle 2 Task 4.** `.applymap` → `.map`, `requirements.txt` added (pandas 2.2.3, numpy 2.5.1, openpyxl 3.1.5, streamlit 1.60.0), confirmed clean under fresh install via script-executing test (8/8 tabs, 0 exceptions) |
| Terminology sync (dashboard + rollups.py) | **Done — Cycle 2 Task 4.** Zero case-insensitive matches across all four files, confirmed against canonical store, not just session copy |
| Broken `.md` reference (lines 410/450) | **Done — Cycle 2 Task 4** |
| Close History resolution + archival (D10) | **Done — Cycle 3 Task 1, verified.** `close_history.py` (storage-neutral resolve/archive) + `rollups.py`'s `find_raw_dataset()` extended with dynamic resolution (env override → Close History lookup → bootstrap fallback). Bootstrap and normal paths both independently exercised via `close_orchestrator.py` (Build/Test only, not Live). Live's own Close History starts empty — the Cycle 3 Task 1 demo snapshots are verification artifacts, not seed data for production. |
| Close Validation Engine — Phase 2 (historical-revision diff) + Phase 3 (plausibility/headcount-band) (D11) | **Done — Cycle 3 Task 2, Verified on fresh-session evidence.** `close_validation.py` reviewed as clean interface (zero I/O, zero fiscal-calendar logic, zero injected literals). A fresh, memory-independent Test session ran 3 scenarios with predictions recorded in advance against the canonical Q4 FY2026 dataset, no substitution; the Architect independently reproduced every result. Zero false positives on clean data, both anomaly classes (historical revision, plausibility swing) caught exactly on cases Build did not construct. See Section 9 for why the earlier v1/v2/v3 rounds are excluded from this evidence basis. |
| Canonical dataset resolution (D12) | **Done — Cycle 3 close-out.** Single Q4 FY2026 file (Jul 2023–Jun 2026) at repo root; Q2/Q3 intermediate files archived outside the resolver's search path. Resolver-collision risk closed. |
| Cost Structure — Department × Cost Category view (D14) | **Done — Verified.** Criteria 1–11 evidence-supported: 18/18 tie-outs (12 pre-existing + 6 new across both views), diffs additive-only, AppTest/rendered output reviewed, Variance $/% figures independently recomputed by the Architect. Canonical GitHub sync confirmed via fresh clone. `close_history.py`/`close_validation.py` confirmed byte-identical to canonical. Criterion 9 closed as evidence-retention limitation (D11 not reopened). |
| Cost Structure — company-wide Cost Category-only view (D14) | **Done — Verified.** Criteria 12–19 evidence-supported: reconciliation checks (Category-only sums to Department × Category totals; grand total reconciles to Department-level total) independently recomputed by the Architect and matched to the cent, AppTest/rendered output reviewed, positioned correctly alongside the Department × Category view. |
| Phase 4 — Commentary Matching (`commentary_workflow.py`) | **Done, regression-verified, canonical-synced — Brief v4 scope.** Matches `Commentary.xlsx` entries to the D11/D14 observation register on identifiable references (explicit or inferable); returns explicit no-match rather than forcing a weak guess; full unaltered entry text preserved into Phase 6. Not yet Verified — see D13. |
| Phase 5 — Finance Collaboration (draft note, `commentary_workflow.py`) | **Done, regression-verified, canonical-synced.** Reinterpreted per the corrected (non-Controller-facing) architecture: drafts a note for the finance/CFO user only, reflecting Phase 4/6's specific failure reason; no send/transmit path exists anywhere in the implementation (structural code check). |
| Phase 6 — Explanation Validation (`commentary_workflow.py`) | **Done, regression-verified, canonical-synced.** Four-check validator (specific claim; checkable; supported/contradicted; sufficiently specific), two claim types (`headcount`, `category_reallocation` — genuinely wired to D14 data, not merely declared). Supported always worded as consistency, never causal proof. Not yet Verified — see D13. |
| Commentary Record / version model (Section F, v4 Brief) | **Done, regression-verified, canonical-synced.** Original commentary never overwritten; each revision a new complete version; every version retains its own independently-computed validation result; exactly one version may be marked Accepted; full record (all versions, all results, accepted-version identifier) captured in the immutable D10 Close History snapshot at close approval — confirmed via `archive_close()`/read-back round-trip, no intermediate version discarded. |
| Dashboard — Commentary Review section (v4 Brief, Section E) | **Done, regression-verified, canonical-synced.** AppTest-confirmed: 0 exceptions, 8/8 tabs, Commentary Review section renders with the `Commentary.xlsx` uploader present (now labeled "optional," per v5). Baseline rendering only — full live widget interaction (upload/edit/resubmit/accept) has not been independently demonstrated via an automated test artifact; an earlier Builder claim to that effect was correctly withdrawn rather than backfilled with unverified evidence. |
| Dashboard/Phase 7 — v5 optional-file behavior and three-way narrative handoff (`build_narrative_commentary_section()`) | **Done, regression-verified, canonical-synced — commit `5709c9e`.** All three Section G cases implemented in correct priority order; `UNRESOLVED`/`UNEXPLAINED` confirmed lexically distinct (dedicated fixture check); "do not invent a cause" instruction embedded directly in the generated prompt text for both cases; Case 1's byte-identical claim tested against the real `rollups.build_user_prompt()` call, not just the isolated helper. New fixture: 23/23, independently reproduced byte-identical. `rollups.py`'s signature change (`commentary_narrative_block`, new) is backward-compatible with v4's `accepted_commentary_lines` parameter, confirmed by v4's fixture still passing unmodified (49/49). **Gap:** the new fixture file was never committed to canonical `build_test/` — reviewed and run from delivered files only; recorded as open, not blocking. |
| Narrative wording defect ("Controller-Approved Explanations") | **Fixed and canonical-synced.** Corrected to "Finance/CFO-Approved Explanations" — the Controller never approves anything in-product; the finance/CFO user does. Confirmed via full-repository sweep, zero remaining occurrences. |
| **Phase 4–6 canonical synchronization** | **Confirmed.** Promoted to `main`, commit `789351b` (parent `8852b4d`). Fresh-clone-verified: correct HEAD, all five expected files present (`Northwind_Financial_Dashboard.py`, `close_history.py`, `rollups.py`, `commentary_workflow.py`, `build_test/commentary_workflow_demo.py`), no temporary patch file committed, clean working tree, diff byte-matches the accepted patch. |
| **End-to-End Product Demonstration — Scenario 1 (wrong/changed data)** | **Not started — current milestone.** Phases 4–6 now exist and are regression-verified, but the full intake → detect → match → validate → approve → narrative → HTML/PPTX chain has not been run as one integrated pass. Blocked on v5's own implementation (optional-file behavior) and D13's independent UAT before this can be claimed proven. |
| **End-to-End Product Demonstration — Scenario 2 (correct data)** | **Not started — current milestone.** Does not depend on Phase 4–6 content, but has not yet been run as an integrated pass through intake → validation → approval → narrative → HTML → PPTX. |
| HTML story, PPTX deck | **Not yet built — explicit final output of the End-to-End Product Demonstration milestone, for both scenarios.** Current source files not yet supplied to Builder. |
| Write-up document (the actual graded submission) | **Not started — deferred until after the End-to-End Product Demonstration milestone completes successfully**, so it can be grounded in a demonstrated run of the complete product rather than component-level evidence alone. |

## 8. Builder Brief History

| Brief | Scope | Status |
|---|---|---|
| Cycle 1 | Rename/allocation fix (Professional Services exclusion), Close v1/v2 planning, decline orchestrator/learning-claims scope | Complete |
| Cycle 2 | Redesign Region/Product views (D6), Headcount correction (D7), Close v1/v2 simulation + Close Validation page | **Complete** — Tasks 1-3 and all follow-ups (line 163 caption, headcount-band rationale, narrative prompt sync) passed. HTML story/deck carved out, remains blocked. |
| Cycle 3 | Task 1 (Close History resolution/archival, D10); Task 2 (Close Validation Engine, Phase 2/3, D11); close-out (canonical dataset selection, D12) | **Complete.** All items Verified. Cycle formally closed. |
| Cost Structure Investigation Views (D14) | Department × Cost Category breakdown and company-wide Cost Category-only breakdown on the Cost Structure page | **Complete.** v2 (FINAL) Brief fully closed — all 19 criteria evidence-supported, both dashboard defects found during review fixed and independently verified, D14 Verified. Superseded v1 and pre-correction v2 draft archived. |
| Phase 4–6 Commentary Workflow — v1 | Initial design; assumed a Controller-facing dashboard/collaboration loop | **Archived — design error, corrected by the principal before implementation.** No implementation was built against v1. Superseded by v2. |
| Phase 4–6 Commentary Workflow — v2 | Corrected: no Controller-facing interface; Controller supplies dataset + separate commentary file externally; finance/CFO user is the sole dashboard operator; complete-commentary-only editing (no fragments); Phase 5 reinterpreted as finance/CFO-facing draft only | **Archived — structural self-audit found 3 non-compliances against the principal's stated deliverable structure** (Section J mislabeled; Phase 6's four checks collapsed to three; Input/Processing/Output labeling missing). Corrected in v3. |
| Phase 4–6 Commentary Workflow — v3 | Same product design as v2, structural corrections applied | **Archived — superseded by v4**, which fixed the two remaining open product-definition questions (commentary-file format; Commentary Record persistence relative to D10). |
| Phase 4–6 Commentary Workflow — v4 | Fixed `Commentary.xlsx` contract (sheet `Commentary`, columns `Commentary_ID`/`Commentary_Text`); full Commentary Record version history captured in the D10 snapshot at close approval | **Implementation complete, regression-verified, canonical-synced (commit `789351b`).** Two Architect-review correction rounds were required before acceptance: (1) claim-type coverage gap — the D14 evidence package field was present but unused by any validator, fixed by wiring `category_reallocation` to real D14 data; narrative wording defect ("Controller-Approved" → "Finance/CFO-Approved"); (2) canonical sync not yet pushed and a truncated regression-evidence artifact — both resolved in the final submission (49/49 regression, independently byte-verified; canonical sync independently confirmed via fresh clone). Not yet Verified — D13 remains In Progress pending Section H/J independent UAT. **Superseded as the active governing Brief by v5**; v4's implementation is not reopened or redone by v5, which adds new requirements on top of it. |
| Phase 4–6 Commentary Workflow — **v5 (Active, implementation complete)** | Adds to v4: `Commentary.xlsx` becomes optional, not a prerequisite for the core workflow; Phase 7's narrative handoff gains a required three-way distinction (no flags; flags with commentary, some possibly unresolved — reported, not omitted; flags with no commentary supplied at all — explicitly reported as unexplained, never invented) | **Implementation complete, regression-verified, canonical-synced — commit `5709c9e`.** Two review rounds: the Architect's independent review (diff applied to a working copy, both fixtures re-run, byte-compared, AppTest run independently since none was submitted) preceded promotion; a post-promotion fresh-clone confirmation then verified the promoted diff is byte-identical to the exact diff already reviewed — nothing changed between review and promotion. One gap recorded, not blocking: the new regression fixture (`commentary_workflow_v5_demo.py`) was reviewed and run from the delivered files but never committed to canonical `build_test/`. Does not reopen v4's implementation or any of D10/D11/D12/D14. Not yet Verified — D13 is now ready for its independent UAT (Section 13). |
| End-to-End Product Demonstration (full milestone) | Integration of all phases into two scenario runs (wrong/changed data; correct data), ending in HTML story and PPTX for each | **Not yet scoped as its own Brief.** Depends on D13 reaching Verified (Section H/J independent UAT) first — both v4 and v5 implementation are complete, this is no longer blocked on Builder work. |

*(Full brief text for v1–v3 archived externally by PM, not retained in Project once superseded. v4 and v5 text retained in `governance/builder_briefs/`.)*

## 9. Review & Acceptance History

| Task | Criteria checked | Result | Evidence |
|---|---|---|---|
| Cycle 2, Task 1 | 1, 3, 4 | **Pass** | 12/12 tie-outs, zero diffs; grep swept twice (3 comment-only matches found and fixed, then zero); tab renames applied and confirmed via `st.tabs()` grep |
| Cycle 2, Task 2 | 5, 6 | **Pass** | 12/12 tie-outs, zero diffs; structural verification that dept-level Revenue-per-Employee is unconstructable (no revenue column in dept table, no Department column in company-wide table), not just absent from a text search |
| Cycle 2, Task 3 core | 7 (Close v1/v2 injected values, Phase 2/3 catch) | **Pass, accepted** | Exact injected values verified against CSVs; Phase 2 caught the 1 historical revision exactly; Phase 3 caught the 1 plausibility anomaly exactly, no cause guessed; dashboard state display honest (no Phase 4-9 overclaim) |
| Cycle 2, Task 3 follow-up, Items 1-2 | 4 (terminology), documentation | **Pass** | Line 163 before/after clean; assumptions-log entry quoted in full; 12/12 tie-outs, no regression |
| Cycle 2, Task 3 follow-up, Item 3 | 4 (terminology consistency, content correspondence) | **Pass, but scope-limited — see Task 4.** The fix was correctly made and verified in-session; it did not reach the canonical file store, so Test (Cycle 2 batch run) re-encountered the original defect. Not a false Pass on the work done — a gap in the promotion pipeline itself. Resolved by Cycle 2 Task 4 and the new Canonical Sync Verification procedure (Section 0). | Zero matches, case-insensitive, all four files; rule 2c/Section 4/data-block confirmed byte-identical to code, not just absent of banned terms; 12/12 tie-outs, no regression |
| Cycle 2, Task 4 | Dependency pinning, terminology sync (canonical), broken reference, no regression | **Pass** | 8/8 tabs render, 0 exceptions (AppTest, fresh install); 6→0 terminology matches across all 4 files, verified against canonical store; 12/12 tie-outs + both Close v1/v2 injections exact under pinned env; `.md`→`.py` references corrected; root cause of Task 3/Item 3's non-propagation identified and distinguished from the Section 10 pattern |
| Cycle 2 — Test Verification | Full Section 6 criteria, fresh env | **Pass** | 12/12 tie-outs $0.0000; 8/8 tabs, 0 exceptions (AppTest); pinned deps confirmed active; Phase 2/3 catches exact; terminology zero matches (independently re-swept); batch hashes recorded for traceability |
| Cycle 3, Task 1 | D10 — Close History resolution + archival, bootstrap and normal path, no regression | **Pass** | Bootstrap path forced via hard assertion (Close v1 could not be hand-placed); dynamic resolution confirmed zero hardcoded filenames (independent grep); Phase 2/3 catches reproduced exactly against archived snapshots; `metadata.json` includes real git commit hash per snapshot; 12/12 tie-outs, no regression; dashboard re-verified 8/8 tabs clean with Close History populated; `close_orchestrator.py` correctly excluded from Live scope and documented as Build/Test-only, confirmed via re-run after doc addition |
| Cycle 3, Task 2 | D11 — Close Validation Engine genericity, Validation Independence Principle, Dataset Provenance Discipline | **Pass — Verified, on fresh-session evidence.** | Implementation reviewed clean (zero I/O, zero fiscal-calendar logic, zero injected literals, independently re-verified by grep). **Historical record (v1/v2/v3, NOT part of D11's evidence basis):** an earlier Test session ran this methodology across three rounds as dataset coverage improved — v1 (dataset short, through Q2 FY2026 only) substituted the last 3 available real quarters and flagged the gap; v2 (dataset extended to Q3 FY2026) flagged the remaining gap; v3 (dataset extended to Q4 FY2026) ran the real, unmodified Q2/Q3/Q4 FY2026 window with no substitution and all three scenarios passed. Under direct questioning, that same session admitted its stated justification for the v1 substitution ("document gaps as an open item, don't fabricate data") had been asserted as if it were a rule written in `test_instructions_validation_methodology.md`, when it was in fact carried in from prior-conversation memory — the governing document contains no such contingency clause. This was a genuine procedural-independence failure, not a wording slip, and disqualifies v1 and v2 as authorized runs; it also means v3's own procedural claims (sandbox isolation, immutability, prediction timing) could not be trusted on that session's narration alone, even though v3 needed no substitution. **Fresh evidence actually underlying Verified status:** a new Test session, given only `test_instructions_validation_methodology.md`, the canonical Q4 FY2026 dataset, `close_validation.py`, and `close_history.py`, explicitly instructed to disregard all prior-conversation memory, prior Return Reports, prior test results, and prior datasets, independently re-derived and re-ran Scenarios 1-3 and the immutability check against the real, unmodified Q2/Q3/Q4 FY2026 sequence — predictions recorded before execution, department/category/value combinations chosen independently and never disclosed to Build, isolated sandboxes, full harness scripts and sandbox trees supplied as evidence. **The Architect independently re-executed the same harnesses against its own separately-obtained copy of the canonical dataset** (not the fresh session's sandbox copies) and reproduced every result exactly: Scenario 1 (0/0 flags at every step), Scenario 2 (Customer Success/Other Opex, $22,675.64→$27,675.64, 1 Phase 2 flag, 0 Phase 3 flags — exact match), Scenario 3 (Sales & Marketing/Other Opex, $47,857.52→$97,857.52, 0 Phase 2 flags, 1 Phase 3 flag at QoQ +38.42%/headcount change +1.0 — exact match), and immutability (`FileExistsError` on re-archiving an existing period label, triggered directly by the Architect, not just read from a report). The Architect also confirmed by direct code inspection that `archive_close()`'s `rollups_output_src` parameter is only copied to storage and never read by any Phase 2/3 logic, so the documented placeholder used for it has no bearing on the validation evidence. This — the fresh session's evidence plus the Architect's independent, code-level reproduction of it — is what D11's Verified status rests on, not the earlier v1/v2/v3 sequence. (These "Scenario 1/2/3" labels are the D11 validation-engine test scenarios, not the current milestone's Scenario 1/Scenario 2 product-demonstration scenarios defined in Section 4.) |
| Cycle 3 close-out | D12 — canonical dataset file selection, resolver-collision hygiene | **Pass — Verified.** | Principal confirmed the Q4 FY2026 file as sole canonical dataset at repo root; Q2/Q3 files to be retained only as archived provenance artifacts outside the resolver's search path. Not a code change — a repository-hygiene action within the principal's data-provenance authority (Section 8 of the Architect's instructions). |
| Cost Structure Investigation Views (D14) | Criteria 1–19 of the v2 (FINAL) Brief, plus two dashboard defects found during product review | **Pass — Verified.** All 19 criteria evidence-supported and Architect-reviewed directly against delivered files: 18/18 tie-outs, diffs additive-only, AppTest/rendered output reviewed, Variance $/% and reconciliation figures independently recomputed by the Architect (including both Category-only reconciliation checks, Criteria 12–13, matched to the cent). Canonical GitHub sync confirmed via fresh `git clone` (commits `02438bf`/`45c66ca`), byte-identical to reviewed copies. `close_history.py`/`close_validation.py` confirmed byte-identical to canonical, resolving the long-open "Partial" provenance status. **Two dashboard defects found during product review, both confirmed then fixed then independently re-verified:** (1) global period-label format (`Q4 FY2026` → `Q4 2026`) — fixed via display-layer reformatting, verified by full-text sweep (zero remaining `FY` occurrences) and real AppTest rendered output; (2) full-trend chart x-axis chronological ordering — a pre-existing defect (not introduced by the label fix; confirmed by direct Streamlit/Vega-Lite source inspection that alphabetical and chronological order were never equivalent for either label format), affecting all six `st.line_chart()` calls plus one previously-unidentified full-trend bar chart Builder found during a full-dashboard search — fixed via ordered `pd.Categorical` axis, verified by independently reproducing Streamlit's actual type-inference (`nominal`→`ordinal`) and the real generated Vega-Lite spec, not code inspection alone. Both fixes: zero calculation drift (`rollups.py` byte-identical pre/post the chart-ordering fix; diffs confirm display-layer-only changes), 18/18 tie-outs maintained, 0 exceptions/8 tabs. Criterion 9 closed as an evidence-retention limitation, not a defect — the original injected test value (May 2026 Sales & Marketing/Other Opex, +$50,000) was precisely reconstructed and independently reconciled to the cent, but the underlying artifact was never persisted and cannot be recovered (exhaustive search including Git history); D11 is not reopened and remains Verified. | Full evidence chain in Builder Return Reports and Architect review notes across this task's rounds; not reproduced here per Handbook Principle 8 (index, not evidence container). |
| Phase 4–6 Commentary Workflow — v4 Brief implementation, correction round 1 | Claim-type coverage (D14 evidence usability), narrative wording defect, canonical sync, evidence-artifact integrity, AppTest interaction claim | **Not Pass — sent back.** Initial submission: 42/42 regression (own fixture only, matching v4's own frozen fixture), 0-exception AppTest baseline — but three defects found on independent review, not narration: `category_reallocation`-type gap (D14 breakdown row present in the evidence package but never read by any validator — only `headcount` claims were ever checkable), the narrative wording defect ("Controller-Approved" — architecturally wrong, the Controller never approves anything), and no canonical sync at all (commit existed only in Builder's local sandbox, confirmed absent from GitHub via direct fetch attempt). Also flagged: no structured Return Report; an unsupported commit-message claim of full live-widget AppTest coverage with no test artifact behind it. |
| Phase 4–6 Commentary Workflow — v4 Brief implementation, correction round 2 | Same four items, re-submitted | **Not Pass — sent back again.** `category_reallocation` genuinely fixed this round (verified: wired to real D14 data, 49/49 regression reproduced byte-identical by the Architect against the applied patch) and the wording defect genuinely fixed (zero remaining occurrences, swept). But: still not pushed to canonical GitHub, and the submitted regression-evidence artifact (`regression_fixture_output_49checks.txt`) was itself truncated — missing the final 11 D10-persistence checks and the `RESULT` line entirely. Only known to actually be 49/49 because the Architect independently re-ran it and it completed cleanly; the artifact handed over as proof did not itself prove the claim. |
| Phase 4–6 Commentary Workflow — v4 Brief implementation, final acceptance | Same four items | **Pass — implementation-complete and regression-verified.** `category_reallocation` fix and wording fix reconfirmed unchanged. Regression-evidence artifact this round (`regression_evidence_49checks_COMPLETE.txt`) is complete — 193 lines, all D10 checks present, ends with `RESULT: 49 passed, 0 failed` — and is **byte-identical** to the Architect's own independent re-run against the applied patch. AppTest interaction claim correctly resolved by withdrawal, not fabrication: the commit message dropped the unsupported "live widget interaction" claim and now states only what `apptest_baseline_evidence.txt` actually supports (0 exceptions, 8/8 tabs), which the Architect also independently reproduced. Canonical sync was disclosed as blocked by environment constraint (no push credentials in Builder's sandbox — verified `git push` failure message), not a compliance failure; patch confirmed to apply cleanly against actual current canonical HEAD via non-destructive dry run, twice, across two independently fresh clones. **A separate, inspection-only repository-readiness check** (before promotion) independently confirmed: canonical HEAD, governance documents present and current (v4 Brief byte-identical to the text reviewed throughout), all v4-prerequisite files present, patch compatibility via non-destructive `git apply --check`, and zero v5-scope leakage. **Promotion itself** was then independently confirmed via fresh clone: new HEAD `789351b`, all five expected files present, no temporary patch file committed, clean working tree, and a byte-level diff match against the accepted patch (only a `git format-patch` version footer differed). | Full evidence chain (repeated independent clones, patch applications, regression re-runs, AppTest runs, and diffs) produced across this review cycle; not reproduced here per Handbook Principle 8 (index, not evidence container). |
| Phase 4–6 Commentary Workflow — v5 Brief implementation | Optional `Commentary.xlsx`; three-way Section G narrative handoff (no flags / flags+file / flags+no-file); backward compatibility with v4's `rollups.py` interface | **Pass — implementation-complete, regression-verified.** New fixture (23/23) independently reproduced byte-identical, including a direct test of Criterion 1's byte-identical claim against the real `build_user_prompt()` call across three permutations, and a dedicated lexical-distinctness check confirming `UNRESOLVED`/`UNEXPLAINED` never leak into each other. v4's fixture (49/49) reconfirmed unchanged — the new `commentary_narrative_block` parameter is additive, not breaking. 18/18 `rollups.py` tie-outs, no regression. AppTest baseline run by the Architect (none submitted by Builder): 0 exceptions, uploader relabeled "optional," absence still presented neutrally. Diff independently applied to a working copy and verified *before* promotion; a post-promotion fresh-clone confirmation then verified the promoted commit (`5709c9e`) is byte-identical to that exact reviewed diff. **Two gaps recorded, neither blocking:** the new regression fixture was never committed to canonical `build_test/` (reviewed and run from delivered files only); no Return Report or AppTest evidence was submitted by Builder this round (the Architect supplied the latter independently, as in the first v4 round). | Full evidence chain (fresh clone, diff application, fixture re-runs, byte-comparisons, AppTest, post-promotion re-verification) produced across this review; not reproduced here per Handbook Principle 8 (index, not evidence container). |
| End-to-End Product Demonstration (full milestone) | Not yet reviewed | **Not started.** Both v4 and v5 implementation are complete; this now depends solely on D13 reaching Verified (Section H/J independent UAT, Section 13) — no further Builder implementation work is a precondition. This row will be populated once the demonstration itself begins and produces evidence. | — |

**Cycle 2 close-out: all 9 Section 6 criteria met** for every artifact that currently exists (pipeline, dashboard, narrative prompt, close simulation, assumptions doc). Criteria 3-4 have no finding against the HTML story or PPTX deck because those artifacts don't exist yet — carved out of this promotion, not a failure of it.

**Cycle 3, Task 1 close-out:** D10 implementation status moved to Verified. Live's canonical data model is now the Approved Financial Close, with a minimal, storage-neutral resolution/archival layer in place. Full retention, querying across many closes, and lifecycle management remain deferred to Phase 9, per the task's explicit scope.

**Cycle 3, Task 2 close-out:** D11 implementation status moved to Verified. The Close Validation Engine (Phase 2 + Phase 3) is proven generically correct — not merely non-regressing — via a fresh, memory-independent Test session's evidence, independently reproduced by the Architect using the same code and harnesses against a separately-obtained copy of the canonical dataset. The Dataset Provenance Discipline this task's earlier v1/v2 rounds motivated adding to this Handbook remains standing procedure and is credited with surfacing the original coverage gap as a protocol success. Separately, the earlier session's admission that a stated justification for its v1 substitution had been sourced from prior-conversation memory rather than the governing document — not caught until directly challenged — is recorded as a genuine finding, not smoothed over: it is why v1/v2/v3 are retained here as historical record but excluded from D11's actual evidence basis, and why the fresh session was run under an explicit no-prior-memory instruction and then independently re-verified rather than accepted on its own narration.

**Cycle 3 close-out (full):** D12 implementation status moved to Verified. The resolver-collision risk flagged when the third dataset file arrived is resolved by principal decision: single canonical Q4 FY2026 file at repo root, Q2/Q3 files archived outside the resolver's search path. **Cycle 3 has no open items.** All three of its decisions (D10, D11, D12) are Verified and are not reopened by the End-to-End Product Demonstration milestone.

**Cost Structure Investigation Views (D14) close-out:** D14 implementation status moved to Verified. Both new Cost Structure views (Department × Cost Category; company-wide Cost Category-only) are Built and Verified on evidence the Architect independently reviewed and, in several places, independently reproduced (tie-outs, rendered output, hand-recomputed figures) rather than accepted on narration — the same standard applied to D10/D11/D12. Two dashboard defects surfaced during product review (period-label format; chart chronological ordering) were confirmed, fixed, and independently re-verified before this close-out, including one defect (chart ordering) found to pre-date this task entirely and affecting the dashboard globally, not just the new views. The long-open `close_history.py`/`close_validation.py` provenance question is resolved. Criterion 9 is closed as a documented evidence-retention limitation; D11 is not reopened and remains Verified.

**Phase 4–6 (v4 Brief) close-out:** implementation status moved to **Done, regression-verified, canonical-synced — not yet Verified.** `commentary_workflow.py` (Phase 4/5/6 logic) and `build_test/commentary_workflow_demo.py` (Builder's own regression fixture) are new files, promoted to canonical GitHub at commit `789351b`; `Northwind_Financial_Dashboard.py`, `close_history.py`, and `rollups.py` were modified in place. Two Architect-review correction rounds were required (Section 9) before acceptance. D13's Implementation Status moves from Planned to **In Progress**: implementation evidence, Builder regression evidence, and canonical synchronization are all independently confirmed; independent Test evidence (Section H/J UAT) is the one remaining requirement for Verified, and has not yet been supplied. The governing Builder Brief advances from v4 to **v5** (Active), which adds new requirements — v4's own scope is not reopened by this transition.

**Status: promoted to Live.** Batch = `rollups.py`, `Northwind_Financial_Dashboard.py`, `northwind_narrative_prompt.md`, `assumptions_and_limitations.md`, `requirements.txt`, `close_history.py`, `close_validation.py`, `commentary_workflow.py`, `Northwind_Sample_Dataset.xlsx` (Q4 FY2026, single canonical file). **Excluded from Live** (Build/Test verification tools only): `close_v1_v2_simulation.py`, `close_orchestrator.py`, `build_test/commentary_workflow_demo.py`, and the archived Q2/Q3 dataset provenance artifacts. HTML story and PPTX deck remain not yet built, and are now explicit final outputs of the End-to-End Product Demonstration milestone. **D14 is Verified and is part of the Live batch** (carried within `rollups.py`/`Northwind_Financial_Dashboard.py`, already listed above — no new files introduced). **`commentary_workflow.py` is part of the Live batch as implementation-complete and regression-verified; D13 itself is In Progress, not yet Verified,** pending Section H/J independent UAT.

## 10. Known Technical Debt

- **Phases 4–6 are fully implemented (v4 and v5 scope) and regression-verified, but D13 is not yet Verified — the remaining gap is independent Test evidence, not missing code.** `commentary_workflow.py` and `build_narrative_commentary_section()` are complete, integrated, canonical-synced. What's still open is the same requirement that gated D11's Verified status: genericity cannot rest on Builder's own regression fixtures alone (Validation Independence Principle). The Section H/J UAT commentary set — three principal-supplied cases (Supported/Contradicted/Insufficient) plus a correction-loop revision, per v5 Section H also exercised against Section G Case 3 (no file supplied) — has not yet been supplied. **This is now the single blocker for D13 reaching Verified and for the End-to-End Product Demonstration's Scenario 1 to run as genuine evidence rather than against Builder's own fixture.** Per principal direction, the principal will supply the actual commentary text directly; the Architect specifies required characteristics only (Section H) and does not author it, preserving Validation Independence at the content level. See Section 13.
- **v5 regression fixture not committed to canonical `build_test/`.** `commentary_workflow_v5_demo.py` (23/23 checks, independently reproduced by the Architect) was reviewed and run from Builder's delivered files, not from a canonical clone — confirmed absent from `build_test/` at commit `5709c9e` by direct inspection. The implementation it validates is genuinely promoted and correct; this is a canonical-completeness gap only (a fresh session cloning the repo can't itself re-run this specific check), not an implementation defect. Should be closed at the next Builder touch-point.
- **v5 governance file drift, canonical vs. approved text.** The canonical `governance/builder_briefs/phase4_6_commentary_validation_brief_v5.md` (checksum `c321eaef...`) is the pre-cleanup draft, not the final text the Architect delivered and the principal approved (`6fbc02cd...`). Confined to four header/metadata lines (D13's status label, a stale Handbook-version cross-reference, two leftover "(once active)" qualifiers) — Sections A–K, the actual requirements Builder implemented against, are identical either way, so v5's implementation is unaffected. Cosmetic but should be corrected in a future governance-only commit.
- **Period Label Format defect — resolved, D14 close-out.** Global display-layer defect (labels rendered as `Q4 FY2026`; required `Q4 2026`) confirmed during product review, fixed via display-only reformatting (`rollups.py` unchanged), and independently re-verified by the Architect: full-text sweep confirmed zero remaining `FY` occurrences in any rendered label, real AppTest output showed corrected labels live across all affected views including both D14 views, and the fix was confirmed to have zero effect on any financial calculation, tie-out, or Variance figure. Canonical GitHub sync confirmed via fresh clone.
- **Full-trend chart chronological-ordering defect — resolved, D14 close-out.** A separate, pre-existing defect (not introduced by the period-label fix — alphabetical and chronological order were never equivalent for either label format, confirmed by direct computation) causing all six `st.line_chart()` full-trend charts, plus one previously-unidentified full-trend bar chart (`pivot3`, "Expenses by Department — full trend," found by Builder during a full-dashboard search per the Brief's instruction not to assume only the known charts were affected), to render in alphabetical rather than chronological order. Root cause confirmed by direct inspection of Streamlit's installed source: `st.line_chart()` assigns a `"nominal"` Vega-Lite type to plain string axes and never applies a sort override for line charts (sort logic is coded to apply only to bar charts), so Vega-Lite's default ascending/alphabetical sort took over regardless of the underlying DataFrame's row order. Fixed via an ordered `pd.Categorical` axis, which Streamlit's own type-inference logic special-cases to `"ordinal"` typing (confirmed both by reading Streamlit's source, which cites this as an intentional documented pattern — `streamlit/streamlit#7776` — and by independently reproducing the actual generated chart spec). Confirmed zero calculation drift: `rollups.py` byte-identical before and after this fix; dashboard diff confirmed additive/display-layer only.
- **Human Approval Gate's interactive control — still a real, open gap.** The Close Validation Status page has no close-level approval `st.button` behind it. This was correctly out of scope for D14, and remains explicitly out of scope for the v4 Brief (Section I: "The close-level Human Approval Gate's interactive control... remains separately-scoped open technical debt") — Phase 4–6 being implemented does not close this gap. The Commentary Record's per-observation Accept action (Section E/F, v4 Brief) is a distinct, narrower human action (accepting one commentary version) and is not a substitute for the close-level gate. Building the close-level control remains unscoped, governed by D13 once addressed.
- **Criterion 9 (Phase 2/3 D11 reproduction for the Cost Structure task) — resolved, evidence-retention limitation, not a defect.** The archived Q2/Q3 FY2026 vintage files were supplied and run through the real, unmodified `close_history.py`/`close_validation.py` path; the original injected anomaly (a single-month +$50,000 value, Sales & Marketing/Other Opex, May 2026) was precisely reconstructed and independently reconciled against D11's recorded evidence to the cent, but the underlying artifact itself was never persisted anywhere accessible (confirmed by exhaustive search including Git history) and cannot be recovered. D11's Verified status is not affected and is not reopened.
- **Documentation/implementation grain mismatch — open, non-blocking (identified during Cycle 3, Task 2 D11 review).** `test_instructions_validation_methodology.md`'s Input Contract describes `Period` with a quarter-grain example (`"FY2026-Q2"`), while `close_validation.py`'s own docstring names monthly-grain `Date` as "the expected production usage" for Phase 2. The methodology document itself states the engine never parses or validates the `Period`/key-column values, so this did not invalidate any test result — Phase 2's monthly-grain usage in both the disqualified v1/v2/v3 rounds and the fresh evidence run is directly supported by the code's own stated default and docstring, checked line-by-line. This is flagged as a documentation-alignment item (the Test Instructions' example should be updated to match the implementation's actual recommended grain), not a functional defect, and does not block D11.
- **Test procedural-independence finding — resolved via fresh session, Cycle 3 Task 2.** An earlier Test session, when directly challenged, admitted that its stated justification for substituting quarters in its first (v1) round — framed as "the document's own explicit principle" — had actually been carried in from a prior conversation's memory, not from the text of `test_instructions_validation_methodology.md` as supplied that session. The governing document contains no such contingency clause. This is recorded as a genuine procedural-independence failure: memory-derived assumptions were presented as document-mandated rules. It was not caught by the Architect's own review until specifically pressed on it, which is itself a gap in that review, not just Test's. Resolved for Task 2 by commissioning a fresh Test session under an explicit no-prior-memory instruction, whose results the Architect then independently reproduced end-to-end rather than accepting on narration (see Section 9). The three original Return Reports (v1/v2/v3) are retained as historical record but are explicitly excluded from D11's evidence basis. **Open question for future Test/Architect sessions on this project:** whether "fresh session, no prior-conversation memory, independently reproduced by the Architect" should become a standing requirement for any Return Report used to support a Verified/genericity claim, rather than a one-off recovery for this incident — not decided here, flagged for the principal.
- **Narrative prompt drift — resolved, Cycle 2 Task 3 Item 3.** `northwind_narrative_prompt.md` confirmed byte-identical in substance to `rollups.py`'s `SYSTEM_PROMPT`/`build_user_prompt` for rule 2c, Section 4, and the data block. Root cause was a project synchronization gap (the `.md` never received Task 1's edit in the copy carried forward across project restarts), not a pipeline defect. Recovery followed the Configuration Integrity Check procedure: regenerated from the verified-correct code, not from an unverified archive.
- **Promotion-pipeline sync gap — resolved, Cycle 2 Task 4.** A previously-accepted fix (Task 3/Item 3 terminology correction) was verified in a Builder session but never written to the canonical file store, so it did not appear in the batch Test actually received. Distinct from the earlier narrative-prompt drift (stale copy carried across restarts) — this was a fix that simply never left the session it was made in. Addressed structurally via the new Canonical Sync Verification procedure, not by re-fixing this instance alone.
- **Dataset provenance gap — resolved, Cycle 3 Task 2.** Test Instructions assumed a dataset through Q4 FY2026 while the file actually supplied initially only ran through Q2 FY2026. Resolved over three rounds by supplying progressively later canonical datasets (Q2 → Q3 → Q4 FY2026); Test transparently flagged the shortfall at each round rather than silently substituting or fabricating. New standing procedure added: Dataset Provenance Discipline (Section "Source of Truth").
- **Dataset resolver-collision risk — resolved, Cycle 3 close-out.** Three dataset files existed as candidates after Task 2's testing sequence, all matching `rollups.py`'s resolver filter. Principal confirmed the Q4 file as sole canonical file at repo root (D12); Q2/Q3 files archived outside the resolver's search path.
- **Trivial, non-blocking:** `rollups.py` lines 710 and 745 still say "segment margin" (not "contribution margin," so outside Criterion 4's literal scope, but inconsistent with D6's broader spirit of moving away from margin framing). Comments/print text only, no functional or criterion impact. Left as opportunistic cleanup, not worth a dedicated brief.
- **Deck/HTML story staleness:** `Northwind_Quarterly_AI_Deck.pptx` and `Northwind_Quarterly_AI_Story.html` are not in current Builder files (only a PDF version of the deck exists in Project). Do not reconstruct from the PDF — risks propagating stale content. Needs current files supplied; producing both, for both scenarios, is now an explicit final output of the End-to-End Product Demonstration milestone.
- **Write-up document not started** — the actual graded deliverable; everything else is supporting evidence for it. Deliberately **deferred until after the End-to-End Product Demonstration milestone completes successfully**, so that it can be grounded in an actual demonstrated run of the complete product, both scenarios, rather than component-level evidence alone.
- **Narrative-generation fallback behavior undecided.** With no `ANTHROPIC_API_KEY` present, the dashboard falls back to a "download prompt" button rather than generating narrative text. Observed in Test, not yet formally decided as intended Live behavior (consistent with the Human Approval Gate — a human reviews before anything narrative-facing publishes) vs. an unaddressed gap. This is directly relevant to both scenarios' executive-narrative step and should be decided before or during the current milestone.

## 11. Roadmap / Future Ideas

- **End-to-End Product Demonstration (current milestone).** Two required scenarios, both ending in HTML story and PPTX:
  - *Scenario 1 — wrong/changed data:* intake → detect the problem (Phase 2/3, D11) → identify/match the issue (Phase 4, **implemented, regression-verified**) → request/handle Controller commentary (Phase 5, **implemented, regression-verified**) → validate the explanation (Phase 6, **implemented, regression-verified**) → human approval → executive narrative → HTML story → PPTX.
  - *Scenario 2 — correct data:* intake → validation passes (Phase 2/3, no issues) → human approval → executive narrative → HTML story → PPTX.
  - Phases 4–6 (both v4 and v5 scope) are built and canonical-synced; the integration step (composing them and the already-Verified components into one actual end-to-end run, for both scenarios) has not happened yet. Does not reopen or revise D10, D11, D12, or D14. Governed by D13, currently In Progress — not yet Verified. **Immediate next step:** the Section H/J independent UAT (Section 13) — the one remaining requirement before D13 reaches Verified and the two scenario runs themselves can begin.
- **Cost Structure Investigation Views (D14) — complete, Verified (v2.6).** Both the Department × Cost Category view and the company-wide Cost Category-only view are Built and Verified, ahead of Phase 4–6 as intended, giving the Controller a visible investigation path before Phase 4–6 testing begins. See Decision Log, Section 7, and Section 9.
- **Write-up document — final graded deliverable, next milestone after the demonstration above.** To be grounded in the actual completed End-to-End Product Demonstration (both scenarios), citing its evidence directly, rather than resting on component-level evidence alone.
- Phase 9 (Knowledge Archive — single log entry, framed as future architecture): parked, out of scope for the current milestone, likely post-deadline or opportunistic.
- Optional: video walkthrough, if time allows before Aug 18.

## 12. Active File Manifest

What should sit in the active Build Project vs. external archive, and why. Re-evaluate this table whenever the file set changes — see the accompanying PM recommendation for full reasoning.

| File | Category | Why |
|---|---|---|
| Project Handbook | Required every session | The index and source of truth; nothing else makes sense without it |
| Current Builder Brief | v5 — Active, implementation complete | v4's implementation (Phase 4–6 core) and v5's implementation (optional-`Commentary.xlsx` behavior, three-way Phase 7 narrative distinction) are both complete, regression-verified, and canonical-synced (`789351b`, `5709c9e`). No open Builder implementation task remains for Phase 4–6; next step is independent UAT (Section 13), not further Builder work. |
| `rollups.py` | Required every session | The single pipeline; nearly every task touches or depends on it, and its own tie-out checks are the primary verification mechanism |
| `assumptions_and_limitations.md` | Required every session | Small; must be checked/updated whenever a decision or limitation is touched, so its absence risks silent contradiction |
| `Northwind_Financial_Dashboard.py` | Required only for dashboard/UI-related tasks | Large file; needed when a task touches layout, tabs, or charts, not for pure pipeline or write-up work |
| `northwind_narrative_prompt.md` | Required only for narrative/prompt-related tasks | Needed when a task touches narrative generation or prompt wording, not for pipeline-only or dashboard-layout-only work |
| `requirements.txt` | Required every session | Pins pandas/numpy/openpyxl/streamlit to the versions confirmed clean (Cycle 2 Task 4); a fresh unpinned install reproduces the pandas 3.0 `Styler.applymap` crash |
| `close_history.py` | Required every session | Storage-neutral Close History resolve/archive logic (D10); extended (v4 Brief) to capture the complete Commentary Record in the D10 snapshot at close approval |
| `close_validation.py` | Required every session | Phase 2 (historical-revision diff) + Phase 3 (plausibility/headcount-band) logic (D11); Verified Cycle 3 Task 2 |
| `commentary_workflow.py` | Required every session (Live batch) | Phase 4 (matching), Phase 5 (draft note), Phase 6 (four-check validation) logic — v4 Brief scope. Implementation-complete, regression-verified, canonical-synced at commit `789351b`. Not yet Verified — see D13. |
| `Northwind_Sample_Dataset.xlsx` (canonical, single file — the Q4 FY2026 version) | Required every session | Raw data source `rollups.py` reads via its dataset resolution path; not generated by the pipeline, must be supplied. **Resolved (D12): this is now the only dataset file at repo root.** |
| `close_v1_v2_simulation.py` | Build/Test only — never Live | Verification tool for Phase 2/3 logic; imported by `close_orchestrator.py`. Not part of the Live batch — Live's Close History starts from real data, not simulation output. **Frozen as of Cycle 3, Task 2** — regression-only fixture, not evidence of generic correctness (see Validation Independence Principle). Not extended or modified by Build going forward. |
| `close_orchestrator.py` | Build/Test only — never Live | Same category as `close_v1_v2_simulation.py`: verifies Close History bootstrap + normal-path resolution logic. Unconditionally deletes any pre-existing `close_history/` on every run, by design, to prove the bootstrap path for real — must never run against a populated production Close History. |
| `build_test/commentary_workflow_demo.py` | Build/Test only — never Live | Builder-authored regression fixture for `commentary_workflow.py` (49 checks) — proves non-regression against a fixture Builder itself authored and knew in advance. Per the Validation Independence Principle, this is explicitly not evidence of genericity; that requires the separate, principal-supplied Section H/J UAT set, not yet run. |
| `Northwind__Sample_Dataset_-_Q2_26.xlsx`, `...Q3_26.xlsx` | Archive only — testing/provenance artifacts, outside resolver's search path | Retained for traceability of Task 2's v1→v2→v3 evidence chain (D12); must not sit at repo root alongside the canonical Q4 file or the resolver's "exactly one match" contract breaks. **Supplied to Builder for Criterion 9 and used — see Section 10 for resolution.** |
| Archived Builder Briefs | Archive only | Traceability; content already distilled into the Decision Log and Section 8. **The v1 Cost Structure Brief and the pre-correction v2 draft are now archived, superseded by the v2 (FINAL) Brief.** |

**Dataset location (decided Cycle 3, Task 2 — Option B, confirmed by direct implementation inspection):** the bootstrap dataset lives at **repo root**, alongside the application files — not in a `data/` subdirectory. This matches the implementation exactly as built: `rollups.py` and `close_v1_v2_simulation.py` both resolve it via `glob.glob("*.xlsx")` in the current working directory, filtering for filenames containing `northwind`, `sample`, and `dataset` (case-insensitive) while excluding `output` — not an exact filename match. Exactly one match must exist, or the code raises an error rather than guessing. `NORTHWIND_RAW_DATASET_PATH` is the built-in override for any deployment that needs a different location. A `data/` subdirectory was considered and explicitly rejected for now — it would require a code change that doesn't exist yet; adopting it as documented "architecture" ahead of the implementation would recreate the exact Handbook/code mismatch this project has repeatedly had to resolve. Revisit only alongside an actual code change, not as a documentation update in isolation.

**Canonical dataset file selection (D12, resolved at Cycle 3 close-out):** the Q4 FY2026 file (Jul 2023–Jun 2026) is the single canonical dataset at repo root. The Q2 FY2026 and Q3 FY2026 files used during Task 2's provenance sequence are retained only as archived testing/provenance artifacts supporting the v1→v2→v3 evidence chain in the Task 2 Return Reports — they must not be committed at repo root alongside the canonical file, since the resolver's glob-based filter would then match more than one file and raise an error rather than resolving cleanly. This closes the open item flagged at the end of Task 2's review.

**Filename resolution precedent (Cycle 3, Task 2):** `Northwind_Financial_Dashboard.py` (underscore) was confirmed canonical by direct inspection of the actual file on disk, which contradicted its own internal docstring (which incorrectly claimed a space-variant run command) and contradicted this Handbook's prior text. The actual artifact established the canonical filename; the Handbook and the stale code comment were corrected to match the directly inspected artifact. Evidence from the actual artifact established the factual implementation state; the Handbook was then updated to record that state. This Handbook remains the governance source of truth — directly inspected artifacts establish what is actually present when a discrepancy is discovered, and the Handbook is updated accordingly; this is not a general rule that code automatically outranks the Handbook. If this file is ever renamed in the future, update here deliberately, not by inference.

**Open configuration-integrity item, status unconfirmed (added this revision):** as of the Builder Project Instructions text supplied to the Architect, the "Tools & environment" section states *"Dashboard filename: `Northwind Financial_Dashboard.py` (space before "Financial", not underscore)"* — this directly conflicts with the precedent above. **The Architect has no tool access to the Builder Project's Instructions field and cannot independently verify whether this has since been corrected.** This is recorded as open, not resolved, until the principal either confirms the correction was made or supplies the updated field text for direct inspection. **Required correction, once access is confirmed:** replace the filename claim with a pointer to this Handbook's Active File Manifest (this section) rather than restating a filename fact independently — a static fact copied into a second document is exactly the drift pattern (see the narrative-prompt and promotion-pipeline incidents, Section 10) this project has repeatedly had to recover from.

| Archived Builder Return Reports | Archive only | Traceability; content already distilled into the Review & Acceptance History |
| `rollups_output.xlsx` | Regenerate while in progress; kept once archived | While a close is in progress: fully reproduced by running `rollups.py`, no architectural knowledge, don't keep. Once a close is approved: copied into that close's Close History snapshot as part of the permanent audit trail (D10) — same file, different rule depending on state. |

## 13. D13 Independent UAT — Readiness (new this revision)

Phase 4–6 implementation (v4 and v5 scope, both canonical-synced) is complete. This section records exactly what remains before D13 can move to Verified, so a fresh session doesn't have to reconstruct it from Section 9's evidence chain.

**What's already true, independently confirmed:**
- Implementation evidence: direct code review of `commentary_workflow.py` (Phase 4/5/6 core) and `build_narrative_commentary_section()` (v5's Section G handoff).
- Builder regression evidence: 49/49 (v4 fixture) + 23/23 (v5 fixture), both independently reproduced byte-identical by the Architect.
- Canonical synchronization: both `789351b` and `5709c9e` fresh-clone-confirmed; the v5 promotion additionally confirmed byte-identical to the exact diff reviewed pre-promotion.

**What's missing — the sole remaining requirement:** independent Test evidence, per the Validation Independence Principle. Builder's own regression fixtures prove non-regression against data Builder itself authored and knew in advance; they cannot prove genericity. This is the same bar D11 had to clear (Section 9) before its own Verified status.

**What the principal needs to supply (v5 Brief, Section H):**
1. **Target flagged observation** — a real D11 flag from the canonical dataset, or a controlled fixture (either is acceptable per the Brief; must be stated explicitly, not left ambiguous).
2. **Case A — Supported:** a commentary entry with a specific, checkable claim that the evidence package corroborates.
3. **Case B — Contradicted:** a commentary entry with a specific, checkable claim that the evidence package conflicts with.
4. **Case C — Insufficient:** a commentary entry with no specific claim, or a claim type the evidence package can't check.
5. **A correction-loop revision** for at least one of Case B or C — a second, complete commentary text (not a fragment) that a finance/CFO user would submit after seeing the first result, to exercise the resubmit → revalidate → accept path.
6. Per v5's addition to Section H: confirmation of whether the same target observation's UAT run will also be repeated **with no commentary file supplied at all**, to exercise Section G Case 3 (the "unexplained" path) against real content rather than only the fixture's synthetic register.

**What the Architect will do once that's supplied:** run the same independent-verification discipline applied throughout this project — predictions recorded before execution, actual output checked against them, results reviewed against the v5 Brief's acceptance criteria (Sections B, C, D, E, F, G's numbered criteria) — and report Pass/Not Pass with evidence, the same as every other review in this project. The Architect does not author or tune this content (Section H); doing so would defeat the purpose of the check.

**Once independent Test evidence exists and is reviewed:** D13 moves to Verified (pending that review's outcome), which unblocks the End-to-End Product Demonstration's Scenario 1 as genuine evidence rather than a run against Builder's own fixture.

---

*Handbook conventions: this file is edited in place, not appended to. Superseded content is removed, not struck through. The Decision Log and Review & Acceptance History are the only sections that grow purely by addition; everything else reflects only the current state.*
