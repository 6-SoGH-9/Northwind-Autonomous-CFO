# Northwind — Autonomous CFO Office — Project Handbook

**Status:** Living document. Contains only stable, approved information — not work in progress.
**Maintained by:** Architect/PM (Claude), updated after each reviewed Builder/Test Return Report.
**Handbook version:** 2.15
**Last updated:** Cycle 3 remains closed (D10/D11/D12 Verified, not reopened) and D14 remains Verified — neither is touched by this revision. v2.9's record (the Human Approval Gate) and v2.10's record (Phase 7 period-display/register-semantics) stand unchanged. v2.11 added the Repository Identity subsection (canonical-synced, confirmed v2.12 via fresh clone at `91f187b3fff971ceb8c7e600c5fbb615e01e8e9d`). v2.12 added Section 2a ("AI Role Boundaries in the Workflow"), stating the corrected Phase 4 semantic-reconciliation requirement and the Phase 7 no-invention boundary as durable product definition. v2.13 finalized the Phase 4 scope against that definition (three concrete implementation gaps confirmed by direct code inspection: no Value-based matching, a Period-narrowing defect, no one-observation-to-one-commentary exclusivity) and produced `phase4_semantic_reconciliation_brief_v1.md` as a not-yet-authorized draft, correcting Section 13 to record two independent outstanding requirements for D13. **v2.14 records a single governance event: the principal has authorized `phase4_semantic_reconciliation_brief_v1.md`.** Its Status line now reads "Active" and it supersedes v5's Section B (Phase 4 matching) as the governing Phase 4 specification; v5's Sections C/D/E/F/K remain the implemented, unmodified baseline. **Authorization is a distinct governance state from implementation and from canonical synchronization, and this revision does not conflate them:** no implementation exists yet (no Builder Return Report), and this Brief's own canonical sync — like v2.11/v2.12/v2.13's — has not yet been verified. Activation does not itself change D13's Implementation Status (still In Progress, still two independent outstanding requirements — see Section 13, updated only to reflect that the second requirement's governing Brief is now Active rather than draft). D10/D11/D12/D14 and the Human Approval Gate are untouched. **v2.15 records a scope refinement, made before any Builder implementation began: `phase4_semantic_reconciliation_brief_v1.md` is superseded in full by `phase4_semantic_reconciliation_brief_v2.md`, per explicit principal instruction to integrate a public-credential-security boundary and an API-cost-consumption boundary into the governing Brief.** v2 adds a new Section 5a (Credential Security & API Cost Boundary) and Acceptance Criteria 9–10; it does not alter Sections 1–4, the exclusivity rule (Section 2), the invocation gate (Section 3), or the original Criteria 1–8 in any way — this is an addition of previously-implicit requirements as explicit, testable governing text, not a redesign. v1's own authorization (v2.14) carries forward to v2; no separate re-authorization event occurred, since the principal's instruction for this revision was itself the authorization for the refined scope. D13's Implementation Status is unaffected — still In Progress, still two independent outstanding requirements (Section 13, updated only to reference v2 instead of v1 as the governing document for the second requirement). **This revision's own canonical GitHub synchronization has not yet been verified** — see Section 10.

**What v2.10 changed (prior revision, carried forward unchanged by v2.11):**
- **Phase 7 period-display formatting, promoted to canonical GitHub, commit `57b3aa24dcd4abd7812bcdeb68bb4bc521fb3140`** (parent `0feadb462f5389b38e26150ad84ef411c858043a`, the v2.9 Handbook promotion). Independently confirmed via fresh `git clone`: an ordinary clone of `main` resolves directly to this commit; working tree clean; exactly two files changed (`commentary_workflow.py`, `Northwind_Financial_Dashboard.py`, 116 insertions / 12 deletions); `rollups.py`, `close_validation.py`, `close_history.py` all confirmed byte-identical to the parent commit; both changed files confirmed byte-identical to the exact versions independently reviewed pre-promotion — nothing drifted between review and push.
- **Scope, as promoted:** `build_narrative_commentary_section()` and `accepted_commentary_prompt_lines()` gained an optional `fmt_period_label` parameter (identity default, fully backward-compatible), threaded to every place Phase 7's CFO/Board-facing narrative handoff renders a period into text. Applied at four render points: the observation-register table, the `open_uncommented` table, the Commentary Review expander title, and the Phase 7 handoff itself. Zero changes to `build_observation_register()`'s functional behavior, `make_observation_id()`, `match_commentary_entries()`/`_find_period_terms()`, or any validation logic — confirmed by scoped diff showing zero matches against those functions.
- **Separately, an independent investigation was commissioned** into whether `build_observation_register()`'s Phase 3 branch should instead store the raw `Fiscal Quarter` value (e.g. `"Q3 FY2026"`) rather than the `fmt_period_label()`-formatted value (e.g. `"Q3 2026"`) it currently stores — motivated by an earlier, informal, never-promoted local change that had assumed the opposite conclusion without review. Conducted from a fresh canonical clone, with an explicit no-prior-memory constraint, and independently reproduced by the Architect at every step, including the empirical leg. **Finding: current canonical behavior is already correct and consistent with the Handbook's own settled D14 display convention.** `_find_period_terms()` is a plain, format-agnostic substring matcher against whatever the register's `Period` column contains; commentary written in the D14 display convention (`"Q2 2025"`) matches correctly today, empirically confirmed via a genuine two-observation Phase 3 register (Sales & Marketing / Other Opex, Q2 FY2025 and Q4 FY2025) built through the real, unmodified `build_observation_register()` — while commentary written in the older, pre-D14 `"FY"`-inclusive convention does not match, and storing the raw value instead would reverse this: breaking currently-correct commentary to accommodate outdated wording, a regression relative to the settled convention. **The investigation was delivered as a documentation-only clarification** — 44 comment lines added at the Phase 3 branch of `build_observation_register()`, zero functional lines changed, confirmed via scoped diff (`git diff | grep "^[+-]"` minus comment/blank lines returns empty) — explaining this finding at the code site so it is not mistakenly re-attempted. If any commentary/UAT content still uses the older `"FY"`-inclusive wording, correcting that content (not the code) is the appropriate fix — a separate, later, explicitly out-of-scope action, not folded into this closure.
- **Evidence basis, all relevant categories present:** implementation evidence (Architect direct code review of `fmt_period_label()`, `build_observation_register()`, `_find_period_terms()`, `match_commentary_entries()`, read directly from canonical, both before and after promotion); Builder regression evidence (v4 49/49, v5 23/23, headcount-direction fixture 23/23, `rollups.py` 18/18 tie-outs, all independently reproduced by the Architect against the actual promoted commit, not a pre-promotion copy); independent empirical evidence for the register-semantics finding (a genuinely reconstructed two-observation register and an 8/8 targeted matching-test suite, run by the Architect from a fresh clone against a fully-specified, deterministic, internally-self-consistent dataset fixture — reproduced byte-identical to Builder's own delivered run); canonical synchronization (fresh-clone-confirmed at `57b3aa24...`, byte-identical to the reviewed pre-promotion files).
- **Supplementary confirmation:** the principal manually opened the live dashboard against the promoted commit and observed the Observation Register and Controller commentary both rendering `"Q4 2026"` (not `"Q4 FY2026"`), a sample Supported match on correctly-formatted commentary, and three intentionally-mismatched commentary samples correctly remaining unmatched, with the Human Approval Gate still present and functioning independently. This is a useful, corroborating spot-check of the promoted behavior, consistent with everything already independently verified above — **it is not, and is not treated as, the Section H/J independent UAT set required for D13** (Section 13). That requirement is unrelated in scope (the Phase 4-6 commentary-matching/validation genericity claim, not Phase 7's period display) and remains open regardless of this spot-check.
- **A genuine fixture-construction defect was found and corrected during this work, and is recorded as a new standing procedure, not smoothed over.** The dataset fixture built to demonstrate the register-semantics finding initially modified the `Expenses` sheet without recomputing the dependent `PL_Summary` sheet's derived fields, producing a real, reproducible tie-out failure (`max diff $45,000.00`) — confirmed as a genuine defect, not incidental, and rebuilt with the matching adjustment (confirmed via the same tie-out check showing `max diff $0.0000` post-correction, independently reproduced by the Architect). Separately confirmed the specific finding this fixture supported (Phase 4 matching, which reads `Expenses` and never reads `PL_Summary`) was unaffected either way. See Section "Source of Truth"'s Dataset Provenance Discipline corollary below.
- **No architectural decision changes.** D10, D11, D12, D14 are not reopened. Cycle 3 remains closed. The Human Approval Gate is not touched — confirmed the promoted diff's changed lines do not overlap the Gate's approval/rejection logic (`_gate_approved`, `close_approval_status`, its structural Phase 7 guard) at all. **D13's Implementation Status is explicitly unchanged by this revision** — see the Decision Log entry below, unaltered. Neither item constitutes new evidence toward D13; the Section H/J UAT set remains the sole outstanding requirement, exactly as recorded prior to this revision.
- No other governance change in v2.10. That revision did not restructure the Handbook, introduce new requirements beyond the one Dataset Provenance Discipline corollary, or touch any other Decision Log entry, Brief, or open item.

**What v2.11 changes (this revision):**
- Adds a **"Repository Identity (standing context)"** subsection at the top of the `## Source of Truth` section, recording the canonical repository, branch, clone URL, and known Codespace path as durable standing context — not as evidence of current repository state.
- Adds an explicit **Architect → Builder handoff rule**: Builder Briefs reference this subsection for repository identity rather than restating the URL/branch/path inline; the Architect adds only task-specific detail (target commit/parent, branch-off-`main` if any, specific files in scope) on top of it.
- Adds an explicit **known-identity-vs-verified-state distinction**, anchored to the existing Canonical Sync Verification principle already standing in this same section: recording a repository's identity is never evidence that the repository currently contains any particular state; that requires an actual fresh clone/checkout performed in the session making the claim.
- **No other change.** No Decision Log entry is added or altered, no code file changes, D13 is not touched, Cycle 3 is not reopened, and no other section of this Handbook is modified.

---

## Current Release

| Field | Value |
|---|---|
| Product target version | v1.0 (Information Architecture frozen per Section 3; updated v2.6 for D14, v2.7 for Phase 4–6 — see notes below) |
| Environment | **Live** (Phase 4–6 implementation and the Human Approval Gate both promoted to canonical `main`; the Phase 7 period-display clarification and register-semantics investigation are now also promoted; not yet exercised in the End-to-End Product Demonstration's two scenarios) |
| Current cycle | **Cycle 3 — complete and closed out.** Not reopened. Phase 4–6, the Human Approval Gate, and the Phase 7 period-display clarification are tracked as part of the End-to-End Product Demonstration milestone, not as a new numbered cycle. |
| Current task / milestone | **End-to-End Product Demonstration — in progress.** Phase 4–6 implementation is complete (v4 scope `789351b`, v5 scope `5709c9e`, headcount-direction fix `4a670d0`) — regression-verified, canonical-synced. The Human Approval Gate is complete and independently Test-verified in full (commit `cdfeb5701...`). **The Phase 7 period-display clarification and register-semantics investigation are complete, independently verified, and canonical-synced (commit `57b3aa24...`).** D13 is **In Progress, not yet Verified** — **two distinct outstanding requirements remain (Section 13 below): (1) the Section H/J independent UAT of Phase 4-6 as currently implemented, and (2) a newly-identified Phase 4 implementation gap against the corrected semantic-reconciliation product definition (Section 2a), for which a finalized Builder scope now exists but has not yet been implemented or independently tested.** |
| Current Builder Brief | **`phase4_semantic_reconciliation_brief_v2.md` — Active, principal-authorized, not yet implemented.** Supersedes v1 in full (v1 retained for provenance only) and supersedes v5's Section B (Phase 4 matching) only; v5's Sections C/D/E/F/K remain the implemented, unmodified baseline for everything this Brief doesn't change. Adds an explicit credential-security and API-cost-consumption boundary (Section 5a, Criteria 9–10) on top of v1's scope, unchanged otherwise. `human_approval_gate_brief_v1.md`, v4/v5's own already-implemented scope, and the Phase 7 period-display addendum remain complete and are not reopened by this revision. No Builder Return Report exists yet against this Brief. |
| Current status | Cycle 2 (Tasks 1–4), Cycle 3 Task 1, Cycle 3 Task 2, and **D14 are all Verified, promoted to Live.** **Phase 4–6 (D13 scope) is implemented, regression-verified, canonical-synced, and In Progress — not yet Verified. The Human Approval Gate is implemented, regression-verified, canonical-synced, and fully independently Test-verified (all eight acceptance criteria). The Phase 7 period-display clarification and register-semantics investigation are complete, independently verified, and canonical-synced.** The End-to-End Product Demonstration's two scenarios have **not** been run end-to-end and are not claimed as verified. |
| Last approved milestone | **Phase 7 period-display clarification and observation-register period-semantics investigation** — independently verified end to end by the Architect, including empirical, executed evidence reproduced from a fresh clone; canonical commit `57b3aa24dcd4abd7812bcdeb68bb4bc521fb3140`, fresh-clone-confirmed. |
| Next milestone | **Two outstanding requirements before D13 reaches Verified (Section 13):** (1) the Section H/J independent UAT of Phase 4-6 as implemented, ready to proceed, blocked on principal-supplied commentary content; (2) the finalized Phase 4 semantic-reconciliation Builder scope (Section 2a; Architect Documentation & Alignment Report, Phase 4 Semantic Reconciliation Scope Finalization task) — implementation not yet authorized/started, and its own independent UAT (Cases B/C/D in that task's UAT specification) not yet run. Once both are reviewed, D13 moves toward Verified; then the End-to-End Product Demonstration's two full scenario runs; then the write-up document — the final graded deliverable. |

*A new Builder session should be able to read this table alone and know exactly where the project stands before opening anything else.*

## Current Milestone: End-to-End Product Demonstration

**Objective:** demonstrate the entire product running end-to-end — not just the already-Verified components — across two required scenarios.

**Scenario 1 — Wrong / changed data:**
Data intake → detect the problem (Phase 2/3 validation, D11) → identify/match the issue (Phase 4 — Commentary Matching) → request or handle Controller commentary (Phase 5 — Finance Collaboration) → validate the explanation (Phase 6 — Explanation Validation) → human approval → executive narrative (Phase 7) → HTML story → PPTX (Phase 8)

**Scenario 2 — Correct data:**
Data intake → validation passes (Phase 2/3, no issues raised) → human approval → executive narrative (Phase 7) → HTML story → PPTX (Phase 8)

**Purpose:** prove the product behaves correctly both when something is wrong (full detect → explain → validate → approve chain) and when everything is correct (clean validation straight through to executive output) — using one coherent workflow, not two different code paths asserted to be equivalent.

**Phases 4–6 — implementation status (v4 Brief scope, complete):**
- Phase 4 — Commentary Matching: implemented, regression-verified, canonical-synced.
- Phase 5 — Finance Collaboration (drafted note for the finance/CFO user only, never sent — corrected architecture, Section 6): implemented, regression-verified, canonical-synced.
- Phase 6 — Explanation Validation: implemented, regression-verified, canonical-synced.

All three are built, integrated (`Northwind_Financial_Dashboard.py`, `rollups.py`, `close_history.py`), and promoted to canonical GitHub (commit `789351b`, plus the headcount-direction fix at `4a670d0`). This is genuinely different in kind from D10/D11/D12's prior integration-of-already-Verified-components work — it was new implementation, and is recorded as such. It is **not yet "Verified"**: D13's Implementation Status is In Progress, pending Section H/J independent Test evidence (Section 5). The governing Builder Brief has advanced to **v5**, which layers two further requirements on top of this completed v4 scope — see below.

**The Human Approval Gate — implementation status (complete):** implemented, regression-verified, canonical-synced (commit `cdfeb5701...`), and independently Test-verified across all eight acceptance criteria of `human_approval_gate_brief_v1.md`, including Case 5 (revision-after-approval reset), which required a separately-constructed real-data fixture and genuine real-UI execution rather than source inspection or session-state injection. This closes the interactive-control gap previously tracked in Known Technical Debt (Section 10). **This is one precondition of D13, not D13 itself** — D13's own Verified status still requires the Section H/J Phase 4-6 UAT set, unaffected by this completion.

**The Phase 7 period-display clarification and register-semantics investigation — implementation status (complete):** implemented, regression-verified, canonical-synced (commit `57b3aa24...`), and independently verified by the Architect including empirical, executed matching-test evidence. Closes two related items previously tracked in Known Technical Debt: (1) Phase 7's narrative/prompt text was not applying the D14 display-format convention the way every other rendered surface already does — fixed via formatter threading, zero effect on matching or validation logic; (2) a separate question, raised during that fix's review, of whether the observation register should store the raw `Fiscal Quarter` value instead of the display-formatted value — investigated independently and resolved in favor of current canonical behavior (already correct), documented at the code site rather than changed. **Neither item is a precondition of, nor moves, D13** — both are display/documentation-layer, with zero functional change to matching or validation logic.

**Explicit final outputs of this milestone:** the HTML story and the PPTX executive deck, produced for both scenarios.

**Boundaries:**
- Cycle 3 is not reopened. D10, D11, D12, and D14 remain Verified as recorded in the Decision Log; this milestone does not re-litigate them.
- No settled architectural decision changes as a result of this milestone.
- The end-to-end product is **not** verified by Phase 4–6 implementation, the Human Approval Gate's completion, or the Phase 7 period-display clarification alone, and full end-to-end completion is not claimed anywhere in this Handbook. It remains unproven until D13 reaches Verified on Section H/J independent UAT evidence and both scenarios are actually run and reviewed against evidence, per the same standard applied everywhere else in this project (Architect's permanent instructions, Section 4).
- The write-up document is deferred until after this milestone completes successfully, so it can be grounded in an actual demonstrated run of the complete product rather than component-level evidence alone.
- **D13 (Machine Recommends, Human Decides)** governed the v4/v5 Brief's scope and the Human Approval Gate Brief's scope alike. Confirmed by direct code inspection: no path in either implementation sets or implies close approval/rejection status; every Phase 6 output is Supported/Contradicted/Insufficient only, never a decision, and the Gate's approve/reject action is a distinct, explicit human action never inferred from a Phase 6 outcome (independently confirmed via Case 2/4 of the Gate's own UAT — the all-Supported-without-approval case).

**Distinction preserved by this milestone (do not collapse these):**
1. **Components already independently Verified and requiring only integration** — D10 (Close History), D11 (Close Validation Engine, Phase 2/3), D12 (canonical dataset), D14 (Cost Structure views), rollups/dashboard, narrative generation, HTML/PPTX generation logic (where it already exists). Unchanged, not re-tested by this milestone.
2. **Phase 4–6 (v4/v5 Brief scope, including the headcount-direction fix) — implemented, regression-verified, canonical-synced, not yet Verified.** The remaining gap to Verified is independent Test evidence of the Section H/J UAT set (D13), not missing implementation.
3. **The Human Approval Gate — implemented, regression-verified, canonical-synced, and fully independently Test-verified.** Complete as its own Brief. Does not by itself move D13 to Verified.
4. **The Phase 7 period-display clarification and register-semantics investigation — implemented, regression-verified, canonical-synced, and independently verified including empirical evidence.** Complete, closed as Known Technical Debt. Display/documentation-layer only; does not by itself move D13 to Verified.
5. **Integration/workflow behavior — not yet demonstrated.** Whether the Verified components, the implemented-but-not-yet-Verified Phase 4–6, and the now-complete Human Approval Gate and Phase 7 display work compose into one coherent, two-scenario run is the open question this milestone still has to answer.
6. **The write-up** — the final graded deliverable, sequenced after this milestone, out of scope for this milestone's evidence standard (per Architect's instructions, Section 15).

## Source of Truth

**Repository Identity (standing context):**

- **Repository:** `6-SoGH-9/Northwind-Autonomous-CFO`
- **Canonical branch:** `main`
- **Clone URL:** `https://github.com/6-SoGH-9/Northwind-Autonomous-CFO.git`
- **Known Codespace path:** `/workspaces/Northwind-Autonomous-CFO`

This is known, standing repository identity, recorded here so Architect and Builder sessions do not need to ask the principal for it again. It is context, not evidence: recording this identity does not confirm that the repository currently exists in any particular state, contains any particular commit, or matches any claim made elsewhere in this Handbook. Any claim about current HEAD, commit history, file contents, diffs, or working-tree cleanliness requires an actual fresh clone or checkout performed in the session making the claim, per the Canonical Sync Verification procedure below — a recorded URL, branch name, or cited commit hash is never itself evidence that the repository currently contains that state.

**Architect → Builder handoff rule:** Builder Briefs reference this subsection for repository identity rather than restating the URL, branch, or Codespace path inline. The Architect adds only what is task-specific on top of it — the target commit and its parent, any branch-off-`main`, and the specific files that task's scope touches. This follows the same anti-duplication discipline already applied to dataset filenames and the Active File Manifest (Section 10/12): a fact lives in exactly one place, and other documents point to it rather than restate it.

When any two documents disagree, precedence resolves the conflict in this order:

1. **Project Handbook** (this document)
2. **Current Builder Brief**
3. **Current project code and documentation** (`rollups.py`, dashboard, `assumptions_and_limitations.md`, `northwind_narrative_prompt.md`)
4. **Archived Builder Briefs**
5. **Archived Builder Return Reports**

Archived documents (4, 5) exist for traceability only and must never override the current Handbook. If a discrepancy is found — e.g., code that doesn't match what the Handbook records, or a brief that references a superseded decision — the correct response is to flag it and update the Handbook (or correct the code, if the Handbook is right and the code drifted), never to silently defer to the lower-precedence source. This is the same discipline that caught the narrative-prompt drift in Cycle 2, Task 1 — applied now as a standing rule instead of something rediscovered each time.

**Configuration Integrity Check (standing procedure, added after the Cycle 2 Task 3 drift discovery):** A task is not accepted as Pass on code correctness alone. Every review must also confirm the active project's files are actually synchronized with what's being claimed as current, specifically:
- A case-insensitive (not case-sensitive) terminology resweep across every file the task touches or references, every time — not just after a rename task, and not assumed clean because a prior sweep passed.
- Where a `.md` template mirrors a code-embedded string (e.g., `northwind_narrative_prompt.md` vs. `rollups.py`'s `SYSTEM_PROMPT`), the review confirms **content correspondence** between the two directly — a clean grep on each file separately is not sufficient, since both files can independently pass a banned-terms sweep while still saying different things.
- When a discrepancy is found between an "accepted" state and the file currently in the project, the default recovery path is to **regenerate the drifted file from the verified-correct source already in the project** (usually the code), not to source a fix from an external archive of unconfirmed provenance — an unverified archived copy carries the same risk it's meant to resolve.
- This check does not depend on anyone's memory of which file was uploaded when — it's evidence-based, run fresh, every review.
- **Canonical Sync Verification (added after Cycle 2, Task 4):** A Return Report may only record a fix as "Pass" once the fix is confirmed present in a freshly-pulled copy of the canonical file set that feeds promotion — not merely verified in the session's working copy. A fix that only exists in-session is not yet a fix, regardless of how it was tested. This closes the specific failure mode found in Cycle 2 Task 4: a terminology correction was reviewed and accepted as Pass in Task 3/Item 3, but never propagated to the canonical store, so Test re-encountered the original defect.

**Self-healing boundary (added after Cycle 2, Task 3, Item 3):** if a review's own verification step (e.g., "resweep must show zero matches across all four files") reveals that a file has regressed from a *previously-approved, specific* fix, the Builder may reproduce that exact previously-approved content without waiting for a new brief — provided the fix is reported prominently, not silently absorbed into a "clean" claim. This is narrow: it covers restoring already-agreed content only. Any new label, threshold, structural change, or judgment call is not covered by this exception and still stops and flags rather than proceeds.

**Validation Independence Principle (added Cycle 3, Task 2):** The Builder may not be the sole author of both a piece of validation/detection logic and the test data used to prove that logic works. Doing so creates an unavoidable conflict of interest — logic can trivially "pass" a test whose specific values its own author already knew in advance, without that being evidence the logic is actually generic. This applies wherever Build produces a rule, threshold, or check meant to operate on arbitrary future data (e.g. Phase 2/3 plausibility and deterministic-diff logic, the headcount-direction fix, and the Human Approval Gate's structural blocking behavior). Consequences for roles:
- Builder's responsibility is limited to the generic logic itself and documenting the business rules it implements (thresholds, rationale).
- Builder does not author, extend, or tune test data, injected anomalies, or fixtures meant to prove that logic's correctness going forward.
- Data used to validate genericity must come from outside Builder's authorship — either genuine approved closes from Close History, or a dataset independently constructed by Test/Architect.
- Pre-existing Builder-authored fixtures (e.g. `close_v1_v2_simulation.py`) may still serve a **narrower** purpose — regression continuity, confirming a refactor didn't change behavior versus an already-established baseline — but must not be cited as proof of genericity. The two claims ("didn't regress" vs. "works generically") must be kept explicitly distinct in Return Reports and Handbook records.

This mirrors, at the role level, the same discipline Canonical Sync Verification already applies at the file level: evidence must come from somewhere the claim-maker doesn't fully control. The Human Approval Gate's Case 5 fixture (Section 9) follows this same discipline: constructed independently by Test and separately reproduced by the Architect, from a fully-specified deterministic procedure grounded in the code's own real flagging thresholds, not an arbitrary or hand-tuned value.

**Dataset Provenance Discipline (added Cycle 3, Task 2):** A dataset supplied to Test for a real-data validation methodology must be checked for actual coverage range before results are treated as satisfying the methodology's stated scenarios — the file's own contents govern, not its filename or an assumption about what it should contain. Consequences, standing:
- If a supplied dataset's coverage falls short of what a Test Instructions document specifies, Test flags this explicitly and either substitutes the closest genuine equivalent (clearly labeled as a substitution, never silently) or declines to proceed — fabricating synthetic "real" data to fill the gap is never acceptable under a real-data methodology.
- Silent substitution is prohibited. Transparent substitution, disclosed in the Return Report with an explicit mapping of what was asked for versus what was used, is acceptable interim protocol and does not itself constitute a failed test.
- Evidence produced against a substituted or short dataset is **provisional**, not Verified, until re-run against the canonical dataset the instructions actually specified.
- The Architect verifies dataset coverage directly (opening the actual file, checking min/max dates across every sheet the methodology touches) before accepting a Test Return Report's coverage claims as fact — this is the same "artifact overrides narration" discipline applied at the file-content level, not just the filename level.
- **Resolver hygiene corollary (added at Cycle 3 close-out):** once a canonical dataset is confirmed, any prior-round dataset files used only to build up to that confirmation (e.g. intermediate quarter-truncated versions used across a multi-round provenance sequence) must be kept outside the production resolver's search path — archived for evidence/traceability of the provenance chain, never left alongside the canonical file where a wildcard resolver could match more than one. This is a repository-hygiene rule, not a new architectural decision — it follows directly from `rollups.py`'s existing "exactly one match" resolution contract (Section 12).
- This round's sequence (Q2 dataset → flagged gap, substituted → Q3 dataset → flagged remaining gap → Q4 dataset → exact match, no substitution) is the discipline working as designed: Test independently rediscovering and flagging a provenance gap is a protocol success, not a defect to be smoothed over.
- **Fixture-construction corollary (added Human Approval Gate Case 5).** where no existing dataset in the repository can exercise a required code path (here: the canonical dataset produces zero Phase 3 flags for its current final period), a new fixture may be constructed, but only via a fully-specified, deterministic procedure grounded in the actual code's real logic and thresholds — never an arbitrary or freely-chosen anomaly, and never a session-state injection standing in for the fixture. The procedure and its justification are recorded in the governing Brief/Test Instructions, not invented ad hoc by whoever executes the test.
- **Fixture internal-consistency corollary (added Handbook revision v2.10).** During the Phase 7 period-display/register-semantics investigation's evidence-reproduction, a constructed test fixture (a modified copy of the canonical dataset, injecting a two-quarter Sales & Marketing / Other Opex plausibility anomaly for demonstration purposes) was found, on independent reviewer request, to have modified the `Expenses` sheet without recomputing the dependent `PL_Summary` sheet's `Total Opex ($)` / `Gross Profit ($)` / `Gross Margin (%)` fields, which the live workbook derives from `Expenses` via SUMIFS but which do not auto-recompute when a static copy is edited outside Excel. This produced a real, reproducible tie-out failure (`rollups.py`'s own "Expenses tie to PL_Summary Total Opex" check: `max diff $45,000.00`, exactly matching the un-propagated injection amount) — confirmed as a genuine fixture-construction defect, not an incidental issue, and not something to route around; the fixture was rebuilt with the matching `PL_Summary` adjustment (both the six affected monthly rows and the `"Total / Avg"` footer row), confirmed via the same tie-out check showing `max diff $0.0000` post-correction. Separately confirmed that the specific demonstration this fixture supported (Phase 4 matching, which reads `Expenses` directly and never reads `PL_Summary`) was unaffected either way — both facts recorded, neither used to excuse the other. **Standing checklist requirement, added by this corollary:** any constructed/injected test fixture that modifies a raw tab with a documented downstream/derived sheet (per each sheet's own stated derivation, e.g. `PL_Summary`'s SUMIFS-from-raw-tabs relationship, documented in the dataset's own `README` sheet) must (1) identify every derived sheet/field affected by the injection, (2) recompute those fields by the same rule the live workbook would apply, and (3) verify the recomputation via the project's own existing tie-out check (`rollups.py`'s import-time verification) before the fixture is delivered as evidence — not after, and not left for the reviewer to discover. This applies in addition to, not in place of, the existing Fixture-construction corollary (Human Approval Gate Case 5).

---

## 1. Project Vision

An AI-powered quarterly close workflow for a submission to the AI in Finance Challenge (deadline August 18). The differentiator is **not** any single KPI or dashboard page — it is the automation of the analyst-judgment step of financial reporting: validating numbers, flagging anomalies without inventing causes, matching real explanations to flagged items, and only then generating executive-ready narrative and deliverables, with a human approval gate before anything publishes.

The graded deliverable is a **single written document** (problem, workflow, impact). The dashboard, workbook, deck, and HTML story are supporting evidence for that document, not the product being judged in themselves.

**The canonical object this system manages is the Approved Financial Close** (see D10). The dashboard, executive narrative, and board deliverables are outputs generated from an approved close — evidence of what the workflow produced — not independent products in their own right. This distinction is architectural, not cosmetic: it's why the workflow diffs against a prior approved close (Phase 2), uses historical context for plausibility (Phase 3), and retains rather than overwrites each close (Workflow State Model, ending in Archived).

## 2. Product Principles

1. **Every number traces to a real calculation.** Nothing invented, nothing asserted without a corresponding figure in the data.
2. **Don't manufacture a metric the dataset can't support.** If a calculation requires more caveats to use safely than it delivers in insight, the calculation is wrong for the product, not just poorly labeled. (See Decision Log, Cycle 2.)
3. **Name metrics after the business question they answer, not the mechanics of how they're computed.** A CFO should understand a page title without opening it.
4. **Structural prevention over disclaimed caveats.** If a UI layout invites a wrong reading (e.g., a side-by-side % comparison), fix the layout — don't add a warning box next to the wrong reading.
5. **Every dashboard page answers exactly one business question**, has a defined primary audience, and does not overlap another page's job.
6. **Settled architectural decisions stay settled** unless a genuine defect is found — not a preference, not a stylistic reopening.
7. **Evidence over claims.** "Done" and "passed" require the actual output shown, not a status word.
8. **The Handbook is an index and a record of decisions, not a container for evidence.** It records what was decided and why, and points to where the proof lives (a Return Report, a tie-out log) — it does not reproduce that proof. This keeps the Handbook small and durable while keeping every claim in it checkable.
9. **A review checks configuration state, not just code correctness.** A task isn't verified merely because the logic is right — the active project's files must also be checked for drift against what was previously accepted as done. "Passed before, assumed synced since" is not evidence; a review that skips this can approve a project that quietly regressed to an earlier state.

## 2a. AI Role Boundaries in the Workflow (durable product definition — added Handbook v2.12)

**Status of this section:** this is a documentation-alignment clarification of already-approved product intent, not a new architectural decision. It does not reopen D10, D11, D12, D14, or the Human Approval Gate, and does not change any Decision Log entry's Implementation Status, including D13's. It exists because the documented Phase 4 capability and the final executive-output flow had drifted from the intended product definition (see Section 9's Architect Documentation & Alignment Report entry for the full evidence chain, including direct inspection of the canonical implementation against this definition). Where this section and prior Handbook wording (e.g. Phase 4's "matching logic" description in Section 4) could be read as describing a purely deterministic capability, this section is the corrected, authoritative statement; Section 4's per-phase rows are annotated to point here rather than duplicating this text.

**The confirmed eight-step product workflow:**

1. **Deterministic financial analysis.** The financial system remains deterministic. It produces financial calculations, variances, observations, flags, and financial evidence (D11/D14 outputs). The LLM does not calculate financial truth and does not become the source of truth for any figure.
2. **Controller commentary.** The Controller supplies the operational explanation as complete, free text. The Controller does not operate the dashboard (unchanged, existing operating model).
3. **LLM-assisted commentary reconciliation (Phase 4).** Answers exactly one question: *"Which financial observation does this commentary refer to, if any?"* This is a semantic-reconciliation requirement, not a keyword/synonym-matching requirement — see "Phase 4 requirement, stated precisely" below.
4. **Bounded evidence validation (Phase 6).** Answers a different question: *"Is the explanation supported by the bounded financial evidence?"* Determines Supported / Contradicted / Insufficient using the existing bounded evidence package (Section 4, Phase 6). Unchanged by this section.
5. **Finance/CFO review.** The finance/CFO user reviews the reconciliation and the Phase 6 result, may edit and resubmit the complete commentary, and explicitly marks one version Accepted. A Supported result is not automatic acceptance. Unchanged by this section.
6. **Human Approval Gate.** The final human decision authority for whether the close proceeds. The AI does not approve the commentary, does not approve the close, and does not infer approval. Unchanged by this section.
7. **LLM executive communication (Phase 7).** Turns the established upstream state — deterministic financial facts, accepted commentary/explanation, and the applicable unresolved/unexplained/insufficient/contradicted state — into CFO/Board/investor narrative. See "Phase 7 no-invention boundary" below.
8. **HTML + PPTX.** Final executive deliverables, derived from the governed executive-output state, traceable back through accepted commentary → validation → reconciliation → the underlying financial facts. Not an independent source of facts.

**Deterministic truth, AI reconciliation, AI communication, AI prohibition — the distinction that must not collapse:**

- **Deterministic truth:** financial calculations, observations, flags, and bounded evidence (Phases 0–2/3, D11, D14) remain deterministic and are the sole source of financial fact. Nothing downstream recomputes or overrides them.
- **AI reconciliation (Phase 4):** an LLM may be used to interpret the meaning of the complete Controller commentary to identify which observation it refers to, including well-supported indirect/inferable references. It must be able to return no confident match. It must not invent an observation, must not decide Supported/Contradicted/Insufficient (that remains Phase 6's separate job), and must not change the observation register.
- **AI communication (Phase 7):** an LLM may turn already-established, accepted financial facts and accepted explanations into executive communication. It communicates controlled upstream state; it does not create new financial or explanatory truth.
- **AI prohibition (both AI steps):** neither step may invent financial facts, financial figures, business events, operational causes, evidence, or conclusions not already established by the deterministic system or the accepted upstream state.

**Phase 4 requirement, stated precisely:** commentary matching must identify which financial observation a piece of Controller commentary refers to, based on the meaning of the complete commentary, including sufficiently supported indirect/inferable references — not only cases where the commentary literally repeats the observation's department, category, period, or value. "Inferable" means interpreting the meaning and available references in the complete commentary text; it specifically does **not** mean synonym expansion, keyword substitution, department/category/value repetition, a larger deterministic dictionary, or additional hard-coded matching patterns. A commentary can refer to an observation without literally naming its department or category. Phase 4 must still be able to return an explicit no-confident-match result rather than forcing a weak match, and must not invent an observation. This clarifies, and does not change, the existing Brief-level requirement that Phase 4 handle "explicit or inferable references" (Brief Section B) — the correction is to the *meaning* of "inferable," not to whether inferable references were already in scope.

**Phase 4 / Phase 6 separation (unchanged, restated for clarity):** matching (Phase 4) answers "which observation is this about?"; validation (Phase 6) answers "is the explanation supported by bounded evidence?" These remain two distinct jobs. Phase 4 must never decide Supported/Contradicted/Insufficient, and Phase 6 must never re-identify which observation is under discussion.

**Accepted-commentary control (unchanged, restated for clarity):** Phase 7 receives only the version of commentary explicitly marked Accepted by the finance/CFO user (Section F's version model). No unaccepted, superseded, or AI-generated substitute commentary may bypass this control, regardless of how Phase 4's reconciliation is implemented.

**Phase 7 no-invention boundary:** the executive narrative communicates deterministic financial facts, established observations, accepted Controller commentary/explanation, and the applicable validated/unresolved/insufficient/contradicted state — and must not invent numbers, financial facts, causes, explanations, or certainty not established upstream. In particular, Phase 7 must not transform an UNEXPLAINED or UNRESOLVED observation, or an Insufficient/Contradicted commentary result, into an apparently certain executive explanation merely to make the narrative read as complete. This boundary applies regardless of how Phase 4's reconciliation is implemented, and is required to be reflected in whichever document/code actually governs the executive narrative prompt (Section 4, Phase 7 row; `northwind_narrative_prompt.md`; `rollups.py`'s `SYSTEM_PROMPT`/`build_user_prompt`).

**What this section does not do:** it does not assert that the canonical implementation currently satisfies the Phase 4 or Phase 7 requirements stated above — that is an implementation-evidence claim, addressed separately (Section 9's Architect Documentation & Alignment Report entry, and Section 13). It does not reopen D10, D11, D12, D14, or the Human Approval Gate. It does not move D13's Implementation Status. It does not authorize any Builder work by itself — Builder work requires its own Brief, per Section 6 of the Architect's permanent instructions.

## 3. Information Architecture (v1.0, frozen — page set and business questions unchanged; D14 added evidenced KPIs to an existing page, see Cost Structure row)

| # | Page | Business Question | Audience | Primary KPIs | Flagship / Supporting | Status |
|---|---|---|---|---|---|---|
| 1 | Executive Summary (KPI strip) | How did we do this period, at a glance? | CFO, Board | Revenue, Opex, Op. Profit, Op. Margin vs prior | Flagship | Built |
| 2 | Revenue Performance | Where is revenue growing/shrinking, broad-based or concentrated? | CFO, FP&A | Revenue by region/product, QoQ/YoY %, breadth/concentration, revenue mix % | Flagship | Built (Cycle 2 Task 2 added mix %) |
| 3 | Cost Structure | Which departments drive opex change, and why? | CFO, FP&A | Opex by dept, QoQ/YoY %, S&B volume/rate bridge, breadth/concentration, Opex per Employee by dept; **Department × Cost Category breakdown; company-wide Cost Category-only breakdown** | Flagship | **Built and Verified — D14 (v2.6).** Department × Cost Category and company-wide Cost Category-only views added, Current/Prior/Variance $/%. First IA change since the v1.0 freeze — applied under the Architect's standing authority to record already-approved, evidenced criteria, not a new architectural decision. |
| 4 | Regional Revenue & Go-to-Market Investment | Is GTM spend proportionate to where revenue comes from, and is that shifting? | FP&A (primary), CFO (secondary) | Region Revenue ($), Allocated GTM cost ($), Net of GTM Cost ($); single-region % trend only | Supporting (demoted from flagship in Cycle 2) | Redesigned, built |
| 5 | Product Line Revenue & R&D Investment | Is R&D investment proportionate to revenue by product, and is that shifting? | FP&A (primary), CFO (secondary) | Product Revenue ($), Allocated R&D cost ($), Net of R&D Cost ($); single-product % trend only | Supporting (demoted from flagship in Cycle 2) | Redesigned, built |
| 6 | Headcount & Efficiency | Is headcount growth tracking ahead of, in line with, or behind revenue growth? | CFO, Board | Company-wide Revenue per Employee (trend), headcount growth % vs revenue growth %, headcount by dept (trend) | Flagship | Corrected (Cycle 2 Task 2 — removed dept-level Revenue/Employee) |
| 7 | Budget vs Actual / Variance Review | Which variances against plan need explanation, which are recurring? | CFO, Controller | Largest fav/unfav variances, Major Miss/Watch flags, consecutive-Watch streaks | Flagship | Built |
| 8 | Close Validation Status | Has this close been reconciled, are anomalies investigated, is it safe to publish? | Controller, FP&A | Close state (Workflow State Model), Explained/Partial/Unexplained counts, diff + plausibility flags | Flagship (differentiator) | **Phase 2/3 logic: Built and Verified — Cycle 3, Task 2. Phase 4–6 outcomes (commentary match, four-check validation trace, version history, accept action): Built, regression-verified, canonical-synced — not yet Verified, pending D13's independent UAT. The interactive close-approval control (Human Approval Gate) is built, regression-verified, canonical-synced, and independently Test-verified in full (commit `cdfeb5701...`). Period display throughout this page (and Phase 7's narrative handoff) follows the settled D14 convention, independently confirmed correct and canonical-synced (commit `57b3aa24...`)** — see Section 10. |
| 9 | AI Executive Narrative | What's the board-ready explanation of this period? | CFO | Prose, 6 sections (Section 4 = "Investment Alignment," $-only) | Flagship | Built, regenerated Q4 FY2026 vs Q3 FY2026 |
| 10 | Executive Deliverables (HTML story, PPTX deck) | What do I hand to the Board? | Board | No new calc — presentation only | Flagship | **Pending — explicit final output of the End-to-End Product Demonstration milestone, for both scenarios; current files not yet supplied** |

**Explicitly rejected metric:** department-level Revenue-per-Employee (attributes company-wide revenue to a single department's headcount — same structural fallacy as the original Contribution Margin design; see Decision Log).

## 4. Workflow Specification

Reused unchanged from `autonomous_cfo_office_master_brief.md`. Ten phases (0-9), modular, each consuming only the prior phase's defined output:

| Phase | Name | Status |
|---|---|---|
| 0 | Close Trigger | Simulated only (folder/naming convention) |
| 1 | Data Intake & Version Control | Simulated (Close v1/v2), in progress |
| 2 | Deterministic Validation | **Verified — Cycle 3, Task 2.** `close_validation.py` Phase 2 (historical-revision diff detection), independently tested against real data through Q4 FY2026. Its role in the End-to-End Product Demonstration (both scenarios) is not yet exercised. |
| 3 | Plausibility Review (AI) | **Verified — Cycle 3, Task 2.** `close_validation.py` Phase 3 (QoQ threshold + headcount-band plausibility), independently tested against real data through Q4 FY2026. Its role in the End-to-End Product Demonstration (both scenarios) is not yet exercised. |
| 4 | Commentary Matching | **Implemented, regression-verified, canonical-synced (`commentary_workflow.py`, v4 Brief scope) — not yet Verified, pending D13's independent UAT.** Matches imported `Commentary.xlsx` entries against the observation register on identifiable references; returns explicit no-match rather than a forced guess. Required for the End-to-End Product Demonstration's Scenario 1. Confirmed unaffected by the Phase 7 period-display work (Section 10). **The governing product requirement for this phase — semantic/LLM-assisted reconciliation of the complete commentary's meaning, not keyword/synonym matching — is stated durably in Section 2a; the current implementation's status against that requirement is an implementation-evidence question, not a documentation question (see Section 9's Architect Documentation & Alignment Report entry and Section 13).** |
| 5 | Finance Collaboration (draft note) | **Implemented, regression-verified, canonical-synced — not yet Verified.** Reinterpreted per the corrected architecture: drafts a plain-language note for the finance/CFO user's own reference when Phase 4/6 can't proceed; never addressed to, formatted for, or sendable to the Controller; no send path exists in the implementation (confirmed by structural code check). |
| 6 | Explanation Validation | **Implemented, regression-verified, canonical-synced — not yet Verified.** `commentary_workflow.py`'s four-check validator (specific claim present; checkable against the evidence package; supported/contradicted; sufficiently specific to be useful), covering two claim types (`headcount`, `category_reallocation` — the latter genuinely wired to D14's Department × Cost Category breakdown). Outputs Supported (worded as consistency, never proof)/Contradicted/Insufficient, never a close-status decision. The `headcount` claim type's direction-parsing gap (Case A/Case B indistinguishability) is fixed and canonical-synced (commit `4a670d0`). Subject to D13. |
| — | **Human Approval Gate** | Between Phase 6 and 7 — no executive output publishes without sign-off on the validated observation register. Required in both scenarios of the current milestone. Per D13, this is a genuine blocking state; no phase or automated agent may advance a close past it. **Implemented, regression-verified, canonical-synced (commit `cdfeb5701...`), and independently Test-verified across all eight acceptance criteria of `human_approval_gate_brief_v1.md`** — see Section 10. This precondition of D13 is complete; D13 itself still requires the Section H/J Phase 4-6 UAT set. |
| 7 | Executive Insight Engine | Reused (`northwind_narrative_prompt.md`) — existing capability; its use as the final leg of both End-to-End Product Demonstration scenarios is not yet exercised. **Its narrative/prompt period-display text now correctly applies the D14 display convention, independently verified and canonical-synced (commit `57b3aa24...`)** — see Section 10. **The governing no-invention boundary for this phase (communicate accepted/established upstream state; never invent facts, causes, or explanations; never smooth an unresolved/unexplained/insufficient/contradicted state into apparent certainty) is stated durably in Section 2a.** |
| 8 | Executive Deliverables | Reused (HTML story, deck) — **explicit final output of the End-to-End Product Demonstration milestone, for both scenarios**; current source files not yet supplied |
| 9 | Knowledge Archive | Not started — single log entry only, explicitly framed as future architecture, not a learning system. Not part of the current milestone's two scenarios (neither scenario's flow above includes Phase 9). |

**Workflow State Model** (per close version): Close Received → Data Validated → Observations Generated → Awaiting Controller Input → Explanations Validated → Executive Ready → Published → Archived. **"Explanations Validated" and "Executive Ready" are computed from real state in the dashboard's status strip** (previously hardcoded placeholders — see Section 10), reflecting the Human Approval Gate's completion.

**Simulated close versions (Build/Test fixture only, regression use):** Close v1 = dataset truncated through Q3 FY2026. Close v2 = full dataset through Q4 FY2026 + two injected, exactly-documented changes (a historical revision to a closed period, a Q4 plausibility anomaly). Both clearly labeled as simulated, not real events, in the write-up. **Superseded, for genericity-proof purposes, by the Cycle 3 Task 2 real-data sequential methodology** — see Section 5's Validation Independence Principle and Section 9's Review & Acceptance History.

**Relationship to the current milestone's two scenarios:** Scenario 1 (wrong/changed data) exercises Phases 0/1 → 2/3 (catch) → 4 → 5 → 6 → Human Approval Gate → 7 → 8. Scenario 2 (correct data) exercises Phases 0/1 → 2/3 (no catch) → Human Approval Gate → 7 → 8, skipping 4–6 because there is nothing to explain. Both scenarios terminate at Phase 8; neither exercises Phase 9.

## 5. Product Decisions — Decision Log

**Convention (added Cycle 3):** Decision Log entries are approved architectural direction, effective immediately upon being agreed — not contingent on implementation. Each entry carries an **Implementation Status**: `Planned` / `In Progress` / `Verified`. A decision's own text describes the decision only, never an implementation claim — "Verified" requires a reviewed Return Report with actual evidence, same bar as everywhere else in this Handbook.

| # | Decision | Rationale | Cycle | Implementation Status |
|---|---|---|---|---|
| D1 | Fiscal year = Jul-Jun, labeled by ending calendar year | Dataset's 36 months split into exactly 3 clean fiscal years on this convention | Pre-Cycle 1 | Verified |
| D2 | "Operating Profit/Margin," never "Gross Profit/Margin" | No COGS line in dataset | Pre-Cycle 1 | Verified |
| D3 | Segment allocation: S&M+CS → Region, R&D → Product, by revenue share; G&A excluded from both | No finer driver available (no rep count, no engineer count, no time-tracking) | Cycle 1 | Verified |
| D4 | Professional Services excluded from R&D allocation base | Real allocation error, not a labeling issue — Professional Services is a services line, not something R&D builds | Cycle 1, retained through Cycle 2 | Verified |
| D5 | BvA flag thresholds: Watch >4%, Major Miss >8% | Reverse-engineered from organizer's reference workbook, 100% match | Pre-Cycle 1 | Verified |
| D6 | **Region/Product allocation views redesigned from "margin" to "investment proportionality"** | Any proportional allocation key makes CM% mathematically identical across all segments in a period — not a labeling problem, a structural one. The metric required 5 simultaneous caveats to read safely and invited the one comparison (cross-segment %) that the math forbids. Renamed, cross-segment % removed entirely, replaced with $-only comparison + single-segment % trend. Demoted from flagship to supporting. | **Cycle 2** | Verified |
| D7 | Department-level Revenue-per-Employee rejected; company-wide Revenue/Employee and department-level Opex/Employee kept instead | Attributing company-wide revenue to one department's headcount reproduces the same attribution fallacy just removed in D6 — revenue isn't produced by a single department. Opex/Employee has no such problem (department's own cost over its own headcount). | Cycle 2 | Verified |
| D8 | Revenue Mix (%) added to Revenue Performance page | Independently varying (reflects real mix shift over time), not a ratio constructed from proportional allocation — does not reduce to D6's mathematical-identity problem | Cycle 2 Task 2 | Verified |
| D9 | `rename_instruction_contribution_margin.md` fully superseded | Superseded by D6; do not apply that file's renaming table in any future session | Cycle 2 | Verified |
| D10 | **Canonical object of the Live system is the Approved Financial Close, not a single active dataset. Live maintains an immutable Close History (one snapshot per approved close). The dashboard, executive narrative, and future board deliverables are outputs generated from an approved close, not independent products.** | The workflow already assumed this: Phase 2 diffs against a previous approved close, Phase 3 uses historical context, the Workflow State Model terminates in Archived (retained, not overwritten), Phase 9 was already framed as a Knowledge Archive. Storage mechanism (GitHub, for this challenge) is an implementation choice, not part of the decision. | Cycle 3 | **Verified — Cycle 3, Task 1.** Minimal folder-based Close History (resolution + archival) confirmed: bootstrap path and normal path both independently exercised (not hand-placed), dynamic resolution with zero hardcoded filenames, Phase 2/3 catches reproduced exactly against archived snapshots, no regression (12/12 tie-outs). Full retention/query/lifecycle management remains deferred to Phase 9, per scope. Not reopened by the End-to-End Product Demonstration milestone. |
| D11 | **Close Validation Engine (`close_validation.py` Phase 2 historical-revision diff + Phase 3 plausibility/headcount-band check) is generically correct, not merely non-regressing against Build's own fixture.** | A fresh, memory-independent Test session — given only the governing methodology document, the canonical Q4 FY2026 dataset, `close_validation.py`, and `close_history.py` — ran Scenarios 1-3 and the immutability check against the real, unmodified Q2/Q3/Q4 FY2026 sequence with no substitution, using department/category/value combinations Test chose independently and never disclosed to Build. The Architect independently re-executed the same harnesses against its own copy of the canonical dataset and reproduced every result exactly. This satisfies the Validation Independence Principle's bar for a genericity claim. **Earlier v1/v2 rounds (dataset-short substitutions, and a procedural claim Test had sourced from prior-conversation memory rather than the governing document) are explicitly excluded from this evidence basis — see Section 9.** | Cycle 3, Task 2 | **Verified — Cycle 3, Task 2, on fresh-session evidence.** See Section 9 for full evidence chain. Not reopened by the End-to-End Product Demonstration milestone. (Note: D11's own Scenarios 1-3 are validation-engine test scenarios, distinct from the current milestone's Scenario 1/Scenario 2 product-demonstration scenarios defined in Section 4.) |
| D12 | **Canonical raw dataset is the Q4 FY2026 file (Jul 2023–Jun 2026), single file at repo root. The Q2 FY2026 and Q3 FY2026 intermediate files used during Task 2's provenance sequence are retained only as archived testing/provenance artifacts, outside the production resolver's search path.** | Principal confirmation, resolving the resolver-collision risk flagged at Task 2 close-out: `rollups.py`'s dataset resolver requires exactly one `*.xlsx` match; multiple dated dataset files at repo root would break it. The Q2/Q3 files have ongoing evidentiary value (they support the v1→v2→v3 evidence chain in Task 2's Return Reports) but no ongoing production role once Q4 superseded them. | Cycle 3 close-out | **Verified — principal-confirmed, resolver-hygiene action, not a code change.** Not reopened by the End-to-End Product Demonstration milestone. |
| D13 | **Machine Recommends, Human Decides. Phases 4–6 (Commentary Matching, Finance Collaboration, Explanation Validation) may produce evidence, matches, drafts, and an adequate/inadequate assessment — they must never set close status to Approved or Rejected. The Human Approval Gate is a genuine blocking state requiring explicit human action; no phase, and no automated agent including Claude itself, may advance a close past it. An "inadequate" assessment is a flag and recommendation to investigate, not a rejection — the human retains full discretion to approve despite it, reject, or request further clarification.** | Preserves the standing principle that the machine assists but does not make the final business decision, made explicit and binding ahead of building Phases 4–6, which introduce automated recommendation/assessment logic into this project for the first time. | Current milestone (End-to-End Product Demonstration prep) | **In Progress — not yet Verified.** Phase 4–6 implementation is complete for v4 scope (`789351b`), v5 scope (`5709c9e`), and the headcount-direction fix (`4a670d0`) — integrated across the Dashboard, `rollups.py`, and `close_history.py`. The Human Approval Gate — the interactive control enforcing this decision's blocking requirement — is complete: implemented, regression-verified, canonical-synced (`cdfeb5701...`), and independently Test-verified across all eight of its own acceptance criteria. **The Phase 7 period-display clarification and register-semantics investigation (commit `57b3aa24...`) are also complete and independently verified — display/documentation-layer only, confirmed to touch zero matching or validation logic, and does not constitute evidence toward D13.** No path anywhere in either implementation sets or implies close approval/rejection status — confirmed by direct code inspection, not narration; every Phase 6 output is Supported/Contradicted/Insufficient only, and the Gate's approve action is never inferred from a Phase 6 outcome (independently confirmed via the Gate's own Case 2/4). Three of the four evidence categories required for Verified are present for the Phase 4-6 UAT set itself: implementation evidence, Builder regression evidence, and confirmed canonical synchronization. **Missing: independent Test evidence of the Section H/J Phase 4-6 UAT set — the one remaining requirement for D13.** Per the Validation Independence Principle, genericity cannot be claimed on Builder's own regression fixtures alone. The Section H/J UAT commentary set (principal-supplied, not yet provided) has not been run. **D13 remains ready for that UAT** — see Section 13, unchanged by this revision. |
| D14 | **Cost Structure page (IA #3) gains two new sub-views: a Department × Cost Category breakdown and a company-wide Cost Category-only breakdown. Both show Current / Prior / Variance $ / Variance %.** | Both real, Verified D11 flagged items (Customer Success/Other Opex, Sales & Marketing/Other Opex) are cost-category-level movements the pre-existing Cost Structure page didn't expose — a Controller could not investigate a flagged variance using only what the product showed. Added to support the "flag → Controller investigates → explains" chain required by the End-to-End Product Demonstration milestone, ahead of Phase 4–6 implementation. | Current milestone (End-to-End Product Demonstration prep) | **Verified.** Criteria 1–19 of the v2 (FINAL) Brief evidence-supported and Architect-reviewed directly against actual delivered files: diffs additive-only, 18/18 tie-outs, AppTest/rendered output reviewed, several Variance $/% and reconciliation figures independently recomputed by hand. Canonical GitHub sync independently confirmed via fresh `git clone` (commits `02438bf`/`45c66ca`), byte-identical to reviewed copies. Two dashboard defects found during product review (period-label format; chart chronological ordering) are both confirmed fixed, with the fixes independently re-verified by the Architect, not accepted on narration — see Section 9 and Section 10. `close_history.py`/`close_validation.py` confirmed byte-identical to canonical (previously open as "Partial," now resolved via direct comparison against fresh-cloned canonical copies). Criterion 9 closed as an evidence-retention limitation, not a defect; D11 is not reopened. |

## 6. Assumptions & Limitations (current, approved)

- Headcount is a stock: rollups report **average** (mean of monthly values) and **ending** (last month's snapshot) headcount, never a sum. Starting company-wide headcount (Jul 2023) = 135; ending (Jun 2026) = 167.
- Region/Product "Net of Allocated Cost" views are **two separate, partial views, not a full segment P&L.** Region view nets S&M+CS only; Product view nets R&D only. Neither is directed at a profitability claim — both are investment-proportionality views by design (D6).
- The mathematical-identity property (any proportional allocation key forces % to be identical across segments in a period) is documented as the reason cross-segment % was removed entirely, not disclaimed alongside it.
- No true volume/rate split exists for revenue or non-headcount expense categories (no unit/customer count in the data) — breadth/concentration measure used instead, named for what it is.
- Department-level Revenue-per-Employee is deliberately not built — see D7.
- Real historical closes: Close History is populated only from this project's own Cycle 3 activity to date — not a long production history — stated explicitly as a limitation, not hidden.
- Finance replies are drafted, never sent — confirmed in the implementation by direct structural code check (no send/transmit function exists anywhere in `commentary_workflow.py`). Phase 6 validation's Verified status is intended to rest on Gregory's synthetic commentary (the Section H/J UAT set), not a live reply thread; that set has not yet been supplied and remains load-bearing for D13 moving off In Progress — see Section 10.
- **`Commentary.xlsx` is optional, not a prerequisite for the core workflow** (v5 Brief, principal decision) — the challenge's judging model requires the workflow to run against the required Northwind sample dataset alone, with no assurance a candidate-specific commentary file will ever be supplied. The core workflow (dataset → dashboard → analysis → narrative → Board output) works correctly whether or not one is present — **implemented, regression-verified, canonical-synced (commit `5709c9e`)**, independently confirmed including the flags-exist-with-no-file case specifically. See Section 8, Section 10.
- Cross-close learning: Phase 9 is a single log entry, described as designed architecture, not a mature capability. Phase 9 is out of scope for the current milestone (Section 4).
- `assumptions_and_limitations.md`'s worked-example dollar figures (e.g. Core Platform Net of R&D Cost) are rounded to the nearest dollar for readability; exact pipeline output carries cents (verified match: $1,638,011 documented vs. $1,638,011.09 computed).
- **Open item:** narrative generation's behavior when `ANTHROPIC_API_KEY` is not present (falls back to a "download prompt" button rather than generating text) has been observed in Test but is not yet formally decided as intended Live behavior vs. a gap — see Section 10. Directly relevant to both scenarios' Phase 7/8 leg. **This fallback path is also the path the Human Approval Gate's Phase 7 guard has been independently Test-verified against**, since the Test environment used had no `ANTHROPIC_API_KEY` set — the Gate's blocking behavior is confirmed for the download-button path; confirming it for the live "Generate narrative" call path (with a key present) remains open, though the guard code is identical for both paths (see Section 10).
- **Resolved:** the dataset-coverage gap that caused Task 2's Test evidence to be provisional through two prior rounds is closed — the canonical dataset now genuinely spans Jul 2023 through Jun 2026 (confirmed by direct inspection of all 5 sheets, not just the Expenses sheet the test scenarios exercised), and is the single file at repo root (D12).
- **Period display convention (added Handbook revision v2.10):** the settled, product-wide, D14-established convention for any period rendered to or matched against user-facing text is `"Q# YYYY"` (e.g. `"Q3 2026"`), never `"Q# FYYYY"` (e.g. `"Q3 FY2026"`). This applies to the observation register, the Commentary Review section, and Phase 7's narrative/prompt handoff alike — confirmed as consistently applied across all of these, independently, at that revision (commit `57b3aa24...`). The raw `"Q# FYYYY"` form remains the correct internal fiscal-calendar value within `rollups.py`'s own pipeline (keying, joining, sorting) and is never used as a lookup/merge key across differently-formatted values; the observation register consistently stores the display-formatted value throughout, so this does not create the cross-format join risk `fmt_period_label()`'s own docstring warns against.

## 7. Current Build Status

| Component | Status |
|---|---|
| Rollups (Revenue, Expenses, Headcount, BvA), QoQ/YoY | Done, verified, 12/12 tie-outs passing |
| Region/Product Investment views (redesigned) | Done — Cycle 2 Task 1 |
| Headcount & Efficiency correction | Done — Cycle 2 Task 2 |
| Opex per Employee (Cost Structure) | Done — Cycle 2 Task 2 |
| Revenue Mix (%) | Done — Cycle 2 Task 2 |
| Narrative prompt — `SYSTEM_PROMPT`/`build_user_prompt` in `rollups.py` and `northwind_narrative_prompt.md` template | **Done — both confirmed byte-identical in substance (rule 2c, Section 4, data block), Cycle 2 Task 3 Item 3** |
| Dashboard tab structure matching IA names | Done — Cycle 2 Task 1 |
| Close v1/v2 simulation + Close Validation Status page | **Done — Cycle 2 Task 3, verified: exact injected values, Phase 2/3 catch confirmed** (regression-only fixture as of Cycle 3 Task 2 — see Validation Independence Principle) |
| Dashboard dependency pin / Styler API | **Done — Cycle 2 Task 4.** `.applymap` → `.map`, `requirements.txt` added (pandas 2.2.3, numpy 2.5.1, openpyxl 3.1.5, streamlit 1.60.0), confirmed clean under fresh install via script-executing test (8/8 tabs, 0 exceptions) |
| Terminology sync (dashboard + rollups.py) | **Done — Cycle 2 Task 4.** Zero case-insensitive matches across all four files, confirmed against canonical store, not just session copy |
| Broken `.md` reference (lines 410/450) | **Done — Cycle 2 Task 4** |
| Close History resolution + archival (D10) | **Done — Cycle 3 Task 1, verified.** `close_history.py` (storage-neutral resolve/archive) + `rollups.py`'s `find_raw_dataset()` extended with dynamic resolution (env override → Close History lookup → bootstrap fallback). Bootstrap and normal paths both independently exercised via `close_orchestrator.py` (Build/Test only, not Live). Live's own Close History starts empty — the Cycle 3 Task 1 demo snapshots are verification artifacts, not seed data for production. |
| Close Validation Engine — Phase 2 (historical-revision diff) + Phase 3 (plausibility/headcount-band) (D11) | **Done — Cycle 3 Task 2, Verified on fresh-session evidence.** `close_validation.py` reviewed as clean interface (zero I/O, zero fiscal-calendar logic, zero injected literals). A fresh, memory-independent Test session ran 3 scenarios with predictions recorded in advance against the canonical Q4 FY2026 dataset, no substitution; the Architect independently reproduced every result. Zero false positives on clean data, both anomaly classes (historical revision, plausibility swing) caught exactly on cases Build did not construct. See Section 9 for why the earlier v1/v2/v3 rounds are excluded from this evidence basis. |
| Canonical dataset resolution (D12) | **Done — Cycle 3 close-out.** Single Q4 FY2026 file (Jul 2023–Jun 2026) at repo root; Q2/Q3 intermediate files archived outside the resolver's search path. Resolver-collision risk closed. |
| Cost Structure — Department × Cost Category view (D14) | **Done — Verified.** Criteria 1–11 evidence-supported: 18/18 tie-outs (12 pre-existing + 6 new across both views), diffs additive-only, AppTest/rendered output reviewed, Variance $/% figures independently recomputed by the Architect. Canonical GitHub sync confirmed via fresh clone. `close_history.py`/`close_validation.py` confirmed byte-identical to canonical. Criterion 9 closed as evidence-retention limitation (D11 not reopened). |
| Cost Structure — company-wide Cost Category-only view (D14) | **Done — Verified.** Criteria 12–19 evidence-supported: reconciliation checks (Category-only sums to Department × Category totals; grand total reconciles to Department-level total) independently recomputed by the Architect and matched to the cent, AppTest/rendered output reviewed, positioned correctly alongside the Department × Category view. |
| Phase 4 — Commentary Matching (`commentary_workflow.py`) | **Done, regression-verified, canonical-synced — Brief v4 scope.** Matches `Commentary.xlsx` entries to the D11/D14 observation register on identifiable references (explicit or inferable); returns explicit no-match rather than forcing a weak guess; full unaltered entry text preserved into Phase 6. Not yet Verified — see D13. **Confirmed unaffected by the Phase 7 period-display work (commit `57b3aa24...`) — zero diff to `build_observation_register()`, `make_observation_id()`, `match_commentary_entries()`, or `_find_period_terms()`.** |
| Phase 5 — Finance Collaboration (draft note, `commentary_workflow.py`) | **Done, regression-verified, canonical-synced.** Reinterpreted per the corrected (non-Controller-facing) architecture: drafts a note for the finance/CFO user only, reflecting Phase 4/6's specific failure reason; no send/transmit path exists anywhere in the implementation (structural code check). |
| Phase 6 — Explanation Validation (`commentary_workflow.py`) | **Done, regression-verified, canonical-synced.** Four-check validator (specific claim; checkable; supported/contradicted; sufficiently specific), two claim types (`headcount`, `category_reallocation` — genuinely wired to D14 data, not merely declared). Supported always worded as consistency, never causal proof. The `headcount` claim type's direction-parsing fix is done, regression-verified, canonical-synced, and independently Test-verified (commit `4a670d0`). Not yet Verified overall — see D13. |
| Commentary Record / version model (Section F, v4 Brief) | **Done, regression-verified, canonical-synced.** Original commentary never overwritten; each revision a new complete version; every version retains its own independently-computed validation result; exactly one version may be marked Accepted; full record (all versions, all results, accepted-version identifier) captured in the immutable D10 Close History snapshot at close approval — confirmed via `archive_close()`/read-back round-trip, no intermediate version discarded. |
| Dashboard — Commentary Review section (v4 Brief, Section E) | **Done, regression-verified, canonical-synced.** AppTest-confirmed: 0 exceptions, 8/8 tabs, Commentary Review section renders with the `Commentary.xlsx` uploader present (labeled "optional," per v5). Live widget interaction (upload → match → accept) independently confirmed via the Human Approval Gate's own Case 5 UAT. Period display in this section now consistently applies the D14 convention at every render point, independently verified at Handbook revision v2.10. |
| Dashboard/Phase 7 — v5 optional-file behavior and three-way narrative handoff (`build_narrative_commentary_section()`) | **Done, regression-verified, canonical-synced — commit `5709c9e`, period-display formatting added commit `57b3aa24...`.** All three Section G cases implemented in correct priority order; `UNRESOLVED`/`UNEXPLAINED` confirmed lexically distinct (dedicated fixture check); "do not invent a cause" instruction embedded directly in the generated prompt text for both cases; Case 1's byte-identical claim tested against the real `rollups.build_user_prompt()` call, not just the isolated helper. New fixture: 23/23, independently reproduced byte-identical. `rollups.py`'s signature change (`commentary_narrative_block`, new) is backward-compatible with v4's `accepted_commentary_lines` parameter, confirmed by v4's fixture still passing unmodified (49/49). **This function now also applies `fmt_period_label` (identity default, backward-compatible) to every rendered period, independently verified.** Gap: the v5 regression fixture file was never committed to canonical `build_test/` — reviewed and run from delivered files only; recorded as open, not blocking. |
| Narrative wording defect ("Controller-Approved Explanations") | **Fixed and canonical-synced.** Corrected to "Finance/CFO-Approved Explanations" — the Controller never approves anything in-product; the finance/CFO user does. Confirmed via full-repository sweep, zero remaining occurrences. |
| **Phase 4–6 canonical synchronization** | **Confirmed.** Promoted to `main`, commit `789351b` (parent `8852b4d`), then the headcount-direction fix at `4a670d0` (parent `9e85292`), then the Human Approval Gate at `cdfeb5701...` (parent `4a670d0`), then the v2.9 Handbook promotion at `0feadb46...` (parent `cdfeb5701...`), then the Phase 7 period-display clarification at `57b3aa24...` (parent `0feadb46...`). Fresh-clone-verified at each step: correct HEAD, expected files present, no temporary patch file committed, clean working tree, diff byte-matches the accepted patch. |
| **The Human Approval Gate (interactive control between Phase 6 and Phase 7)** | **Done, regression-verified, canonical-synced (commit `cdfeb5701...`), and independently Test-verified in full.** Structural `if not _gate_approved:` guard confirmed by direct code review to make `R.call_claude_narrative()` and the prompt-download action genuinely unreachable, not merely cosmetically warned-around, when `close_approval_status != "approved"`. Explicit "Approve close"/"Reject / return close" controls, available regardless of Phase 6 assessment outcome. Revising commentary after approval resets the gate to not-yet-decided. The two Workflow-state-strip rows ("Explanations Validated," "Executive Ready") are computed from real state rather than hardcoded `False`. **Confirmed at Handbook revision v2.10: the promoted Phase 7 period-display diff's changed lines do not overlap the Gate's approval/rejection logic at all.** |
| **The Phase 7 period-display clarification and register-semantics investigation** | **Done, regression-verified, canonical-synced (commit `57b3aa24...`), and independently verified end to end — Handbook revision v2.10.** `build_narrative_commentary_section()` and `accepted_commentary_prompt_lines()` gained an optional `fmt_period_label` parameter, applied at four render points; zero change to `build_observation_register()`, `make_observation_id()`, matching, or validation logic. A separate investigation into the observation register's own period-storage convention concluded current canonical behavior (storing the D14-formatted value) is correct, evidenced via a genuine, executed two-observation matching demonstration, independently reproduced by the Architect from a fresh clone using a fully-specified, internally-consistent dataset fixture. Delivered as a documentation-only clarification (44 comment lines, zero functional change). |
| **End-to-End Product Demonstration — Scenario 1 (wrong/changed data)** | **Not started — current milestone.** Phases 4–6, the Human Approval Gate, and the Phase 7 period-display work all now exist and are regression-verified (the Gate and the Phase 7 work additionally fully independently verified), but the full intake → detect → match → validate → approve → narrative → HTML/PPTX chain has not been run as one integrated pass. Blocked on D13's independent UAT of the Phase 4-6 Section H/J set before this can be claimed proven. |
| **End-to-End Product Demonstration — Scenario 2 (correct data)** | **Not started — current milestone.** Does not depend on Phase 4–6 content, but has not yet been run as an integrated pass through intake → validation → approval → narrative → HTML → PPTX. |
| HTML story, PPTX deck | **Not yet built — explicit final output of the End-to-End Product Demonstration milestone, for both scenarios.** Current source files not yet supplied to Builder. |
| Write-up document (the actual graded submission) | **Not started — deferred until after the End-to-End Product Demonstration milestone completes successfully**, so it can be grounded in a demonstrated run of the complete product rather than component-level evidence alone. |

## 8. Builder Brief History

| Brief | Scope | Status |
|---|---|---|
| Cycle 1 | Rename/allocation fix (Professional Services exclusion), Close v1/v2 planning, decline orchestrator/learning-claims scope | Complete |
| Cycle 2 | Redesign Region/Product views (D6), Headcount correction (D7), Close v1/v2 simulation + Close Validation page | **Complete** — Tasks 1-3 and all follow-ups (line 163 caption, headcount-band rationale, narrative prompt sync) passed. HTML story/deck carved out, remains blocked. |
| Cycle 3 | Task 1 (Close History resolution/archival, D10); Task 2 (Close Validation Engine, Phase 2/3, D11); close-out (canonical dataset selection, D12) | **Complete.** All items Verified. Cycle formally closed. |
| Cost Structure Investigation Views (D14) | Department × Cost Category breakdown and company-wide Cost Category-only breakdown on the Cost Structure page | **Complete.** v2 (FINAL) Brief fully closed — all 19 criteria evidence-supported, both dashboard defects found during review fixed and independently verified, D14 Verified. Superseded v1 and pre-correction v2 draft archived. |
| Phase 4–6 Commentary Workflow — v1 | Initial design; assumed a Controller-facing dashboard/collaboration loop | **Archived — design error, corrected by the principal before implementation.** No implementation was built against v1. Superseded by v2. |
| Phase 4–6 Commentary Workflow — v2 | Corrected: no Controller-facing interface; Controller supplies dataset + separate commentary file externally; finance/CFO user is the sole dashboard operator; complete-commentary-only editing (no fragments); Phase 5 reinterpreted as finance/CFO-facing draft only | **Archived — structural self-audit found 3 non-compliances against the principal's stated deliverable structure** (Section J mislabeled; Phase 6's four checks collapsed to three; Input/Processing/Output labeling missing). Corrected in v3. |
| Phase 4–6 Commentary Workflow — v3 | Same product design as v2, structural corrections applied | **Archived — superseded by v4**, which fixed the two remaining open product-definition questions (commentary-file format; Commentary Record persistence relative to D10). |
| Phase 4–6 Commentary Workflow — v4 | Fixed `Commentary.xlsx` contract (sheet `Commentary`, columns `Commentary_ID`/`Commentary_Text`); full Commentary Record version history captured in the D10 snapshot at close approval | **Implementation complete, regression-verified, canonical-synced (commit `789351b`).** Two Architect-review correction rounds were required before acceptance: (1) claim-type coverage gap — the D14 evidence package field was present but unused by any validator, fixed by wiring `category_reallocation` to real D14 data; narrative wording defect ("Controller-Approved" → "Finance/CFO-Approved"); (2) canonical sync not yet pushed and a truncated regression-evidence artifact — both resolved in the final submission (49/49 regression, independently byte-verified; canonical sync independently confirmed via fresh clone). Not yet Verified — D13 remains In Progress pending Section H/J independent UAT. **Superseded as the active governing Brief by v5**; v4's implementation is not reopened or redone by v5, which adds new requirements on top of it. |
| Phase 4–6 Commentary Workflow — v5 | Adds to v4: `Commentary.xlsx` becomes optional, not a prerequisite for the core workflow; Phase 7's narrative handoff gains a required three-way distinction (no flags; flags with commentary, some possibly unresolved — reported, not omitted; flags with no commentary supplied at all — explicitly reported as unexplained, never invented) | **Implementation complete, regression-verified, canonical-synced — commit `5709c9e`.** Two review rounds: the Architect's independent review (diff applied to a working copy, both fixtures re-run, byte-compared, AppTest run independently since none was submitted) preceded promotion; a post-promotion fresh-clone confirmation then verified the promoted commit (`5709c9e`) is byte-identical to that exact reviewed diff. **Two gaps recorded, neither blocking:** the new regression fixture was never committed to canonical `build_test/` (reviewed and run from delivered files only); no Return Report or AppTest evidence was submitted by Builder this round (the Architect supplied the latter independently, as in the first v4 round). |
| Phase 6 headcount-direction fix — v1 | Direction-aware evaluation of headcount-type claims within `_validate_headcount_claim()` — fixes the Case A/Case B indistinguishability defect an earlier independent Test session discovered against pre-fix commit `5709c9e` (materiality band swallowed claimed direction) | **Implementation complete, regression-verified, canonical-synced (commit `4a670d0`), and independently Test-verified (clean-room Incognito session, all six UAT cases plus the Human Approval Gate's then-absence, Architect-reproduced against the same commit).** Scope confined entirely to `_validate_headcount_claim()` and its new `_parse_headcount_direction()` helper; `close_validation.py` and all other files confirmed byte-identical to canonical throughout. **Superseded as the active governing Brief by the Human Approval Gate Brief.** |
| Human Approval Gate — v1 | Minimum implementation to make the Human Approval Gate a genuine, structural, independently testable blocking control between Phase 6 and Phase 7, per D13's existing (not new) requirement | **Complete.** Implementation complete, regression-verified, canonical-synced (commit `cdfeb5701...`, parent `4a670d0`), and independently Test-verified across all eight acceptance criteria: Cases 1-4, 7, 8 confirmed by a first clean-room Test session and independently reproduced by the Architect; Case 5 (revision-after-approval reset) confirmed by a second, separately-scoped clean-room Test session using a purpose-built, deterministic real-data fixture and genuine real-UI execution, independently reproduced by the Architect with a deliberately varied revision text. Explicit non-goals honored throughout: Phase 4-6, the headcount-direction fix, D11, and Phase 7/8 content were not touched or retested. No further Builder or Test work outstanding on this Brief. Does not by itself move D13 to Verified — the Section H/J Phase 4-6 UAT set remains D13's sole outstanding requirement. **Superseded as the active governing Brief by the Phase 7 period-display addendum.** |
| Phase 7 period-display addendum (addendum to `phase6_headcount_direction_fix_brief_v1.md`), plus follow-on register-semantics investigation | Thread `fmt_period_label` through Phase 7's CFO/Board-facing narrative handoff to apply the settled D14 display convention; separately, investigate whether the observation register should instead store the raw `Fiscal Quarter` value | **Complete — Handbook revision v2.10.** Implementation complete, regression-verified, canonical-synced (commit `57b3aa24...`, parent `0feadb46...`), and independently verified by the Architect end to end, including a from-scratch reproduction of the register-semantics investigation's empirical evidence (fresh clone, fully-specified fixture, executed matching tests, byte-identical results). One round of rework was required mid-task: the first delivered patch bundled an unreviewed, previously-informal register-storage change alongside the display fix; this was caught, separated into its own independently-scoped investigation (Patch B), and resolved in favor of current canonical behavior rather than the informal change. A second round closed a genuine evidence-reproducibility gap (the investigation's demonstration fixture was initially undelivered and, on rebuild, found to have a real internal-consistency defect — see Section 10's corollary) before the finding could be accepted. Does not reopen D10, D11, D12, D14, or the Human Approval Gate. Does not move D13. No further Builder or Test work outstanding on this item. |
| End-to-End Product Demonstration (full milestone) | Integration of all phases into two scenario runs (wrong/changed data; correct data), ending in HTML story and PPTX for each | **Not yet scoped as its own Brief.** Depends on D13 reaching Verified (Section H/J independent UAT) first — implementation work (Phase 4-6, the headcount-direction fix, the Human Approval Gate, and the Phase 7 period-display work) is now entirely complete; this is no longer blocked on any Builder work. |

*(Full brief text for v1–v3 archived externally by PM, not retained in Project once superseded. v4, v5, the headcount-direction fix Brief, the Human Approval Gate Brief, and the Phase 7 period-display addendum text retained in `governance/builder_briefs/`.)*

## 9. Review & Acceptance History

| Task | Criteria checked | Result | Evidence |
|---|---|---|---|
| Cycle 2, Task 1 | 1, 3, 4 | **Pass** | 12/12 tie-outs, zero diffs; grep swept twice (3 comment-only matches found and fixed, then zero); tab renames applied and confirmed via `st.tabs()` grep |
| Cycle 2, Task 2 | 5, 6 | **Pass** | 12/12 tie-outs, zero diffs; structural verification that dept-level Revenue-per-Employee is unconstructable (no revenue column in dept table, no Department column in company-wide table), not just absent from a text search |
| Cycle 2, Task 3 core | 7 (Close v1/v2 injected values, Phase 2/3 catch) | **Pass, accepted** | Exact injected values verified against CSVs; Phase 2 caught the 1 historical revision exactly; Phase 3 caught the 1 plausibility anomaly exactly, no cause guessed; dashboard state display honest (no Phase 4-9 overclaim) |
| Cycle 2, Task 3 follow-up, Items 1-2 | 4 (terminology), documentation | **Pass** | Line 163 before/after clean; assumptions-log entry quoted in full; 12/12 tie-outs, no regression |
| Cycle 2, Task 3 follow-up, Item 3 | 4 (terminology consistency, content correspondence) | **Pass, but scope-limited — see Task 4.** The fix was correctly made and verified in-session; it did not reach the canonical file store, so Test (Cycle 2 batch run) re-encountered the original defect. Not a false Pass on the work done — a gap in the promotion pipeline itself. Resolved by Cycle 2 Task 4 and the new Canonical Sync Verification procedure (Section 0). | Zero matches, case-insensitive, all four files; rule 2c/Section 4/data-block confirmed byte-identical to code, not just absent of banned terms; 12/12 tie-outs, no regression |
| Cycle 2, Task 4 | Dependency pinning, terminology sync (canonical), broken reference, no regression | **Pass** | 8/8 tabs render, 0 exceptions (AppTest, fresh install); 6→0 terminology matches across all 4 files, verified against canonical store; 12/12 tie-outs + both Close v1/v2 injections exact under pinned env; `.md`→`.py` references corrected; root cause of Task 3/Item 3's non-propagation identified and distinguished from the Section 10 pattern |
| Cycle 2 — Test Verification | Full Section 6 criteria, fresh env | **Pass** | 12/12 tie-outs $0.0000; 8/8 tabs, 0 exceptions (AppTest); pinned deps confirmed active; Phase 2/3 catches exact; terminology zero matches (independently re-swept); batch hashes recorded for traceability |
| Cycle 3, Task 1 | D10 — Close History resolution + archival, bootstrap and normal path, no regression | **Pass** | Bootstrap path forced via hard assertion (Close v1 could not be hand-placed); dynamic resolution confirmed zero hardcoded filenames (independent grep); Phase 2/3 catches reproduced exactly against archived snapshots; `metadata.json` includes real git commit hash per snapshot; 12/12 tie-outs, no regression; dashboard re-verified 8/8 tabs clean with Close History populated; `close_orchestrator.py` correctly excluded from Live scope and documented as Build/Test-only, confirmed via re-run after doc addition |
| Cycle 3, Task 2 | D11 — Close Validation Engine genericity, Validation Independence Principle, Dataset Provenance Discipline | **Pass — Verified, on fresh-session evidence.** | Implementation reviewed clean (zero I/O, zero fiscal-calendar logic, zero injected literals, independently re-verified by grep). **Historical record (v1/v2/v3, NOT part of D11's evidence basis):** an earlier Test session ran this methodology across three rounds as dataset coverage improved — v1 (dataset short, through Q2 FY2026 only) substituted the last 3 available real quarters and flagged the gap; v2 (dataset extended to Q3 FY2026) flagged the remaining gap; v3 (dataset extended to Q4 FY2026) ran the real, unmodified Q2/Q3/Q4 FY2026 window with no substitution and all three scenarios passed. Under direct questioning, that same session admitted its stated justification for the v1 substitution ("document gaps as an open item, don't fabricate data") had been asserted as if it were a rule written in `test_instructions_validation_methodology.md`, when it was in fact carried in from prior-conversation memory — the governing document contains no such contingency clause. This was a genuine procedural-independence failure, not a wording slip, and disqualifies v1 and v2 as authorized runs; it also means v3's own procedural claims (sandbox isolation, immutability, prediction timing) could not be trusted on that session's narration alone, even though v3 needed no substitution. **Fresh evidence actually underlying Verified status:** a new Test session, given only `test_instructions_validation_methodology.md`, the canonical Q4 FY2026 dataset, `close_validation.py`, and `close_history.py`, explicitly instructed to disregard all prior-conversation memory, prior Return Reports, prior test results, and prior datasets, independently re-derived and re-ran Scenarios 1-3 and the immutability check against the real, unmodified Q2/Q3/Q4 FY2026 sequence — predictions recorded before execution, department/category/value combinations chosen independently and never disclosed to Build, isolated sandboxes, full harness scripts and sandbox trees supplied as evidence. **The Architect independently re-executed the same harnesses against its own separately-obtained copy of the canonical dataset** (not the fresh session's sandbox copies) and reproduced every result exactly: Scenario 1 (0/0 flags at every step), Scenario 2 (Customer Success/Other Opex, $22,675.64→$27,675.64, 1 Phase 2 flag, 0 Phase 3 flags — exact match), Scenario 3 (Sales & Marketing/Other Opex, $47,857.52→$97,857.52, 0 Phase 2 flags, 1 Phase 3 flag at QoQ +38.42%/headcount change +1.0 — exact match), and immutability (`FileExistsError` on re-archiving an existing period label, triggered directly by the Architect, not just read from a report). The Architect also confirmed by direct code inspection that `archive_close()`'s `rollups_output_src` parameter is only copied to storage and never read by any Phase 2/3 logic, so the documented placeholder used for it has no bearing on the validation evidence. This — the fresh session's evidence plus the Architect's independent, code-level reproduction of it — is what D11's Verified status rests on, not the earlier v1/v2/v3 sequence. (These "Scenario 1/2/3" labels are the D11 validation-engine test scenarios, not the current milestone's Scenario 1/Scenario 2 product-demonstration scenarios defined in Section 4.) |
| Cycle 3 close-out | D12 — canonical dataset file selection, resolver-collision hygiene | **Pass — Verified.** | Principal confirmed the Q4 FY2026 file as sole canonical dataset at repo root; Q2/Q3 files to be retained only as archived provenance artifacts outside the resolver's search path. Not a code change — a repository-hygiene action within the principal's data-provenance authority (Section 8 of the Architect's instructions). |
| Cost Structure Investigation Views (D14) | Criteria 1–19 of the v2 (FINAL) Brief, plus two dashboard defects found during product review | **Pass — Verified.** | All 19 criteria evidence-supported and Architect-reviewed directly against delivered files: 18/18 tie-outs, diffs additive-only, AppTest/rendered output reviewed, Variance $/% and reconciliation figures independently recomputed by the Architect (including both Category-only reconciliation checks, Criteria 12–13, matched to the cent). Canonical GitHub sync confirmed via fresh `git clone` (commits `02438bf`/`45c66ca`), byte-identical to reviewed copies. `close_history.py`/`close_validation.py` confirmed byte-identical to canonical, resolving the long-open "Partial" provenance status. **Two dashboard defects found during product review, both confirmed then fixed then independently re-verified:** (1) global period-label format (`Q4 FY2026` → `Q4 2026`) — fixed via display-layer reformatting, verified by full-text sweep (zero remaining `FY` occurrences) and real AppTest rendered output; (2) full-trend chart x-axis chronological ordering — a pre-existing defect (not introduced by the label fix; confirmed by direct Streamlit/Vega-Lite source inspection that alphabetical and chronological order were never equivalent for either label format), affecting all six `st.line_chart()` calls plus one previously-unidentified full-trend bar chart Builder found during a full-dashboard search — fixed via ordered `pd.Categorical` axis, verified by independently reproducing Streamlit's actual type-inference (`nominal`→`ordinal`) and the real generated Vega-Lite spec, not code inspection alone. Both fixes: zero calculation drift (`rollups.py` byte-identical pre/post the chart-ordering fix; diffs confirm display-layer-only changes), 18/18 tie-outs maintained, 0 exceptions/8 tabs. Criterion 9 closed as an evidence-retention limitation, not a defect — the original injected test value (May 2026 Sales & Marketing/Other Opex, +$50,000) was precisely reconstructed and independently reconciled to the cent, but the underlying artifact was never persisted and cannot be recovered (exhaustive search including Git history); D11 is not reopened and remains Verified. |
| Phase 4–6 Commentary Workflow — v4 Brief implementation, correction round 1 | Claim-type coverage (D14 evidence usability), narrative wording defect, canonical sync, evidence-artifact integrity, AppTest interaction claim | **Not Pass — sent back.** | Initial submission: 42/42 regression (own fixture only, matching v4's own frozen fixture), 0-exception AppTest baseline — but three defects found on independent review, not narration: `category_reallocation`-type gap (D14 breakdown row present in the evidence package but never read by any validator — only `headcount` claims were ever checkable), the narrative wording defect ("Controller-Approved" — architecturally wrong, the Controller never approves anything), and no canonical sync at all (commit existed only in Builder's local sandbox, confirmed absent from GitHub via direct fetch attempt). Also flagged: no structured Return Report; an unsupported commit-message claim of full live-widget AppTest coverage with no test artifact behind it. |
| Phase 4–6 Commentary Workflow — v4 Brief implementation, correction round 2 | Same four items, re-submitted | **Not Pass — sent back again.** | `category_reallocation` genuinely fixed this round (verified: wired to real D14 data, 49/49 regression reproduced byte-identical by the Architect against the applied patch) and the wording defect genuinely fixed (zero remaining occurrences, swept). But: still not pushed to canonical GitHub, and the submitted regression-evidence artifact (`regression_fixture_output_49checks.txt`) was itself truncated — missing the final 11 D10-persistence checks and the `RESULT` line entirely. Only known to actually be 49/49 because the Architect independently re-ran it and it completed cleanly; the artifact handed over as proof did not itself prove the claim. |
| Phase 4–6 Commentary Workflow — v4 Brief implementation, final acceptance | Same four items | **Pass — implementation-complete and regression-verified.** | `category_reallocation` fix and wording fix reconfirmed unchanged. Regression-evidence artifact this round (`regression_evidence_49checks_COMPLETE.txt`) is complete — 193 lines, all D10 checks present, ends with `RESULT: 49 passed, 0 failed` — and is **byte-identical** to the Architect's own independent re-run against the applied patch. AppTest interaction claim correctly resolved by withdrawal, not fabrication: the commit message dropped the unsupported "live widget interaction" claim and now states only what `apptest_baseline_evidence.txt` actually supports (0 exceptions, 8/8 tabs), which the Architect also independently reproduced. Canonical sync was disclosed as blocked by environment constraint (no push credentials in Builder's sandbox — verified `git push` failure message), not a compliance failure; patch confirmed to apply cleanly against actual current canonical HEAD via non-destructive dry run, twice, across two independently fresh clones. **A separate, inspection-only repository-readiness check** (before promotion) independently confirmed: canonical HEAD, governance documents present and current (v4 Brief byte-identical to the text reviewed throughout), all v4-prerequisite files present, patch compatibility via non-destructive `git apply --check`, and zero v5-scope leakage. **Promotion itself** was then independently confirmed via fresh clone: new HEAD `789351b`, all five expected files present, no temporary patch file committed, clean working tree, and a byte-level diff match against the accepted patch (only a `git format-patch` version footer differed). |
| Phase 4–6 Commentary Workflow — v5 Brief implementation | Optional `Commentary.xlsx`; three-way Section G narrative handoff (no flags / flags+file / flags+no-file); backward compatibility with v4's `rollups.py` interface | **Pass — implementation-complete, regression-verified.** | New fixture (23/23) independently reproduced byte-identical, including a direct test of Criterion 1's byte-identical claim against the real `build_user_prompt()` call across three permutations, and a dedicated lexical-distinctness check confirming `UNRESOLVED`/`UNEXPLAINED` never leak into each other. v4's fixture (49/49) reconfirmed unchanged — the new `commentary_narrative_block` parameter is additive, not breaking. 18/18 `rollups.py` tie-outs, no regression. AppTest baseline run by the Architect (none submitted by Builder): 0 exceptions, uploader relabeled "optional," absence still presented neutrally. Diff independently applied to a working copy and verified *before* promotion; a post-promotion fresh-clone confirmation then verified the promoted commit (`5709c9e`) is byte-identical to that exact reviewed diff. **Two gaps recorded, neither blocking:** the new regression fixture was never committed to canonical `build_test/` (reviewed and run from delivered files only); no Return Report or AppTest evidence was submitted by Builder this round (the Architect supplied the latter independently, as in the first v4 round). |
| Phase 6 headcount-direction fix, canonical promotion and independent UAT | Case A-C, correction loop, no-commentary path, Human Approval Gate absence check | **Pass — canonical sync confirmed, Builder regression evidence accepted, independent Test evidence confirmed.** | Canonical promotion (`4a670d0`) independently confirmed via fresh clone: correct HEAD, exactly one file changed, checksum match, four other files byte-identical to parent. Builder regression evidence (49/49 v4, 23/23 v5, seven mandatory direction tests 23/23, rollups 18/18, AppTest 0/8, `close_validation.py` zero-diff) independently reproduced by the Architect (v4 fixture rerun directly; checksums recomputed independently and matched exactly). Independent Test evidence: a clean-room Incognito Test session, given only a self-contained Test Instructions document with an explicit no-external-sources clause, ran the six frozen UAT cases plus the Human Approval Gate re-inspection against canonical `4a670d0` — all six reproduced exactly by the Architect via an independently-written AppTest script, including the headline Supported-without-approval-equivalent case (Case A/Case B no longer producing identical `Phase6Result` objects) and the Gate's then-absence (`archive_close()` never called from the dashboard, confirmed by direct grep). |
| Human Approval Gate Brief implementation, canonical promotion, and independent UAT (Cases 1-4, 7, 8) | Structural blocking (Criteria 1-3), Supported-without-approval (the headline case), approval unlocking Phase 7, rejection re-blocking it, source-level guard confirmation, zero exceptions | **Pass — canonical sync confirmed, Builder regression evidence accepted, independent Test evidence confirmed for six of eight cases.** | Local-commit-then-branch-then-`main` promotion sequence independently verified at each stage (local SHA not treated as evidence until pushed; branch fetched and verified before authorizing `main` promotion; `main` HEAD independently confirmed post-promotion via an ordinary default-branch clone resolving to `cdfeb5701...`). Checksum `939c9240...` confirmed at every stage. A clean-room Test session (self-contained Test Instructions, explicit no-external-sources clause, Incognito) executed Cases 1-4, 7, 8 against `cdfeb5701...`; the Architect independently reproduced all six via a separately-written AppTest script (11 individual assertions), including against the real, now-canonical artifact (not just the pre-promotion patched file). Case 5 (revision-after-approval reset) was explicitly identified as the one case requiring genuine UI execution the canonical dataset alone could not support, and was carried forward as a separately-scoped follow-up rather than inferred or source-read as a substitute. |
| Human Approval Gate Brief, Case 5 (revision-after-approval reset) | Genuine real-data fixture construction; real upload/match/accept/approve/revise UI execution; observed (not inferred) reset of `close_approval_status`; observed (not inferred) re-blocking of Phase 7 | **Pass.** | A dedicated, self-contained clean-room Test Brief (`case5_test_brief.md`) specified a fully deterministic fixture-construction procedure — a single, exactly-documented `+$40,000.00` modification to Sales & Marketing / Other Opex for May 2026, grounded directly in `close_validation.py`'s own real `DEFAULT_PLAUSIBILITY_QOQ_THRESHOLD`/`DEFAULT_PLAUSIBILITY_HEADCOUNT_BAND` constants (not an arbitrary value), plus a matching PL_Summary adjustment to keep the workbook internally self-consistent. A second, separately-scoped clean-room Test session constructed this fixture independently, confirmed the real starting value (`$47,857.52`) before mutating it, confirmed the resulting Phase 3 flag text matched the Brief's predicted text, and then executed the full real-UI sequence (upload → match → accept → approve → confirm Phase 7 reachable → submit a genuine revision via the real "Submit revision & revalidate" button → observe `close_approval_status` reset to `not_yet_decided` → observe Phase 7 blocked again) with zero exceptions at every step, predictions recorded before each step per this project's standing discipline. **The Architect independently reproduced this entire sequence from scratch** — a separately-obtained fresh clone, an independently-constructed fixture (confirmed against the same real starting value), and a separately-written harness using a deliberately different revision text than either the Brief's example or the Test session's, specifically to rule out any dependency on the exact wording of the revision. Both runs produced identical results: flag text, `close_approval_status` transitions (`approved` → `not_yet_decided`), and Phase 7 reachability (present, then absent) all matched exactly. The Test session's raw console log was independently spot-checked and matches its Return Report's quoted values exactly — not narration. This closes the last of the Human Approval Gate Brief's eight acceptance criteria. Does not itself move D13 to Verified. |
| Phase 7 period-display addendum, initial submission | Formatter-threading scope, non-regression | **Not Pass — sent back.** | The delivered patch correctly implemented the display fix but also bundled an unreviewed, previously-informal, not-yet-promoted change to `build_observation_register()`'s Phase 3 branch (storing the raw `Fiscal Quarter` value instead of the display-formatted value) — disclosed by Builder in the Return Report's own environment note, but never independently reviewed. The Architect confirmed via fresh clone that canonical `build_observation_register()` still applied `fmt_period_label()` as expected, then flagged the bundling and required the two changes be separated. |
| Phase 7 period-display addendum, register-semantics investigation (Patch A / Patch B split) | Ten investigation items (source dataset format, governing convention, current canonical behavior, `_find_period_terms()` behavior, root cause, correctness of the raw-storage fix, correct representation, effect on Observation IDs, effect on matching, other downstream consumers) | **Pass, with one evidence gap flagged.** | Patch A (display formatting) independently confirmed scoped correctly: applies cleanly to true canonical, zero diff to `build_observation_register()`/matching/validation, 49/49 v4 fixture and clean 18/18 tie-outs reproduced by the Architect from a fresh checkout. Patch B (comment-only clarification) independently confirmed genuinely comment-only (45 added lines, 1 removed, zero non-comment/blank additions) via direct diff count. Code-level reasoning (Items 1-4, 6-10) independently verified by the Architect reading `fmt_period_label()`, `build_observation_register()`, `_find_period_terms()`, and `match_commentary_entries()` directly from a fresh canonical clone — matches Builder's claims exactly, no dataset dependency. **Item 5's empirical demonstration could not be independently reproduced this round**: the modified dataset fixture (`incoming_test2.xlsx`) was not delivered, was not covered by the delivered checksum evidence (which attested only to the four `.py` files), and the Architect's own attempt to run the identical test script against a genuinely unmodified canonical dataset produced an empty register, not the claimed two-observation result. The fixture's own printed output additionally showed a real tie-out failure (`max diff $45,000.00`). Patch A accepted for promotion on its own at this stage; Patch B and the "no code fix needed" conclusion held provisional pending the missing evidence. |
| Phase 7 period-display addendum, evidence-reproducibility follow-up (Item 5) | Exact fixture delivery or deterministic reconstruction procedure; explanation and resolution of the tie-out mismatch; confirmation the mismatch had no bearing on Item 5's finding | **Pass.** | Builder delivered both the exact reconciled fixture (`incoming_test2_RECONCILED.xlsx`) and its full deterministic construction procedure (exact rows, exact dollar amounts, exact PL_Summary adjustment). **The Architect independently reproduced every element of this from scratch**, not from Builder's own rerun: diffed the reconciled workbook against a fresh canonical clone and confirmed the claimed 6 Expenses rows and matching PL_Summary rows/footer to the cent; ran `rollups.py`'s own tie-out check directly against the reconciled file and confirmed a clean result with zero `FAILURES` output; ran the unmodified Item 5 test suite from a fresh code checkout against the reconciled fixture and obtained **8/8 passed**, byte-identical (same register, same Observation IDs, same match/no-match outcomes) to Builder's own delivered rerun. Confirmed the original defect (Expenses modified without recomputing `PL_Summary`) was genuine, not incidental — acknowledged as a fixture-construction error rather than argued around. This closed the outstanding evidence gap; Patch B and the underlying conclusion moved from provisional to fully independently verified. |
| Phase 7 period-display addendum, promotion checklist | Pre-promotion readiness, regression against combined state, Handbook draft accuracy, scope confirmation (D10/D11/D12/D14/Gate/D13 untouched) | **Pass, promotion itself not completed this round (environment constraint, disclosed).** | Pre-promotion readiness independently confirmed: both patches, individually and combined, apply cleanly to a fresh canonical clone via `git apply --check`; combined diff scope confirmed as exactly `commentary_workflow.py` and `Northwind_Financial_Dashboard.py` (116 insertions, 12 deletions), zero diff elsewhere. The Human Approval Gate's approval/rejection logic confirmed to sit entirely outside the diff's changed lines. Regression against the actual combined, locally-committed state independently reproduced by the Architect: v4 49/49, clean `rollups.py` tie-out, and the period-semantics suite 8/8 against the reconciled fixture (byte-identical to Builder's delivered evidence). Handbook draft reviewed against the evidence chain and found accurate. Push itself genuinely failed (`fatal: could not read Username for 'https://github.com'`) — correctly reported as a real attempted-and-failed action, not narrated around; the Architect did not attempt a push either, consistent with holding no GitHub write authority under this project's governance. One trivial, non-blocking note: the checklist's file manifest listed three files not actually present in that delivery (already held from prior turns); no functional impact. |
| Phase 7 period-display addendum, genuine promotion and final verification | Actual push completion, fresh-clone post-promotion verification, byte-match against previously-reviewed files, scope and regression re-confirmation against the real commit | **Pass — Verified.** | Fresh clone independently performed by the Architect (not relying on the principal's own reported clone): resolves directly to `57b3aa24dcd4abd7812bcdeb68bb4bc521fb3140`, parent `0feadb46...`. Exactly two files changed (`Northwind_Financial_Dashboard.py`, `commentary_workflow.py`; 116 insertions, 12 deletions), matching the pre-promotion diff stat exactly. Both files at the real canonical commit confirmed byte-identical to the exact versions independently reviewed pre-promotion — nothing drifted between review and push. `rollups.py`, `close_validation.py`, `close_history.py` confirmed zero-diff against the parent commit. `python3 -m py_compile` on both changed files: clean. Regression re-run directly against the actual promoted commit (not a pre-promotion copy): v4 fixture 49/49, `rollups.py` tie-out clean. This closed the item; Handbook updated to v2.10 to record it. |
| Handbook revision v2.10 → v2.11 (Repository Identity subsection) | Documentation-only, governance addition; confirm no architectural/code/Decision Log impact | **Pass — governance-only, Architect standing authority. Canonical synchronization confirmed this revision (v2.12).** | New subsection added to Source of Truth recording repository identity, the Architect→Builder handoff rule, and the known-identity-vs-verified-state distinction. No code touched, no Decision Log entry added or altered, D13/Cycle 3/D14 unaffected. **Canonical GitHub synchronization of v2.11 independently confirmed this revision** via a fresh clone that resolved to `91f187b3fff971ceb8c7e600c5fbb615e01e8e9d`: `project_handbook.md` at that commit is byte-identical to the v2.11 text previously held in context (spot-checked directly, including the Repository Identity subsection itself), and `governance/architect_project_instructions.md` at that commit likewise matches the Architect instructions text already in use — no drift found on either file. |
| Handbook revision v2.11 → v2.12 (Section 2a, AI Role Boundaries) | Documentation-alignment clarification of already-approved product intent; confirm no Decision Log/architectural impact; confirm implementation was inspected separately, not assumed | **Pass — documentation-alignment clarification, Architect standing authority for documentation-only corrections; implementation separately inspected against this definition (see Architect Documentation & Alignment Report entry, this table).** | New Section 2a added, stating the confirmed eight-step workflow, the deterministic-truth/AI-reconciliation/AI-communication/AI-prohibition distinction, the corrected Phase 4 requirement ("inferable" = meaning-based interpretation of the complete commentary, not synonym/keyword matching), and the Phase 7 no-invention boundary. Two one-line pointers added to Section 4's Phase 4 and Phase 7 rows. No Decision Log entry added or altered; D10/D11/D12/D14/Human Approval Gate not reopened; D13's Implementation Status not moved. **Canonical GitHub synchronization of v2.12 has not yet been verified** — drafted in a session with repository read access but no push credentials (consistent with the Architect's standing lack of direct GitHub write authority); see the corresponding Known Technical Debt entry. |
| Architect Documentation & Alignment Report — Phase 4 semantic reconciliation / Phase 7 output alignment | Corrected product workflow confirmed; canonical `commentary_workflow.py` (Phase 4 matching) and the executive-narrative prompt chain (`rollups.py` `SYSTEM_PROMPT`/`build_user_prompt`, `commentary_workflow.py`'s Section G handoff, `northwind_narrative_prompt.md`) inspected directly against Section 2a's corrected requirement | **Phase 4: does not satisfy the corrected requirement. Phase 7: satisfies the no-invention boundary in the executable prompt-construction path; the standing `northwind_narrative_prompt.md` specification document was found stale/incomplete relative to that path and has been updated to match.** | Full implementation-assessment reasoning, evidence, and the resulting minimum Builder scope are recorded in the Architect's delivered Documentation & Alignment Report for this task (external deliverable, not reproduced in full here per the Handbook's evidence-pointer convention). Summary: `match_commentary_entries()`/`_find_canonical_terms()` in `commentary_workflow.py` require an exact canonical Department/Category name or a fixed, hard-coded synonym-dictionary hit for a match to occur at all (`if not dept_hits or not cat_hits: ... no match`) — structurally incapable of resolving a reference that names neither, however clear its meaning would be to a human reader; zero LLM/API calls exist anywhere in the Phase 4 path (confirmed by direct code read, no `import anthropic` or equivalent in the matching functions). `build_narrative_commentary_section()` (Section G) already carries explicit, per-block "do not infer, guess, or invent a cause" instructions for both the Case 2 (unresolved) and Case 3 (unexplained) blocks it inserts into the prompt — this satisfies the no-invention requirement at the level that actually reaches the model. `northwind_narrative_prompt.md`, however, predates Phase 4-6 entirely and contained no reference to accepted commentary, unresolved/unexplained states, or a no-invention rule for them — a genuine documentation-drift finding, now corrected (see `northwind_narrative_prompt.md`'s own revision note). D13's Implementation Status is unaffected — see the Decision Log entry, unaltered. |
| Architect Documentation & Alignment Report — Phase 4 Semantic Reconciliation Scope Finalization | Finalize implementation-ready Phase 4 scope: one-observation-to-one-commentary exclusivity; Department+Category+Value+conditional-Period as the deterministic-match condition; semantic reconciliation as a strictly gated exception path (unresolved commentary AND at least one unmatched observation remaining); LLM-must-not-run conditions; failure/degraded-mode behavior | **Three further, concrete gaps confirmed by direct code inspection beyond the original finding: (1) no `Value` field is read anywhere in `match_commentary_entries()` — Department/Category/Period only; (2) a genuine matching defect — when a commentary states a period that does not belong to the single Department/Category candidate, the code silently keeps that wrong-period candidate instead of rejecting it, because `candidates` is only reassigned when the narrowed set is non-empty; (3) no exclusivity enforcement exists anywhere in the matching or dashboard glue code — two independent commentary entries that both resolve to the same observation are silently appended as two versions of the same `CommentaryRecord`, with no rejection, warning, or "already occupied" signal.** | Full per-item classification (NO CHANGE REQUIRED / CHANGE REQUIRED — ADOPT / CONFLICT IDENTIFIED / DOCUMENTATION-ONLY / DEFERRED), the finalized Builder Brief text, and the UAT specification are recorded in the Architect's delivered report for this task (external deliverable). Evidence for the three findings: `_find_canonical_terms()`/`match_commentary_entries()` signature and body read directly from the fresh canonical clone (HEAD `91f187b3fff971ceb8c7e600c5fbb615e01e8e9d`) — grepped for any dollar/value/amount field, none found; the period-narrowing bug is a direct read of the `if period_hits: narrowed = ...; if not narrowed.empty: candidates = narrowed` block, which leaves `candidates` unchanged (including a wrong-period single candidate) whenever the stated period matches none of the Department/Category candidates; the exclusivity gap is a direct read of `Northwind_Financial_Dashboard.py`'s match-results loop, which keys `commentary_records` by `matched_observation_id` and calls `record.add_version()` for every matching commentary with no check for a prior, different commentary already occupying that `oid`. No LLM/API call exists anywhere in the Phase 4 path (unchanged finding from the prior task). D13's Implementation Status is unaffected — see the Decision Log entry, unaltered, and the corrected Section 13 language above (two independent outstanding requirements, not one). |
| `phase4_semantic_reconciliation_brief_v1.md` — principal authorization | Governance-only: activate the finalized Brief per explicit principal instruction | **Pass — authorized. Not an implementation or verification event.** | The Brief's Status line changed from "DRAFT — NOT YET ACTIVATED" to "Active," superseding v5's Section B only. No code was written or modified by the Architect (Builder-owned work, per Architect instructions Section 3/Section 6). No Return Report exists. D13's Implementation Status is unaffected — remains In Progress, two independent outstanding requirements (Section 13). Canonical synchronization of this authorization (i.e., of the Brief document itself reaching `governance/builder_briefs/` on canonical `main`) has not been verified — see Section 10. |
| `phase4_semantic_reconciliation_brief_v1.md` → `v2.md` — credential-security / API-cost scope refinement | Integrate a public-project credential-exposure boundary and an API-cost-consumption boundary into the governing Brief, per explicit principal instruction; confirm the deterministic-first/semantic-fallback architecture is unchanged; confirm no Builder access to protected UAT Cases B/C is introduced | **Pass — scope refinement, made before any Builder implementation began, still governance-only (no code touched).** | New Section 5a added (credential boundary: never hardcode/commit/expose the Anthropic API key, runtime-secret-only, existing `ANTHROPIC_API_KEY` pattern remains the model; cost boundary: deterministic success short-circuits the LLM call, no automatic per-commentary calls, no retry loops on failure, no rerun-triggered duplicate calls, no background polling). Two new acceptance criteria added (9: full-repository credential-exposure sweep; 10: call-count assertion across simulated reruns confirming no duplicate/uncontrolled invocation). Sections 1–4 (deterministic pass, exclusivity, invocation gate, LLM boundaries) and original Criteria 1–8 are byte-for-byte unchanged — confirmed by direct diff between v1 and v2, no unintended drift. Independent Test boundary (Cases B/C) is unaffected; nothing in this revision grants Builder access to protected UAT content. D13's Implementation Status is unaffected. Canonical synchronization of v2 has not been verified — see Section 10. |
| End-to-End Product Demonstration (full milestone) | Not yet reviewed | **Not started.** | All Phase 4-6 implementation work (v4, v5, the headcount-direction fix), the Human Approval Gate, and the Phase 7 period-display work are now complete; this now depends solely on D13 reaching Verified via the Section H/J independent UAT of the Phase 4-6 commentary set (Section 13) — no further Builder implementation work is a precondition. **A newly-identified precondition, from this revision's Architect Documentation & Alignment Report, may also apply: if the principal authorizes the minimum Phase 4 semantic-reconciliation change that report defines, that work (and its own independent UAT) would need to complete before Scenario 1 can be considered proven against the corrected product definition — see that report's Section H/Governance Status.** This row will be populated once the demonstration itself begins and produces evidence. |

**Cycle 2 close-out: all 9 Section 6 criteria met** for every artifact that currently exists (pipeline, dashboard, narrative prompt, close simulation, assumptions doc). Criteria 3-4 have no finding against the HTML story or PPTX deck because those artifacts don't exist yet — carved out of this promotion, not a failure of it.

**Cycle 3, Task 1 close-out:** D10 implementation status moved to Verified. Live's canonical data model is now the Approved Financial Close, with a minimal, storage-neutral resolution/archival layer in place. Full retention, querying across many closes, and lifecycle management remain deferred to Phase 9, per the task's explicit scope.

**Cycle 3, Task 2 close-out:** D11 implementation status moved to Verified. The Close Validation Engine (Phase 2 + Phase 3) is proven generically correct — not merely non-regressing — via a fresh, memory-independent Test session's evidence, independently reproduced by the Architect using the same code and harnesses against a separately-obtained copy of the canonical dataset. The Dataset Provenance Discipline this task's earlier v1/v2 rounds motivated adding to this Handbook remains standing procedure and is credited with surfacing the original coverage gap as a protocol success. Separately, the earlier session's admission that a stated justification for its v1 substitution had been sourced from prior-conversation memory rather than the governing document — not caught until directly challenged — is recorded as a genuine finding, not smoothed over: it is why v1/v2/v3 are retained here as historical record but excluded from D11's actual evidence basis, and why the fresh session was run under an explicit no-prior-memory instruction and then independently re-verified rather than accepted on its own narration.

**Cycle 3 close-out (full):** D12 implementation status moved to Verified. The resolver-collision risk flagged when the third dataset file arrived is resolved by principal decision: single canonical Q4 FY2026 file at repo root, Q2/Q3 files archived outside the resolver's search path. **Cycle 3 has no open items.** All three of its decisions (D10, D11, D12) are Verified and are not reopened by the End-to-End Product Demonstration milestone.

**Cost Structure Investigation Views (D14) close-out:** D14 implementation status moved to Verified. Both new Cost Structure views (Department × Cost Category; company-wide Cost Category-only) are Built and Verified on evidence the Architect independently reviewed and, in several places, independently reproduced (tie-outs, rendered output, hand-recomputed figures) rather than accepted on narration — the same standard applied to D10/D11/D12. Two dashboard defects surfaced during product review (period-label format; chart chronological ordering) were confirmed, fixed, and independently re-verified before this close-out, including one defect (chart ordering) found to pre-date this task entirely and affecting the dashboard globally, not just the new views. The long-open `close_history.py`/`close_validation.py` provenance question is resolved. Criterion 9 is closed as a documented evidence-retention limitation; D11 is not reopened and remains Verified.

**Phase 4–6 (v4 Brief) close-out:** implementation status moved to **Done, regression-verified, canonical-synced — not yet Verified.** `commentary_workflow.py` (Phase 4/5/6 logic) and `build_test/commentary_workflow_demo.py` (Builder's own regression fixture) are new files, promoted to canonical GitHub at commit `789351b`; `Northwind_Financial_Dashboard.py`, `close_history.py`, and `rollups.py` were modified in place. Two Architect-review correction rounds were required (see above) before acceptance. D13's Implementation Status moves from Planned to **In Progress**: implementation evidence, Builder regression evidence, and canonical synchronization are all independently confirmed for the v4/v5/headcount-fix scope, and both the Human Approval Gate precondition and the Phase 7 period-display work are also fully independently verified; independent Test evidence of the Section H/J Phase 4-6 UAT set is the one remaining requirement for Verified, and has not yet been supplied.

**Human Approval Gate Brief close-out:** implementation status moved to **Complete — all eight acceptance criteria independently evidenced.** `Northwind_Financial_Dashboard.py` was the only file modified across the Brief's entire lifecycle (local commit → branch push → `main` promotion), confirmed at every stage. Cases 1-4, 7, 8 were independently Test-verified in one clean-room session; Case 5 required a second, separately-scoped clean-room session using a purpose-built, deterministic real-data fixture, since the canonical dataset alone cannot exercise the revision-reset path. Both sessions' results were independently reproduced by the Architect, including a deliberate variation (a different revision text for Case 5) specifically to rule out a text-dependent result. This closed the Human Approval Gate as an open item in Known Technical Debt and as a precondition of D13. D13 itself was not moved by this close-out — it remained In Progress, gated solely on the Section H/J Phase 4-6 UAT set (Section 13).

**Phase 7 period-display addendum and register-semantics investigation close-out (Handbook revision v2.10):** implementation status moved to **Complete — independently verified end to end, including empirical evidence, canonical-synced at commit `57b3aa24dcd4abd7812bcdeb68bb4bc521fb3140`.** `commentary_workflow.py` and `Northwind_Financial_Dashboard.py` were the only two files modified. Two rounds of rework were required and are recorded, not smoothed over: (1) the first delivered patch bundled an unreviewed register-storage change alongside the display fix, caught and separated into its own investigation; (2) the investigation's initial empirical evidence depended on an undelivered dataset fixture that, on rebuild, was found to have a genuine internal-consistency defect (a real tie-out failure), which was corrected and re-verified before the finding was accepted. The final conclusion — current canonical behavior is already correct, no code fix warranted, delivered as documentation only — is now independently, empirically verified by the Architect from a fresh clone, not accepted on narration. This closes both items as open technical debt (Section 10). D13 itself is not moved by this close-out — it remains In Progress, gated solely on the Section H/J Phase 4-6 UAT set (Section 13), unaffected by any part of this work.

**Handbook revision v2.10 → v2.11 close-out (this revision):** a single "Repository Identity (standing context)" subsection was added to the top of the Source of Truth section, recording the project's canonical repository, branch, clone URL, and known Codespace path as durable standing context, together with an Architect → Builder handoff rule and an explicit known-identity-vs-verified-state distinction. This is a documentation-only addition made under the Architect's standing authority for documentation-only corrections with zero logic or behavioral impact (Architect instructions, Section 3). No code changed, no Decision Log entry was added or altered, and D13, Cycle 3, and D14 are all unaffected. **Canonical GitHub synchronization of v2.11 itself is not claimed** — this revision has not yet been confirmed present in a fresh clone of the canonical repository; that check should be run, and this row updated, once repository access is available in a session making that claim.

**Status: promoted to Live (as of commit `57b3aa24...`; v2.11's own promotion status is separately tracked above, not yet confirmed).** Batch = `rollups.py`, `Northwind_Financial_Dashboard.py`, `northwind_narrative_prompt.md`, `assumptions_and_limitations.md`, `requirements.txt`, `close_history.py`, `close_validation.py`, `commentary_workflow.py`, `Northwind_Sample_Dataset.xlsx` (Q4 FY2026, single canonical file). **Excluded from Live** (Build/Test verification tools only): `close_v1_v2_simulation.py`, `close_orchestrator.py`, `build_test/commentary_workflow_demo.py`, and the archived Q2/Q3 dataset provenance artifacts. HTML story and PPTX deck remain not yet built, and are now explicit final outputs of the End-to-End Product Demonstration milestone. **D14 is Verified and is part of the Live batch** (carried within `rollups.py`/`Northwind_Financial_Dashboard.py`, already listed above — no new files introduced). **`commentary_workflow.py` and `Northwind_Financial_Dashboard.py` are part of the Live batch at commit `57b3aa24...`, incorporating the Human Approval Gate and the Phase 7 period-display work, both fully complete and independently verified; D13 itself is In Progress, not yet Verified,** pending Section H/J independent UAT.

## 10. Known Technical Debt

- **Phases 4–6 are fully implemented (v4 and v5 scope, plus the headcount-direction fix) and regression-verified, but D13 is not yet Verified — the remaining gap is independent Test evidence of the Section H/J Phase 4-6 UAT set, not missing code or missing interactive control.** `commentary_workflow.py` and `build_narrative_commentary_section()` are complete, integrated, canonical-synced. The Human Approval Gate is complete and fully independently Test-verified; it is not part of this gap. **The Phase 7 period-display formatting is also complete and independently verified; it is not part of this gap either.** What's still open is the same requirement that gated D11's Verified status: genericity cannot rest on Builder's own regression fixtures alone (Validation Independence Principle). The Section H/J UAT commentary set — three principal-supplied cases (Supported/Contradicted/Insufficient) plus a correction-loop revision, per v5 Section H also exercised against Section G Case 3 (no file supplied) — has not yet been supplied. **This is now the single blocker for D13 reaching Verified and for the End-to-End Product Demonstration's Scenario 1 to run as genuine evidence rather than against Builder's own fixture.** Per principal direction, the principal will supply the actual commentary text directly; the Architect specifies required characteristics only (Section H) and does not author it, preserving Validation Independence at the content level. See Section 13.
- **The Human Approval Gate's interactive control — resolved.** Previously recorded here as "still a real, open gap" (no `st.button` approval control existed behind the Close Validation Status page). This is closed: implemented, regression-verified, canonical-synced (commit `cdfeb5701...`), and independently Test-verified across all eight acceptance criteria of `human_approval_gate_brief_v1.md`, including the structural (not cosmetic) nature of the block, the Supported-without-approval case, and the revision-after-approval reset (Case 5), which required a purpose-built fixture and genuine real-UI execution rather than source inspection. The Commentary Record's per-observation Accept action (Section E/F, v4 Brief) remains a distinct, narrower human action (accepting one commentary version) from the close-level approve/reject action — both are real, working controls.
- **Phase 7 narrative period-display + observation-register period-semantics investigation — resolved, canonical-synced (`57b3aa24...`).** An addendum to `phase6_headcount_direction_fix_brief_v1.md` identified that `build_narrative_commentary_section()` (Phase 7's Section G handoff, CFO/Board-facing per IA #9) was not applying the D14 display-format convention to period text the way every other rendered surface already does. This was fixed by threading an optional `fmt_period_label` parameter through `build_narrative_commentary_section()` and `accepted_commentary_prompt_lines()`, applied at four render points, with zero changes to `build_observation_register()`, `make_observation_id()`, `match_commentary_entries()`/`_find_period_terms()`, or any validation logic. A follow-on, independent investigation into whether the observation register should instead store the raw `Fiscal Quarter` value established that **current canonical behavior is already correct**: `_find_period_terms()` is a format-agnostic substring matcher, and commentary written in the settled D14 convention (`"Q2 2025"`) matches correctly today; storing the raw value would have reversed this. Delivered as a documentation-only clarification (44 comment lines, zero functional change) rather than a code fix, independently confirmed genuinely comment-only. If commentary/UAT content still uses the older `"FY"`-inclusive wording, correcting that content is a separate, later, out-of-scope action. Regression: v4 49/49, v5 23/23, headcount-direction fixture 23/23, `rollups.py` 18/18 tie-outs, targeted period-semantics suite 8/8 — all independently reproduced by the Architect against the actual promoted commit, not a pre-promotion copy. `close_validation.py`/`close_history.py` confirmed byte-identical to canonical.
- **v5 regression fixture not committed to canonical `build_test/`.** `commentary_workflow_v5_demo.py` (23/23 checks, independently reproduced by the Architect) was reviewed and run from Builder's delivered files, not from a canonical clone — confirmed absent from `build_test/` at commit `5709c9e` by direct inspection. The implementation it validates is genuinely promoted and correct; this is a canonical-completeness gap only (a fresh session cloning the repo can't itself re-run this specific check), not an implementation defect. Should be closed at the next Builder touch-point.
- **v5 governance file drift, canonical vs. approved text.** The canonical `governance/builder_briefs/phase4_6_commentary_validation_brief_v5.md` (checksum `c321eaef...`) is the pre-cleanup draft, not the final text the Architect delivered and the principal approved (`6fbc02cd...`). Confined to four header/metadata lines (D13's status label, a stale Handbook-version cross-reference, two leftover "(once active)" qualifiers) — Sections A–K, the actual requirements Builder implemented against, are identical either way, so v5's implementation is unaffected. Cosmetic but should be corrected in a future governance-only commit.
- **Period Label Format defect — resolved, D14 close-out.** Global display-layer defect (labels rendered as `Q4 FY2026`; required `Q4 2026`) confirmed during product review, fixed via display-only reformatting (`rollups.py` unchanged), and independently re-verified by the Architect: full-text sweep confirmed zero remaining `FY` occurrences in any rendered label, real AppTest output showed corrected labels live across all affected views including both D14 views, and the fix was confirmed to have zero effect on any financial calculation, tie-out, or Variance figure. Canonical GitHub sync confirmed via fresh clone.
- **Full-trend chart chronological-ordering defect — resolved, D14 close-out.** A separate, pre-existing defect (not introduced by the period-label fix — alphabetical and chronological order were never equivalent for either label format, confirmed by direct computation) causing all six `st.line_chart()` full-trend charts, plus one previously-unidentified full-trend bar chart (`pivot3`, "Expenses by Department — full trend," found by Builder during a full-dashboard search per the Brief's instruction not to assume only the known charts were affected), to render in alphabetical rather than chronological order. Root cause confirmed by direct inspection of Streamlit's installed source: `st.line_chart()` assigns a `"nominal"` Vega-Lite type to plain string axes and never applies a sort override for line charts (sort logic is coded to apply only to bar charts), so Vega-Lite's default ascending/alphabetical sort took over regardless of the underlying DataFrame's row order. Fixed via an ordered `pd.Categorical` axis, which Streamlit's own type-inference logic special-cases to `"ordinal"` typing (confirmed both by reading Streamlit's source, which cites this as an intentional documented pattern — `streamlit/streamlit#7776` — and by independently reproducing the actual generated chart spec). Confirmed zero calculation drift: `rollups.py` byte-identical before and after this fix; dashboard diff confirmed additive/display-layer only.
- **Criterion 9 (Phase 2/3 D11 reproduction for the Cost Structure task) — resolved, evidence-retention limitation, not a defect.** The archived Q2/Q3 FY2026 vintage files were supplied and run through the real, unmodified `close_history.py`/`close_validation.py` path; the original injected anomaly (a single-month +$50,000 value, Sales & Marketing/Other Opex, May 2026) was precisely reconstructed and independently reconciled against D11's recorded evidence to the cent, but the underlying artifact itself was never persisted anywhere accessible (confirmed by exhaustive search including Git history) and cannot be recovered. D11's Verified status is not affected and is not reopened.
- **Documentation/implementation grain mismatch — open, non-blocking (identified during Cycle 3, Task 2 D11 review).** `test_instructions_validation_methodology.md`'s Input Contract describes `Period` with a quarter-grain example (`"FY2026-Q2"`), while `close_validation.py`'s own docstring names monthly-grain `Date` as "the expected production usage" for Phase 2. The methodology document itself states the engine never parses or validates the `Period`/key-column values, so this did not invalidate any test result — Phase 2's monthly-grain usage in both the disqualified v1/v2/v3 rounds and the fresh evidence run is directly supported by the code's own stated default and docstring, checked line-by-line. This is flagged as a documentation-alignment item (the Test Instructions' example should be updated to match the implementation's actual recommended grain), not a functional defect, and does not block D11.
- **Test procedural-independence finding — resolved via fresh session, Cycle 3 Task 2.** An earlier Test session, when directly challenged, admitted that its stated justification for substituting quarters in its first (v1) round — framed as "the document's own explicit principle" — had actually been carried in from a prior conversation's memory, not from the text of `test_instructions_validation_methodology.md` as supplied that session. The governing document contains no such contingency clause. This is recorded as a genuine procedural-independence failure: memory-derived assumptions were presented as document-mandated rules. It was not caught by the Architect's own review until specifically pressed on it, which is itself a gap in that review, not just Test's. Resolved for Task 2 by commissioning a fresh Test session under an explicit no-prior-memory instruction, whose results the Architect then independently reproduced end-to-end rather than accepting on narration (see Section 9). The three original Return Reports (v1/v2/v3) are retained as historical record but are explicitly excluded from D11's evidence basis. **This same discipline (explicit no-external-sources clause, Incognito/memory-isolated execution, Architect reproduction) has been applied consistently since — to the headcount-direction fix's UAT, both Human Approval Gate Test sessions, and the register-semantics investigation and its evidence-reproducibility follow-up — now a consistently applied standing practice, not a one-off recovery.** Open question for future Test/Architect sessions on this project: whether this should be formally written into `architect_project_instructions.md` as a standing requirement rather than practiced consistently but informally — not decided here, flagged for the principal.
- **Narrative prompt drift — resolved, Cycle 2 Task 3 Item 3.** `northwind_narrative_prompt.md` confirmed byte-identical in substance to `rollups.py`'s `SYSTEM_PROMPT`/`build_user_prompt` for rule 2c, Section 4, and the data block. Root cause was a project synchronization gap (the `.md` never received Task 1's edit in the copy carried forward across project restarts), not a pipeline defect. Recovery followed the Configuration Integrity Check procedure: regenerated from the verified-correct code, not from an unverified archive.
- **Promotion-pipeline sync gap — resolved, Cycle 2 Task 4.** A previously-accepted fix (Task 3/Item 3 terminology correction) was verified in a Builder session but never written to the canonical file store, so it did not appear in the batch Test actually received. Distinct from the earlier narrative-prompt drift (stale copy carried across restarts) — this was a fix that simply never left the session it was made in. Addressed structurally via the new Canonical Sync Verification procedure, not by re-fixing this instance alone.
- **Dataset provenance gap — resolved, Cycle 3 Task 2.** Test Instructions assumed a dataset through Q4 FY2026 while the file actually supplied initially only ran through Q2 FY2026. Resolved over three rounds by supplying progressively later canonical datasets (Q2 → Q3 → Q4 FY2026); Test transparently flagged the shortfall at each round rather than silently substituting or fabricating. New standing procedure added: Dataset Provenance Discipline (Section "Source of Truth").
- **Dataset resolver-collision risk — resolved, Cycle 3 close-out.** Three dataset files existed as candidates after Task 2's testing sequence, all matching `rollups.py`'s resolver filter. Principal confirmed the Q4 file as sole canonical file at repo root (D12); Q2/Q3 files archived outside the resolver's search path.
- **Fixture internal-consistency gap — resolved, Handbook revision v2.10.** A constructed test fixture for the register-semantics investigation initially modified `Expenses` without recomputing the dependent `PL_Summary` fields, producing a genuine tie-out failure (`max diff $45,000.00`). Confirmed as a real defect, rebuilt with the matching adjustment, re-verified to a clean tie-out. Standing checklist added — see the Dataset Provenance Discipline corollary (Section "Source of Truth").
- **Trivial, non-blocking:** `rollups.py` lines 710 and 745 still say "segment margin" (not "contribution margin," so outside Criterion 4's literal scope, but inconsistent with D6's broader spirit of moving away from margin framing). Comments/print text only, no functional or criterion impact. Left as opportunistic cleanup, not worth a dedicated brief.
- **Deck/HTML story staleness:** `Northwind_Quarterly_AI_Deck.pptx` and `Northwind_Quarterly_AI_Story.html` are not in current Builder files (only a PDF version of the deck exists in Project). Do not reconstruct from the PDF — risks propagating stale content. Needs current files supplied; producing both, for both scenarios, is now an explicit final output of the End-to-End Product Demonstration milestone.
- **Write-up document not started** — the actual graded deliverable; everything else is supporting evidence for it. Deliberately **deferred until after the End-to-End Product Demonstration milestone completes successfully**, so that it can be grounded in an actual demonstrated run of the complete product, both scenarios, rather than component-level evidence alone.
- **Narrative-generation fallback behavior undecided.** With no `ANTHROPIC_API_KEY` present, the dashboard falls back to a "download prompt" button rather than generating narrative text. Observed in Test, not yet formally decided as intended Live behavior (consistent with the Human Approval Gate — a human reviews before anything narrative-facing publishes) vs. an unaddressed gap. This is directly relevant to both scenarios' executive-narrative step and should be decided before or during the current milestone. **The Human Approval Gate's own independent Test evidence (both sessions) was produced in an environment without `ANTHROPIC_API_KEY` set, so the Gate's blocking behavior is confirmed against the download-button path; confirming the identical guard against the live "Generate narrative" call path (API key present) remains open, though the guarding code itself does not differ between the two paths (both sit inside the same `if not _gate_approved:` block, confirmed by direct code read).**
- **Builder push credentials, recurring environment constraint (reconfirmed at Handbook revision v2.10).** Builder sessions in this project consistently lack GitHub push credentials, requiring a principal (or Architect-facilitated) push step after Builder's local commit and review are complete. This was true for the v4 Brief's final round and is true again for the Phase 7 period-display work. Not a defect in any individual Return Report — recorded here so it isn't mistaken for one in future review, and so future promotion planning accounts for it as a standing step, not a surprise.
- **v2.11 canonical synchronization — resolved, confirmed this revision (v2.12).** A fresh clone performed this revision resolves to `91f187b3fff971ceb8c7e600c5fbb615e01e8e9d` and contains `project_handbook.md` byte-identical to the v2.11 text (including the Repository Identity subsection) previously held in context; `governance/architect_project_instructions.md` at that commit likewise matches. No drift found on either file.
- **v2.12 canonical synchronization not yet verified.** Same standing constraint as v2.11/v2.13 below — drafted with read access but no push credentials.
- **v2.13 canonical synchronization not yet verified.** This revision (corrected Section 13 language; the Phase 4 Semantic Reconciliation Scope Finalization findings recorded in Section 9) was authored in a session with repository read access but no push credentials. A principal or Builder push step, followed by an Architect fresh-clone confirmation, is required before v2.13 can be described as canonical.
- **v2.14 canonical synchronization not yet verified.** Same standing constraint — the Brief's authorization was recorded in this session's copy of the Handbook, not yet pushed.
- **v2.15 canonical synchronization not yet verified.** This revision (`phase4_semantic_reconciliation_brief_v1.md` → `v2.md`, credential-security/API-cost scope refinement) was authored in a session with repository read access but no push credentials. Both the Handbook and the new Brief file need to be pushed together — the Handbook's "Current Builder Brief" field and the Brief's own Precedence line each name the other's version number, so a partial push (one file updated, the other not) would itself create a fresh documentation-drift instance of exactly the kind this project's discipline exists to catch. Push both in the same commit/session, then confirm via fresh clone.
- **Phase 4 semantic-reconciliation capability gap — identified, then finalized into a concrete Builder scope (not yet implemented).** Direct inspection of canonical `commentary_workflow.py` (`match_commentary_entries()`, `_find_canonical_terms()`) confirms Phase 4 matching is a deterministic keyword/fixed-synonym-dictionary matcher on Department and Category only (no Value field, no exclusivity enforcement, and a period-narrowing defect that can silently accept a wrong-period match — see the Phase 4 Semantic Reconciliation Scope Finalization report). Zero LLM/API calls exist in the Phase 4 path. This does not satisfy Section 2a's corrected Phase 4 requirement. A finalized, implementation-ready Builder scope now exists (external deliverable) but is **not yet authorized** — that requires principal review. See Section 13 for how this interacts with D13's overall Implementation Status (now two independent outstanding requirements, not one).

## 11. Roadmap / Future Ideas

- **End-to-End Product Demonstration (current milestone).** Two required scenarios, both ending in HTML story and PPTX:
  - *Scenario 1 — wrong/changed data:* intake → detect the problem (Phase 2/3, D11) → identify/match the issue (Phase 4, **implemented, regression-verified**) → request/handle Controller commentary (Phase 5, **implemented, regression-verified**) → validate the explanation (Phase 6, **implemented, regression-verified**) → human approval (**Human Approval Gate implemented and fully independently Test-verified**) → executive narrative (**period display independently verified**) → HTML story → PPTX.
  - *Scenario 2 — correct data:* intake → validation passes (Phase 2/3, no issues) → human approval (**Human Approval Gate implemented and fully independently Test-verified**) → executive narrative (**period display independently verified**) → HTML story → PPTX.
  - Phase 4–6 (v4/v5 scope plus the headcount-direction fix), the Human Approval Gate, and the Phase 7 period-display work are all built, canonical-synced, and fully independently verified; the integration step (composing them and the already-Verified components into one actual end-to-end run, for both scenarios) has not happened yet. Does not reopen or revise D10, D11, D12, or D14. Governed by D13, currently In Progress — not yet Verified. **Immediate next step:** the Section H/J independent UAT (Section 13) — the one remaining requirement before D13 reaches Verified and the two scenario runs themselves can begin.
- **Cost Structure Investigation Views (D14) — complete, Verified (v2.6).** Both the Department × Cost Category view and the company-wide Cost Category-only view are Built and Verified, ahead of Phase 4–6 as intended, giving the Controller a visible investigation path before Phase 4–6 testing begins. See Decision Log, Section 7, and Section 9.
- **The Human Approval Gate — complete.** Implemented, regression-verified, canonical-synced, and independently Test-verified across all eight acceptance criteria. Closes a precondition of D13; does not itself move D13 to Verified.
- **The Phase 7 period-display clarification and register-semantics investigation — complete, Handbook revision v2.10.** Implemented, regression-verified, canonical-synced, and independently verified end to end including empirical evidence. Closes two related Known Technical Debt items; does not itself move D13 to Verified.
- **Write-up document — final graded deliverable, next milestone after the demonstration above.** To be grounded in the actual completed End-to-End Product Demonstration (both scenarios), citing its evidence directly, rather than resting on component-level evidence alone.
- Phase 9 (Knowledge Archive — single log entry, framed as future architecture): parked, out of scope for the current milestone, likely post-deadline or opportunistic.
- Optional: video walkthrough, if time allows before Aug 18.

## 12. Active File Manifest

What should sit in the active Build Project vs. external archive, and why. Re-evaluate this table whenever the file set changes — see the accompanying PM recommendation for full reasoning.

| File | Category | Why |
|---|---|---|
| Project Handbook | Required every session | The index and source of truth; nothing else makes sense without it |
| Current Builder Brief | **`phase4_semantic_reconciliation_brief_v2.md` — Active** | Supersedes v1 in full (credential-security/API-cost boundary added, Sections 1–4 and Criteria 1–8 unchanged) and v5's Section B only. `human_approval_gate_brief_v1.md`, v4/v5/headcount-fix implementation, and the Phase 7 period-display addendum remain complete and unaffected. This is the one open Builder implementation task in the project as of v2.15 — authorized, not yet implemented, no Return Report yet. |
| `rollups.py` | Required every session | The single pipeline; nearly every task touches or depends on it, and its own tie-out checks are the primary verification mechanism |
| `assumptions_and_limitations.md` | Required every session | Small; must be checked/updated whenever a decision or limitation is touched, so its absence risks silent contradiction |
| `Northwind_Financial_Dashboard.py` | Required only for dashboard/UI-related tasks | Large file; needed when a task touches layout, tabs, or charts, not for pure pipeline or write-up work. Includes the Human Approval Gate's approve/reject controls, structural Phase 7 guard, and (as of `57b3aa24...`) consistent D14-convention period-display formatting throughout the Commentary Review section. |
| `northwind_narrative_prompt.md` | Required only for narrative/prompt-related tasks | Needed when a task touches narrative generation or prompt wording, not for pipeline-only or dashboard-layout-only work |
| `requirements.txt` | Required every session | Pins pandas/numpy/openpyxl/streamlit to the versions confirmed clean (Cycle 2 Task 4); a fresh unpinned install reproduces the pandas 3.0 `Styler.applymap` crash |
| `close_history.py` | Required every session | Storage-neutral Close History resolve/archive logic (D10); extended (v4 Brief) to capture the complete Commentary Record in the D10 snapshot at close approval |
| `close_validation.py` | Required every session | Phase 2 (historical-revision diff) + Phase 3 (plausibility/headcount-band) logic (D11); Verified Cycle 3 Task 2. Its threshold constants are also the basis for the Human Approval Gate's Case 5 fixture and the register-semantics investigation fixture (Section 9). |
| `commentary_workflow.py` | Required every session (Live batch) | Phase 4 (matching), Phase 5 (draft note), Phase 6 (four-check validation) logic — v4 Brief scope, plus the headcount-direction fix, plus (as of `57b3aa24...`) the Phase 7 period-display formatter threading and the register-semantics documentation clarification. Implementation-complete, regression-verified, canonical-synced. Not yet Verified — see D13. |
| `Northwind_Sample_Dataset.xlsx` (canonical, single file — the Q4 FY2026 version) | Required every session | Raw data source `rollups.py` reads via its dataset resolution path; not generated by the pipeline, must be supplied. **Resolved (D12): this is now the only dataset file at repo root.** |
| `close_v1_v2_simulation.py` | Build/Test only — never Live | Verification tool for Phase 2/3 logic; imported by `close_orchestrator.py`. Not part of the Live batch — Live's Close History starts from real data, not simulation output. **Frozen as of Cycle 3, Task 2** — regression-only fixture, not evidence of generic correctness (see Validation Independence Principle). Not extended or modified by Build going forward. |
| `close_orchestrator.py` | Build/Test only — never Live | Same category as `close_v1_v2_simulation.py`: verifies Close History bootstrap + normal-path resolution logic. Unconditionally deletes any pre-existing `close_history/` on every run, by design, to prove the bootstrap path for real — must never run against a populated production Close History. |
| `build_test/commentary_workflow_demo.py` | Build/Test only — never Live | Builder-authored regression fixture for `commentary_workflow.py` (49 checks) — proves non-regression against a fixture Builder itself authored and knew in advance. Per the Validation Independence Principle, this is explicitly not evidence of genericity; that requires the separate, principal-supplied Section H/J UAT set, not yet run. |
| `Northwind__Sample_Dataset_-_Q2_26.xlsx`, `...Q3_26.xlsx` | Archive only — testing/provenance artifacts, outside resolver's search path | Retained for traceability of Task 2's v1→v2→v3 evidence chain (D12); must not sit at repo root alongside the canonical Q4 file or the resolver's "exactly one match" contract breaks. Supplied to Builder for Criterion 9 and used — see Section 10 for resolution. |
| `incoming_test2_RECONCILED.xlsx` (register-semantics investigation fixture) | Archive only — testing/demonstration artifact | Constructed, fully-specified, internally-consistent fixture used to empirically demonstrate the register-semantics investigation's Item 5 finding (Section 9); not part of the Live dataset, not the canonical dataset, must not sit at repo root. Retained for traceability of this revision's evidence chain. |
| Archived Builder Briefs | Archive only | Traceability; content already distilled into the Decision Log and Section 8. The v1 Cost Structure Brief, the pre-correction v2 draft, Phase 4-6 v1-v5, the headcount-direction fix Brief, the Human Approval Gate Brief, and the Phase 7 period-display addendum are all archived, superseded/complete, with content already distilled here. |

**Dataset location (decided Cycle 3, Task 2 — Option B, confirmed by direct implementation inspection):** the bootstrap dataset lives at **repo root**, alongside the application files — not in a `data/` subdirectory. This matches the implementation exactly as built: `rollups.py` and `close_v1_v2_simulation.py` both resolve it via `glob.glob("*.xlsx")` in the current working directory, filtering for filenames containing `northwind`, `sample`, and `dataset` (case-insensitive) while excluding `output` — not an exact filename match. Exactly one match must exist, or the code raises an error rather than guessing. `NORTHWIND_RAW_DATASET_PATH` is the built-in override for any deployment that needs a different location — this same override was used, unmodified, to point the dashboard at the Human Approval Gate's Case 5 fixture, and again at the register-semantics investigation fixture, without touching the resolver or the canonical dataset at repo root. A `data/` subdirectory was considered and explicitly rejected for now — it would require a code change that doesn't exist yet; adopting it as documented "architecture" ahead of the implementation would recreate the exact Handbook/code mismatch this project has repeatedly had to resolve. Revisit only alongside an actual code change, not as a documentation update in isolation.

**Canonical dataset file selection (D12, resolved at Cycle 3 close-out):** the Q4 FY2026 file (Jul 2023–Jun 2026) is the single canonical dataset at repo root. The Q2 FY2026 and Q3 FY2026 files used during Task 2's provenance sequence are retained only as archived testing/provenance artifacts supporting the v1→v2→v3 evidence chain in the Task 2 Return Reports — they must not be committed at repo root alongside the canonical file, since the resolver's glob-based filter would then match more than one file and raise an error rather than resolving cleanly. This closes the open item flagged at the end of Task 2's review.

**Filename resolution precedent (Cycle 3, Task 2):** `Northwind_Financial_Dashboard.py` (underscore) was confirmed canonical by direct inspection of the actual file on disk, which contradicted its own internal docstring (which incorrectly claimed a space-variant run command) and contradicted this Handbook's prior text. The actual artifact established the canonical filename; the Handbook and the stale code comment were corrected to match the directly inspected artifact. Evidence from the actual artifact established the factual implementation state; the Handbook was then updated to record that state. This Handbook remains the governance source of truth — directly inspected artifacts establish what is actually present when a discrepancy is discovered, and the Handbook is updated accordingly; this is not a general rule that code automatically outranks the Handbook. If this file is ever renamed in the future, update here deliberately, not by inference.

**Open configuration-integrity item, status unconfirmed:** as of the Builder Project Instructions text supplied to the Architect, the "Tools & environment" section states *"Dashboard filename: `Northwind Financial_Dashboard.py` (space before "Financial", not underscore)"* — this directly conflicts with the precedent above. **The Architect has no tool access to the Builder Project's Instructions field and cannot independently verify whether this has since been corrected.** This is recorded as open, not resolved, until the principal either confirms the correction was made or supplies the updated field text for direct inspection. **Required correction, once access is confirmed:** replace the filename claim with a pointer to this Handbook's Active File Manifest (this section) rather than restating a filename fact independently — a static fact copied into a second document is exactly the drift pattern (see the narrative-prompt and promotion-pipeline incidents, Section 10) this project has repeatedly had to recover from.

| Archived Builder Return Reports | Archive only | Traceability; content already distilled into the Review & Acceptance History |
| `rollups_output.xlsx` | Regenerate while in progress; kept once archived | While a close is in progress: fully reproduced by running `rollups.py`, no architectural knowledge, don't keep. Once a close is approved: copied into that close's Close History snapshot as part of the permanent audit trail (D10) — same file, different rule depending on state. |

## 13. D13 Independent UAT — Readiness

Phase 4–6 implementation (v4, v5, and the headcount-direction fix, all canonical-synced) is complete. The Human Approval Gate — the interactive control enforcing D13's blocking requirement — is complete and fully independently Test-verified. **The Phase 7 period-display clarification and register-semantics investigation (commit `57b3aa24...`) are also now complete and fully independently verified.** **Corrected, this revision (v2.13): there are now two distinct, independent outstanding requirements for D13 to reach Verified, not one.** Requirement 1 (below, unchanged from prior revisions) concerns the Phase 4-6 implementation as it exists today. Requirement 2 (new) concerns a corrected product definition (Section 2a) that today's Phase 4 implementation does not satisfy, per direct code inspection — see the Architect Documentation & Alignment Report, "Phase 4 Semantic Reconciliation Scope Finalization" task, for the full evidence chain, the finalized Builder scope, and its own UAT specification. Neither requirement is a substitute for the other; both must be satisfied before D13 can reach Verified.

**Requirement 1 — What's already true, independently confirmed (unchanged from prior revisions):**
- Implementation evidence: direct code review of `commentary_workflow.py` (Phase 4/5/6 core, including the headcount-direction fix and the period-display formatter threading) and `build_narrative_commentary_section()` (v5's Section G handoff).
- Builder regression evidence: 49/49 (v4 fixture) + 23/23 (v5 fixture) + 23/23 (headcount-direction mandatory tests), all independently reproduced byte-identical by the Architect. The Phase 7 period-display work additionally confirmed zero-diff against these same fixtures.
- Canonical synchronization: `789351b`, `5709c9e`, `4a670d0` (headcount-direction fix), `cdfeb5701...` (Human Approval Gate), and `57b3aa24...` (Phase 7 period-display work) all fresh-clone-confirmed.
- **The Human Approval Gate's own eight acceptance criteria are independently evidenced in full** — a separate precondition, now closed, not a substitute for what remains below.
- **The Phase 7 period-display clarification's own investigation is independently evidenced in full, including empirical, executed matching-test evidence** — a separate, display/documentation-layer item, also not a substitute for what remains below.

**Requirement 1 — what's missing:** independent Test evidence of the Section H/J Phase 4-6 UAT set (Cases A/B/C plus the correction loop, against the implementation as it exists today), per the Validation Independence Principle. Builder's own regression fixtures prove non-regression against data Builder itself authored and knew in advance; they cannot prove genericity. This is the same bar D11 had to clear (Section 9) before its own Verified status, and the same bar the Human Approval Gate's Cases 1-8 and the register-semantics investigation have now cleared — but for the Phase 4-6 commentary-matching/validation set specifically, this step has not yet been supplied.

**Requirement 2 — authorized (v2.14), scope refined (v2.15), not yet implemented:** `phase4_semantic_reconciliation_brief_v2.md` is now **Active**, correcting Phase 4 matching to (a) require Department, Category, Value, and Period-when-explicitly-stated for a deterministic match, (b) enforce one-observation-to-one-commentary exclusivity, (c) invoke semantic/LLM-assisted reconciliation only as a bounded exception path for commentary left unresolved after deterministic matching, against only observations left unmatched, and (d) — added v2 — bound API cost consumption and protect the public project's Anthropic credential from exposure (Section 5a, Criteria 9–10). None of this is implemented in canonical `commentary_workflow.py` or `Northwind_Financial_Dashboard.py` as of the last fresh clone (HEAD `91f187b3fff971ceb8c7e600c5fbb615e01e8e9d`) — confirmed by direct code inspection, see the Architect Documentation & Alignment Report ("Phase 4 Semantic Reconciliation Scope Finalization"), Section 2. This requirement is not satisfied by Requirement 1's UAT set, which tests the current (unchanged) implementation, not the corrected one. **Authorization ≠ implementation ≠ verification — these remain three separate states.** Once Builder implements against this Brief: Builder regression evidence is required for Criteria 1, 2, 3, 5, 6, 8, 9, 10 and the logic half of 7 of the Brief's Section 7; independent Test evidence (authored outside Builder, per the Validation Independence Principle) is separately required for Criterion 4's semantic-match case (UAT Cases B/C) and the live-call half of Criterion 7 before this requirement can be considered evidenced.

**What the principal needs to supply (v5 Brief, Section H):**
1. **Target flagged observation** — a real D11 flag from the canonical dataset, or a controlled fixture (either is acceptable per the Brief; must be stated explicitly, not left ambiguous).
2. **Case A — Supported:** a commentary entry with a specific, checkable claim that the evidence package corroborates.
3. **Case B — Contradicted:** a commentary entry with a specific, checkable claim that the evidence package conflicts with.
4. **Case C — Insufficient:** a commentary entry with no specific claim, or a claim type the evidence package can't check.
5. **A correction-loop revision** for at least one of Case B or C — a second, complete commentary text (not a fragment) that a finance/CFO user would submit after seeing the first result, to exercise the resubmit → revalidate → accept path.
6. Per v5's addition to Section H: confirmation of whether the same target observation's UAT run will also be repeated **with no commentary file supplied at all**, to exercise Section G Case 3 (the "unexplained" path) against real content rather than only the fixture's synthetic register.

**A note for whoever authors this content, given the register-semantics investigation's findings:** per the now-settled D14/Phase 7 period-display convention, any period reference in the supplied commentary text should use the `"Q# YYYY"` form (e.g. `"Q3 2026"`), not the `"Q# FYYYY"` form (e.g. `"Q3 FY2026"`) — the latter will not match the observation register's `Period` field, by design, per that investigation (Section 9).

**What the Architect will do once that's supplied:** run the same independent-verification discipline applied throughout this project — predictions recorded before execution, actual output checked against them, results reviewed against the v5 Brief's acceptance criteria (Sections B, C, D, E, F, G's numbered criteria) — and report Pass/Not Pass with evidence, the same as every other review in this project. The Architect does not author or tune this content (Section H); doing so would defeat the purpose of the check.

**Once independent Test evidence exists and is reviewed:** D13 moves to Verified (pending that review's outcome), which unblocks the End-to-End Product Demonstration's Scenario 1 as genuine evidence rather than a run against Builder's own fixture.

---

*Handbook conventions: this file is edited in place, not appended to. Superseded content is removed, not struck through. The Decision Log and Review & Acceptance History are the only sections that grow purely by addition; everything else reflects only the current state.*
